-- THIS FILE WAS AUTOMATICALLY GENERATED BY AENEAS
-- [multi_agent]
import Aeneas
open Aeneas Aeneas.Std Result ControlFlow Error
set_option linter.dupNamespace false
set_option linter.hashCommand false
set_option linter.unusedVariables false

/- You can set the `maxHeartbeats` value with the `-max-heartbeats` CLI option -/
set_option maxHeartbeats 1000000

/- You can remove the following line by using the CLI option `-all-computable`: -/
noncomputable section

namespace multi_agent

/-- [core::cmp::impls::{core::cmp::PartialEq<bool> for bool}::ne]:
    Source: '/rustc/library/core/src/cmp.rs', lines 1872:16-1872:50
    Name pattern: [core::cmp::impls::{core::cmp::PartialEq<bool, bool>}::ne] -/
@[rust_fun "core::cmp::impls::{core::cmp::PartialEq<bool, bool>}::ne"]
axiom Bool.Insts.CoreCmpPartialEqBool.ne : Bool → Bool → Result Bool

/-- [core::fmt::{core::fmt::Formatter<'a>}::debug_struct_field2_finish]:
    Source: '/rustc/library/core/src/fmt/mod.rs', lines 2466:4-2473:15
    Name pattern: [core::fmt::{core::fmt::Formatter<'a>}::debug_struct_field2_finish] -/
@[rust_fun "core::fmt::{core::fmt::Formatter<'a>}::debug_struct_field2_finish"]
axiom core.fmt.Formatter.debug_struct_field2_finish
  :
  core.fmt.Formatter → Str → Str → Dyn (fun _dyn => core.fmt.Debug _dyn)
    → Str → Dyn (fun _dyn => core.fmt.Debug _dyn) → Result
    ((core.result.Result Unit core.fmt.Error) × core.fmt.Formatter)

/-- [core::fmt::{core::fmt::Formatter<'a>}::debug_struct_field3_finish]:
    Source: '/rustc/library/core/src/fmt/mod.rs', lines 2485:4-2494:15
    Name pattern: [core::fmt::{core::fmt::Formatter<'a>}::debug_struct_field3_finish] -/
@[rust_fun "core::fmt::{core::fmt::Formatter<'a>}::debug_struct_field3_finish"]
axiom core.fmt.Formatter.debug_struct_field3_finish
  :
  core.fmt.Formatter → Str → Str → Dyn (fun _dyn => core.fmt.Debug _dyn)
    → Str → Dyn (fun _dyn => core.fmt.Debug _dyn) → Str → Dyn (fun _dyn
    => core.fmt.Debug _dyn) → Result ((core.result.Result Unit
    core.fmt.Error) × core.fmt.Formatter)

/-- [core::fmt::{core::fmt::Formatter<'a>}::debug_struct_field5_finish]:
    Source: '/rustc/library/core/src/fmt/mod.rs', lines 2532:4-2545:15
    Name pattern: [core::fmt::{core::fmt::Formatter<'a>}::debug_struct_field5_finish] -/
@[rust_fun "core::fmt::{core::fmt::Formatter<'a>}::debug_struct_field5_finish"]
axiom core.fmt.Formatter.debug_struct_field5_finish
  :
  core.fmt.Formatter → Str → Str → Dyn (fun _dyn => core.fmt.Debug _dyn)
    → Str → Dyn (fun _dyn => core.fmt.Debug _dyn) → Str → Dyn (fun _dyn
    => core.fmt.Debug _dyn) → Str → Dyn (fun _dyn => core.fmt.Debug _dyn)
    → Str → Dyn (fun _dyn => core.fmt.Debug _dyn) → Result
    ((core.result.Result Unit core.fmt.Error) × core.fmt.Formatter)

/-- [core::fmt::{core::fmt::Debug for (U, T)}::fmt]:
    Source: '/rustc/library/core/src/fmt/mod.rs', lines 3082:16-3082:62
    Name pattern: [core::fmt::{core::fmt::Debug<(@U, @T)>}::fmt] -/
@[rust_fun "core::fmt::{core::fmt::Debug<(@U, @T)>}::fmt"]
axiom Pair.Insts.CoreFmtDebug.fmt
  {U : Type} {T : Type} (DebugInst : core.fmt.Debug U) (DebugInst1 :
  core.fmt.Debug T) :
  (U × T) → core.fmt.Formatter → Result ((core.result.Result Unit
    core.fmt.Error) × core.fmt.Formatter)

/-- Trait implementation: [core::fmt::{core::fmt::Debug for (U, T)}]
    Source: '/rustc/library/core/src/fmt/mod.rs', lines 3080:12-3080:57
    Name pattern: [core::fmt::Debug<(@U, @T)>] -/
@[reducible, rust_trait_impl "core::fmt::Debug<(@U, @T)>"]
def Pair.Insts.CoreFmtDebug {U : Type} {T : Type} (DebugInst : core.fmt.Debug
  U) (DebugInst1 : core.fmt.Debug T) : core.fmt.Debug (U × T) := {
  fmt := Pair.Insts.CoreFmtDebug.fmt DebugInst DebugInst1
}

/-- [core::iter::adapters::filter::Filter]
    Source: '/rustc/library/core/src/iter/adapters/filter.rs', lines 21:0-21:23
    Name pattern: [core::iter::adapters::filter::Filter] -/
@[rust_type "core::iter::adapters::filter::Filter"]
axiom core.iter.adapters.filter.Filter (I : Type) (P : Type) : Type

/-- [core::iter::adapters::filter::{core::iter::traits::iterator::Iterator<Clause0_Item> for core::iter::adapters::filter::Filter<I, P>}::count]:
    Source: '/rustc/library/core/src/iter/adapters/filter.rs', lines 135:4-135:27
    Name pattern: [core::iter::adapters::filter::{core::iter::traits::iterator::Iterator<core::iter::adapters::filter::Filter<@I, @P>, @Clause0_Item>}::count] -/
@[rust_fun
  "core::iter::adapters::filter::{core::iter::traits::iterator::Iterator<core::iter::adapters::filter::Filter<@I, @P>, @Clause0_Item>}::count"]
axiom
  core.iter.adapters.filter.Filter.Insts.CoreIterTraitsIteratorIterator.count
  {I : Type} {P : Type} {Clause0_Item : Type} (traitsiteratorIteratorInst :
  core.iter.traits.iterator.Iterator I Clause0_Item)
  (opsfunctionFnMutPTupleSharedClause0_ItemBoolInst : core.ops.function.FnMut P
  Clause0_Item Bool) :
  core.iter.adapters.filter.Filter I P → Result Std.Usize

/-- [core::iter::adapters::map::{core::iter::traits::iterator::Iterator<B> for core::iter::adapters::map::Map<I, F>}::collect]:
    Source: '/rustc/library/core/src/iter/adapters/map.rs', lines 99:0-101:27
    Name pattern: [core::iter::adapters::map::{core::iter::traits::iterator::Iterator<core::iter::adapters::map::Map<@I, @F>, @B>}::collect] -/
@[rust_fun
  "core::iter::adapters::map::{core::iter::traits::iterator::Iterator<core::iter::adapters::map::Map<@I, @F>, @B>}::collect"]
axiom core.iter.adapters.map.Map.Insts.CoreIterTraitsIteratorIterator.collect
  {B : Type} {I : Type} {F : Type} {B1 : Type} {Clause0_Item : Type}
  (traitsiteratorIteratorInst : core.iter.traits.iterator.Iterator I
  Clause0_Item) (opsfunctionFnMutFTupleClause0_ItemBInst :
  core.ops.function.FnMut F Clause0_Item B) (traitscollectFromIteratorInst :
  core.iter.traits.collect.FromIterator B1 B) :
  core.iter.adapters.map.Map I F → Result B1

/-- [core::iter::traits::iterator::Iterator::count]:
    Source: '/rustc/library/core/src/iter/traits/iterator.rs', lines 225:4-227:20
    Name pattern: [core::iter::traits::iterator::Iterator::count] -/
@[rust_fun "core::iter::traits::iterator::Iterator::count"]
axiom core.iter.traits.iterator.Iterator.count.default
  {Self : Type} {Clause0_Item : Type} (IteratorInst :
  core.iter.traits.iterator.Iterator Self Clause0_Item) :
  Self → Result Std.Usize

/-- [core::iter::traits::iterator::Iterator::map]:
    Source: '/rustc/library/core/src/iter/traits/iterator.rs', lines 789:4-792:34
    Name pattern: [core::iter::traits::iterator::Iterator::map] -/
@[rust_fun "core::iter::traits::iterator::Iterator::map"]
axiom core.iter.traits.iterator.Iterator.map.default
  {Self : Type} {B : Type} {F : Type} {Clause0_Item : Type} (IteratorInst :
  core.iter.traits.iterator.Iterator Self Clause0_Item)
  (opsfunctionFnMutFTupleClause0_ItemBInst : core.ops.function.FnMut F
  Clause0_Item B) :
  Self → F → Result (core.iter.adapters.map.Map Self F)

/-- [core::iter::traits::iterator::Iterator::filter]:
    Source: '/rustc/library/core/src/iter/traits/iterator.rs', lines 911:4-914:38
    Name pattern: [core::iter::traits::iterator::Iterator::filter] -/
@[rust_fun "core::iter::traits::iterator::Iterator::filter"]
axiom core.iter.traits.iterator.Iterator.filter.default
  {Self : Type} {P : Type} {Clause0_Item : Type} (IteratorInst :
  core.iter.traits.iterator.Iterator Self Clause0_Item)
  (opsfunctionFnMutPTupleSharedClause0_ItemBoolInst : core.ops.function.FnMut P
  Clause0_Item Bool) :
  Self → P → Result (core.iter.adapters.filter.Filter Self P)

/-- [core::mem::take]:
    Source: '/rustc/library/core/src/mem/mod.rs', lines 820:0-820:56
    Name pattern: [core::mem::take] -/
@[rust_fun "core::mem::take"]
axiom core.mem.take
  {T : Type} (defaultDefaultInst : core.default.Default T) :
  T → Result (T × T)

/-- [core::num::{u32}::is_multiple_of]:
    Source: '/rustc/library/core/src/num/uint_macros.rs', lines 3682:8-3682:60
    Name pattern: [core::num::{u32}::is_multiple_of] -/
@[rust_fun "core::num::{u32}::is_multiple_of"]
axiom core.num.U32.is_multiple_of : Std.U32 → Std.U32 → Result Bool

/-- [core::slice::iter::{core::iter::traits::iterator::Iterator<&'a (T)> for core::slice::iter::Iter<'a, T>}::count]:
    Source: '/rustc/library/core/src/slice/iter/macros.rs', lines 224:12-224:35
    Name pattern: [core::slice::iter::{core::iter::traits::iterator::Iterator<core::slice::iter::Iter<'a, @T>, &'a @T>}::count] -/
@[rust_fun
  "core::slice::iter::{core::iter::traits::iterator::Iterator<core::slice::iter::Iter<'a, @T>, &'a @T>}::count"]
axiom core.slice.iter.Iter.Insts.CoreIterTraitsIteratorIteratorSharedAT.count
  {T : Type} : core.slice.iter.Iter T → Result Std.Usize

/-- [core::slice::iter::{core::iter::traits::iterator::Iterator<&'a (T)> for core::slice::iter::Iter<'a, T>}::collect]:
    Source: '/rustc/library/core/src/slice/iter/macros.rs', lines 153:8-153:45
    Name pattern: [core::slice::iter::{core::iter::traits::iterator::Iterator<core::slice::iter::Iter<'a, @T>, &'a @T>}::collect] -/
@[rust_fun
  "core::slice::iter::{core::iter::traits::iterator::Iterator<core::slice::iter::Iter<'a, @T>, &'a @T>}::collect"]
axiom core.slice.iter.Iter.Insts.CoreIterTraitsIteratorIteratorSharedAT.collect
  {T : Type} {B : Type} (itertraitscollectFromIteratorBSharedATInst :
  core.iter.traits.collect.FromIterator B T) :
  core.slice.iter.Iter T → Result B

/-- [core::slice::iter::{core::iter::traits::iterator::Iterator<&'a (T)> for core::slice::iter::Iter<'a, T>}::filter]:
    Source: '/rustc/library/core/src/slice/iter/macros.rs', lines 153:8-153:45
    Name pattern: [core::slice::iter::{core::iter::traits::iterator::Iterator<core::slice::iter::Iter<'a, @T>, &'a @T>}::filter] -/
@[rust_fun
  "core::slice::iter::{core::iter::traits::iterator::Iterator<core::slice::iter::Iter<'a, @T>, &'a @T>}::filter"]
axiom core.slice.iter.Iter.Insts.CoreIterTraitsIteratorIteratorSharedAT.filter
  {T : Type} {P : Type} (opsfunctionFnMutPTupleSharedASharedATBoolInst :
  core.ops.function.FnMut P T Bool) :
  core.slice.iter.Iter T → P → Result (core.iter.adapters.filter.Filter
    (core.slice.iter.Iter T) P)

/-- [core::slice::iter::{core::iter::traits::iterator::Iterator<&'a (T)> for core::slice::iter::Iter<'a, T>}::map]:
    Source: '/rustc/library/core/src/slice/iter/macros.rs', lines 153:8-153:45
    Name pattern: [core::slice::iter::{core::iter::traits::iterator::Iterator<core::slice::iter::Iter<'a, @T>, &'a @T>}::map] -/
@[rust_fun
  "core::slice::iter::{core::iter::traits::iterator::Iterator<core::slice::iter::Iter<'a, @T>, &'a @T>}::map"]
axiom core.slice.iter.Iter.Insts.CoreIterTraitsIteratorIteratorSharedAT.map
  {T : Type} {B : Type} {F : Type} (opsfunctionFnMutFTupleSharedATBInst :
  core.ops.function.FnMut F T B) :
  core.slice.iter.Iter T → F → Result (core.iter.adapters.map.Map
    (core.slice.iter.Iter T) F)

/-- [core::tuple::{core::cmp::PartialEq<(U, T)> for (U, T)}::ne]:
    Source: '/rustc/library/core/src/tuple.rs', lines 34:16-34:55
    Name pattern: [core::tuple::{core::cmp::PartialEq<(@U, @T), (@U, @T)>}::ne] -/
@[rust_fun "core::tuple::{core::cmp::PartialEq<(@U, @T), (@U, @T)>}::ne"]
axiom Pair.Insts.CoreCmpPartialEqPair.ne
  {U : Type} {T : Type} (cmpPartialEqInst : core.cmp.PartialEq U U)
  (cmpPartialEqInst1 : core.cmp.PartialEq T T) :
  (U × T) → (U × T) → Result Bool

/-- [core::tuple::{core::cmp::PartialEq<(U, T)> for (U, T)}::eq]:
    Source: '/rustc/library/core/src/tuple.rs', lines 30:16-30:55
    Name pattern: [core::tuple::{core::cmp::PartialEq<(@U, @T), (@U, @T)>}::eq] -/
@[rust_fun "core::tuple::{core::cmp::PartialEq<(@U, @T), (@U, @T)>}::eq"]
axiom Pair.Insts.CoreCmpPartialEqPair.eq
  {U : Type} {T : Type} (cmpPartialEqInst : core.cmp.PartialEq U U)
  (cmpPartialEqInst1 : core.cmp.PartialEq T T) :
  (U × T) → (U × T) → Result Bool

/-- Trait implementation: [core::tuple::{core::cmp::PartialEq<(U, T)> for (U, T)}]
    Source: '/rustc/library/core/src/tuple.rs', lines 28:12-28:74
    Name pattern: [core::cmp::PartialEq<(@U, @T), (@U, @T)>] -/
@[reducible, rust_trait_impl "core::cmp::PartialEq<(@U, @T), (@U, @T)>"]
def Pair.Insts.CoreCmpPartialEqPair {U : Type} {T : Type} (cmpPartialEqInst :
  core.cmp.PartialEq U U) (cmpPartialEqInst1 : core.cmp.PartialEq T T) :
  core.cmp.PartialEq (U × T) (U × T) := {
  eq := Pair.Insts.CoreCmpPartialEqPair.eq cmpPartialEqInst cmpPartialEqInst1
  ne := Pair.Insts.CoreCmpPartialEqPair.ne cmpPartialEqInst cmpPartialEqInst1
}

/-- [alloc::vec::into_iter::{core::iter::traits::iterator::Iterator<T> for alloc::vec::into_iter::IntoIter<T, A>}::count]:
    Source: '/rustc/library/alloc/src/vec/into_iter.rs', lines 263:4-263:27
    Name pattern: [alloc::vec::into_iter::{core::iter::traits::iterator::Iterator<alloc::vec::into_iter::IntoIter<@T, @A>, @T>}::count] -/
@[rust_fun
  "alloc::vec::into_iter::{core::iter::traits::iterator::Iterator<alloc::vec::into_iter::IntoIter<@T, @A>, @T>}::count"]
axiom alloc.vec.into_iter.IntoIter.Insts.CoreIterTraitsIteratorIterator.count
  {T : Type} {A : Type} : alloc.vec.into_iter.IntoIter T → Result Std.Usize

/-- [alloc::vec::into_iter::{core::iter::traits::iterator::Iterator<T> for alloc::vec::into_iter::IntoIter<T, A>}::collect]:
    Source: '/rustc/library/alloc/src/vec/into_iter.rs', lines 210:0-210:49
    Name pattern: [alloc::vec::into_iter::{core::iter::traits::iterator::Iterator<alloc::vec::into_iter::IntoIter<@T, @A>, @T>}::collect] -/
@[rust_fun
  "alloc::vec::into_iter::{core::iter::traits::iterator::Iterator<alloc::vec::into_iter::IntoIter<@T, @A>, @T>}::collect"]
axiom alloc.vec.into_iter.IntoIter.Insts.CoreIterTraitsIteratorIterator.collect
  {T : Type} {A : Type} {B : Type} (coreitertraitscollectFromIteratorInst :
  core.iter.traits.collect.FromIterator B T) :
  alloc.vec.into_iter.IntoIter T → Result B

/-- [alloc::vec::into_iter::{core::iter::traits::iterator::Iterator<T> for alloc::vec::into_iter::IntoIter<T, A>}::filter]:
    Source: '/rustc/library/alloc/src/vec/into_iter.rs', lines 210:0-210:49
    Name pattern: [alloc::vec::into_iter::{core::iter::traits::iterator::Iterator<alloc::vec::into_iter::IntoIter<@T, @A>, @T>}::filter] -/
@[rust_fun
  "alloc::vec::into_iter::{core::iter::traits::iterator::Iterator<alloc::vec::into_iter::IntoIter<@T, @A>, @T>}::filter"]
axiom alloc.vec.into_iter.IntoIter.Insts.CoreIterTraitsIteratorIterator.filter
  {T : Type} {A : Type} {P : Type} (coreopsfunctionFnMutPTupleSharedTBoolInst :
  core.ops.function.FnMut P T Bool) :
  alloc.vec.into_iter.IntoIter T → P → Result
    (core.iter.adapters.filter.Filter (alloc.vec.into_iter.IntoIter T) P)

/-- [alloc::vec::{alloc::vec::Vec<T>}::remove]:
    Source: '/rustc/library/alloc/src/vec/mod.rs', lines 2276:4-2276:47
    Name pattern: [alloc::vec::{alloc::vec::Vec<@T>}::remove] -/
@[rust_fun "alloc::vec::{alloc::vec::Vec<@T>}::remove"]
axiom alloc.vec.Vec.remove
  {T : Type} (A : Type) :
  alloc.vec.Vec T → Std.Usize → Result (T × (alloc.vec.Vec T))

/-- [alloc::vec::{alloc::vec::Vec<T>}::is_empty]:
    Source: '/rustc/library/alloc/src/vec/mod.rs', lines 2956:4-2956:40
    Name pattern: [alloc::vec::{alloc::vec::Vec<@T>}::is_empty] -/
@[rust_fun "alloc::vec::{alloc::vec::Vec<@T>}::is_empty"]
axiom alloc.vec.Vec.is_empty
  {T : Type} (A : Type) : alloc.vec.Vec T → Result Bool

/-- [alloc::vec::{core::iter::traits::collect::IntoIterator<&'a (T), core::slice::iter::Iter<'a, T>> for &'a (alloc::vec::Vec<T>)}::into_iter]:
    Source: '/rustc/library/alloc/src/vec/mod.rs', lines 3848:4-3848:40
    Name pattern: [alloc::vec::{core::iter::traits::collect::IntoIterator<&'a alloc::vec::Vec<@T>, &'a @T, core::slice::iter::Iter<'a, @T>>}::into_iter] -/
@[rust_fun
  "alloc::vec::{core::iter::traits::collect::IntoIterator<&'a alloc::vec::Vec<@T>, &'a @T, core::slice::iter::Iter<'a, @T>>}::into_iter"]
axiom SharedAVec.Insts.CoreIterTraitsCollectIntoIteratorSharedATIter.into_iter
  {T : Type} (A : Type) : alloc.vec.Vec T → Result (core.slice.iter.Iter T)

/-- [alloc::vec::{core::iter::traits::collect::Extend<T> for alloc::vec::Vec<T>}::extend]:
    Source: '/rustc/library/alloc/src/vec/mod.rs', lines 3867:4-3867:60
    Name pattern: [alloc::vec::{core::iter::traits::collect::Extend<alloc::vec::Vec<@T>, @T>}::extend] -/
@[rust_fun
  "alloc::vec::{core::iter::traits::collect::Extend<alloc::vec::Vec<@T>, @T>}::extend"]
axiom alloc.vec.Vec.Insts.CoreIterTraitsCollectExtend.extend
  {T : Type} (A : Type) {I : Type} {Clause1_IntoIter : Type}
  (coreitertraitscollectIntoIteratorInst :
  core.iter.traits.collect.IntoIterator I T Clause1_IntoIter) :
  alloc.vec.Vec T → I → Result (alloc.vec.Vec T)

/-- [alloc::vec::{core::default::Default for alloc::vec::Vec<T>}::default]:
    Source: '/rustc/library/alloc/src/vec/mod.rs', lines 4171:4-4171:26
    Name pattern: [alloc::vec::{core::default::Default<alloc::vec::Vec<@T>>}::default] -/
@[rust_fun
  "alloc::vec::{core::default::Default<alloc::vec::Vec<@T>>}::default"]
axiom alloc.vec.Vec.Insts.CoreDefaultDefault.default
  (T : Type) : Result (alloc.vec.Vec T)

/-- Trait implementation: [alloc::vec::{core::default::Default for alloc::vec::Vec<T>}]
    Source: '/rustc/library/alloc/src/vec/mod.rs', lines 4167:0-4167:32
    Name pattern: [core::default::Default<alloc::vec::Vec<@T>>] -/
@[reducible, rust_trait_impl "core::default::Default<alloc::vec::Vec<@T>>"]
def alloc.vec.Vec.Insts.CoreDefaultDefault (T : Type) : core.default.Default
  (alloc.vec.Vec T) := {
  default := alloc.vec.Vec.Insts.CoreDefaultDefault.default T
}

/-- [multi_agent::agent_trait::AgentState]
    Source: 'src/agent_trait.rs', lines 6:0-11:1 -/
@[discriminant isize]
inductive agent_trait.AgentState where
| Ready : agent_trait.AgentState
| Busy : agent_trait.AgentState
| Finished : agent_trait.AgentState
| Failed : agent_trait.AgentState

/-- [multi_agent::agent_trait::{core::clone::Clone for multi_agent::agent_trait::AgentState}::clone]:
    Source: 'src/agent_trait.rs', lines 5:9-5:14 -/
def agent_trait.AgentState.Insts.CoreCloneClone.clone
  (self : agent_trait.AgentState) : Result agent_trait.AgentState := do
  ok self

/-- Trait implementation: [multi_agent::agent_trait::{core::clone::Clone for multi_agent::agent_trait::AgentState}]
    Source: 'src/agent_trait.rs', lines 5:9-5:14 -/
@[reducible]
def agent_trait.AgentState.Insts.CoreCloneClone : core.clone.Clone
  agent_trait.AgentState := {
  clone := agent_trait.AgentState.Insts.CoreCloneClone.clone
}

/-- Trait implementation: [multi_agent::agent_trait::{core::marker::Copy for multi_agent::agent_trait::AgentState}]
    Source: 'src/agent_trait.rs', lines 5:16-5:20 -/
@[reducible]
def agent_trait.AgentState.Insts.CoreMarkerCopy : core.marker.Copy
  agent_trait.AgentState := {
  cloneInst := agent_trait.AgentState.Insts.CoreCloneClone
}

/-- Trait implementation: [multi_agent::agent_trait::{core::marker::StructuralPartialEq for multi_agent::agent_trait::AgentState}]
    Source: 'src/agent_trait.rs', lines 5:22-5:31 -/
@[reducible]
def agent_trait.AgentState.Insts.CoreMarkerStructuralPartialEq :
  core.marker.StructuralPartialEq agent_trait.AgentState := {
}

/-- [multi_agent::agent_trait::{core::cmp::PartialEq<multi_agent::agent_trait::AgentState> for multi_agent::agent_trait::AgentState}::ne]:
    Source: 'src/agent_trait.rs', lines 5:22-5:31 -/
axiom agent_trait.AgentState.Insts.CoreCmpPartialEqAgentState.ne
  : agent_trait.AgentState → agent_trait.AgentState → Result Bool

/-- [multi_agent::agent_trait::{core::cmp::PartialEq<multi_agent::agent_trait::AgentState> for multi_agent::agent_trait::AgentState}::eq]:
    Source: 'src/agent_trait.rs', lines 5:22-5:31 -/
def agent_trait.AgentState.Insts.CoreCmpPartialEqAgentState.eq
  (self : agent_trait.AgentState) (other : agent_trait.AgentState) :
  Result Bool
  := do
  let self1 := read_discriminant self
  let other1 := read_discriminant other
  ok (self1 = other1)

/-- Trait implementation: [multi_agent::agent_trait::{core::cmp::PartialEq<multi_agent::agent_trait::AgentState> for multi_agent::agent_trait::AgentState}]
    Source: 'src/agent_trait.rs', lines 5:22-5:31 -/
@[reducible]
def agent_trait.AgentState.Insts.CoreCmpPartialEqAgentState :
  core.cmp.PartialEq agent_trait.AgentState agent_trait.AgentState := {
  eq := agent_trait.AgentState.Insts.CoreCmpPartialEqAgentState.eq
  ne := agent_trait.AgentState.Insts.CoreCmpPartialEqAgentState.ne
}

/-- [multi_agent::agent_trait::{core::cmp::Eq for multi_agent::agent_trait::AgentState}::assert_receiver_is_total_eq]:
    Source: 'src/agent_trait.rs', lines 5:33-5:35 -/
def agent_trait.AgentState.Insts.CoreCmpEq.assert_receiver_is_total_eq
  (self : agent_trait.AgentState) : Result Unit := do
  ok ()

/-- Trait implementation: [multi_agent::agent_trait::{core::cmp::Eq for multi_agent::agent_trait::AgentState}]
    Source: 'src/agent_trait.rs', lines 5:33-5:35 -/
@[reducible]
def agent_trait.AgentState.Insts.CoreCmpEq : core.cmp.Eq agent_trait.AgentState
  := {
  partialEqInst := agent_trait.AgentState.Insts.CoreCmpPartialEqAgentState
  assert_receiver_is_total_eq :=
    agent_trait.AgentState.Insts.CoreCmpEq.assert_receiver_is_total_eq
}

/-- [multi_agent::agent_trait::{core::fmt::Debug for multi_agent::agent_trait::AgentState}::fmt]:
    Source: 'src/agent_trait.rs', lines 5:37-5:42 -/
def agent_trait.AgentState.Insts.CoreFmtDebug.fmt
  (self : agent_trait.AgentState) (f : core.fmt.Formatter) :
  Result ((core.result.Result Unit core.fmt.Error) × core.fmt.Formatter)
  := do
  match self with
  | agent_trait.AgentState.Ready =>
    core.fmt.Formatter.write_str f (toStr "Ready")
  | agent_trait.AgentState.Busy =>
    core.fmt.Formatter.write_str f (toStr "Busy")
  | agent_trait.AgentState.Finished =>
    core.fmt.Formatter.write_str f (toStr "Finished")
  | agent_trait.AgentState.Failed =>
    core.fmt.Formatter.write_str f (toStr "Failed")

/-- Trait implementation: [multi_agent::agent_trait::{core::fmt::Debug for multi_agent::agent_trait::AgentState}]
    Source: 'src/agent_trait.rs', lines 5:37-5:42 -/
@[reducible]
def agent_trait.AgentState.Insts.CoreFmtDebug : core.fmt.Debug
  agent_trait.AgentState := {
  fmt := agent_trait.AgentState.Insts.CoreFmtDebug.fmt
}

/-- [multi_agent::agent_trait::MessageKind]
    Source: 'src/agent_trait.rs', lines 15:0-23:1 -/
@[discriminant isize]
inductive agent_trait.MessageKind where
| Task : agent_trait.MessageKind
| Response : agent_trait.MessageKind
| Review : agent_trait.MessageKind
| Vote : agent_trait.MessageKind
| Delegation : agent_trait.MessageKind
| Completion : agent_trait.MessageKind
| Error : agent_trait.MessageKind

/-- [multi_agent::agent_trait::{core::clone::Clone for multi_agent::agent_trait::MessageKind}::clone]:
    Source: 'src/agent_trait.rs', lines 14:9-14:14 -/
def agent_trait.MessageKind.Insts.CoreCloneClone.clone
  (self : agent_trait.MessageKind) : Result agent_trait.MessageKind := do
  ok self

/-- Trait implementation: [multi_agent::agent_trait::{core::clone::Clone for multi_agent::agent_trait::MessageKind}]
    Source: 'src/agent_trait.rs', lines 14:9-14:14 -/
@[reducible]
def agent_trait.MessageKind.Insts.CoreCloneClone : core.clone.Clone
  agent_trait.MessageKind := {
  clone := agent_trait.MessageKind.Insts.CoreCloneClone.clone
}

/-- Trait implementation: [multi_agent::agent_trait::{core::marker::Copy for multi_agent::agent_trait::MessageKind}]
    Source: 'src/agent_trait.rs', lines 14:16-14:20 -/
@[reducible]
def agent_trait.MessageKind.Insts.CoreMarkerCopy : core.marker.Copy
  agent_trait.MessageKind := {
  cloneInst := agent_trait.MessageKind.Insts.CoreCloneClone
}

/-- Trait implementation: [multi_agent::agent_trait::{core::marker::StructuralPartialEq for multi_agent::agent_trait::MessageKind}]
    Source: 'src/agent_trait.rs', lines 14:22-14:31 -/
@[reducible]
def agent_trait.MessageKind.Insts.CoreMarkerStructuralPartialEq :
  core.marker.StructuralPartialEq agent_trait.MessageKind := {
}

/-- [multi_agent::agent_trait::{core::cmp::PartialEq<multi_agent::agent_trait::MessageKind> for multi_agent::agent_trait::MessageKind}::ne]:
    Source: 'src/agent_trait.rs', lines 14:22-14:31 -/
axiom agent_trait.MessageKind.Insts.CoreCmpPartialEqMessageKind.ne
  : agent_trait.MessageKind → agent_trait.MessageKind → Result Bool

/-- [multi_agent::agent_trait::{core::cmp::PartialEq<multi_agent::agent_trait::MessageKind> for multi_agent::agent_trait::MessageKind}::eq]:
    Source: 'src/agent_trait.rs', lines 14:22-14:31 -/
def agent_trait.MessageKind.Insts.CoreCmpPartialEqMessageKind.eq
  (self : agent_trait.MessageKind) (other : agent_trait.MessageKind) :
  Result Bool
  := do
  let self1 := read_discriminant self
  let other1 := read_discriminant other
  ok (self1 = other1)

/-- Trait implementation: [multi_agent::agent_trait::{core::cmp::PartialEq<multi_agent::agent_trait::MessageKind> for multi_agent::agent_trait::MessageKind}]
    Source: 'src/agent_trait.rs', lines 14:22-14:31 -/
@[reducible]
def agent_trait.MessageKind.Insts.CoreCmpPartialEqMessageKind :
  core.cmp.PartialEq agent_trait.MessageKind agent_trait.MessageKind := {
  eq := agent_trait.MessageKind.Insts.CoreCmpPartialEqMessageKind.eq
  ne := agent_trait.MessageKind.Insts.CoreCmpPartialEqMessageKind.ne
}

/-- [multi_agent::agent_trait::{core::cmp::Eq for multi_agent::agent_trait::MessageKind}::assert_receiver_is_total_eq]:
    Source: 'src/agent_trait.rs', lines 14:33-14:35 -/
def agent_trait.MessageKind.Insts.CoreCmpEq.assert_receiver_is_total_eq
  (self : agent_trait.MessageKind) : Result Unit := do
  ok ()

/-- Trait implementation: [multi_agent::agent_trait::{core::cmp::Eq for multi_agent::agent_trait::MessageKind}]
    Source: 'src/agent_trait.rs', lines 14:33-14:35 -/
@[reducible]
def agent_trait.MessageKind.Insts.CoreCmpEq : core.cmp.Eq
  agent_trait.MessageKind := {
  partialEqInst := agent_trait.MessageKind.Insts.CoreCmpPartialEqMessageKind
  assert_receiver_is_total_eq :=
    agent_trait.MessageKind.Insts.CoreCmpEq.assert_receiver_is_total_eq
}

/-- [multi_agent::agent_trait::{core::fmt::Debug for multi_agent::agent_trait::MessageKind}::fmt]:
    Source: 'src/agent_trait.rs', lines 14:37-14:42 -/
def agent_trait.MessageKind.Insts.CoreFmtDebug.fmt
  (self : agent_trait.MessageKind) (f : core.fmt.Formatter) :
  Result ((core.result.Result Unit core.fmt.Error) × core.fmt.Formatter)
  := do
  match self with
  | agent_trait.MessageKind.Task =>
    core.fmt.Formatter.write_str f (toStr "Task")
  | agent_trait.MessageKind.Response =>
    core.fmt.Formatter.write_str f (toStr "Response")
  | agent_trait.MessageKind.Review =>
    core.fmt.Formatter.write_str f (toStr "Review")
  | agent_trait.MessageKind.Vote =>
    core.fmt.Formatter.write_str f (toStr "Vote")
  | agent_trait.MessageKind.Delegation =>
    core.fmt.Formatter.write_str f (toStr "Delegation")
  | agent_trait.MessageKind.Completion =>
    core.fmt.Formatter.write_str f (toStr "Completion")
  | agent_trait.MessageKind.Error =>
    core.fmt.Formatter.write_str f (toStr "Error")

/-- Trait implementation: [multi_agent::agent_trait::{core::fmt::Debug for multi_agent::agent_trait::MessageKind}]
    Source: 'src/agent_trait.rs', lines 14:37-14:42 -/
@[reducible]
def agent_trait.MessageKind.Insts.CoreFmtDebug : core.fmt.Debug
  agent_trait.MessageKind := {
  fmt := agent_trait.MessageKind.Insts.CoreFmtDebug.fmt
}

/-- [multi_agent::agent_trait::Message]
    Source: 'src/agent_trait.rs', lines 27:0-30:1 -/
structure agent_trait.Message where
  kind : agent_trait.MessageKind
  content_id : Std.U32

/-- [multi_agent::agent_trait::{core::clone::Clone for multi_agent::agent_trait::Message}::clone]:
    Source: 'src/agent_trait.rs', lines 26:9-26:14 -/
def agent_trait.Message.Insts.CoreCloneClone.clone
  (self : agent_trait.Message) : Result agent_trait.Message := do
  let mk ← agent_trait.MessageKind.Insts.CoreCloneClone.clone self.kind
  let i ← lift (core.clone.impls.CloneU32.clone self.content_id)
  ok { kind := mk, content_id := i }

/-- Trait implementation: [multi_agent::agent_trait::{core::clone::Clone for multi_agent::agent_trait::Message}]
    Source: 'src/agent_trait.rs', lines 26:9-26:14 -/
@[reducible]
def agent_trait.Message.Insts.CoreCloneClone : core.clone.Clone
  agent_trait.Message := {
  clone := agent_trait.Message.Insts.CoreCloneClone.clone
}

/-- Trait implementation: [multi_agent::agent_trait::{core::marker::StructuralPartialEq for multi_agent::agent_trait::Message}]
    Source: 'src/agent_trait.rs', lines 26:16-26:25 -/
@[reducible]
def agent_trait.Message.Insts.CoreMarkerStructuralPartialEq :
  core.marker.StructuralPartialEq agent_trait.Message := {
}

/-- [multi_agent::agent_trait::{core::cmp::PartialEq<multi_agent::agent_trait::Message> for multi_agent::agent_trait::Message}::ne]:
    Source: 'src/agent_trait.rs', lines 26:16-26:25 -/
axiom agent_trait.Message.Insts.CoreCmpPartialEqMessage.ne
  : agent_trait.Message → agent_trait.Message → Result Bool

/-- [multi_agent::agent_trait::{core::cmp::PartialEq<multi_agent::agent_trait::Message> for multi_agent::agent_trait::Message}::eq]:
    Source: 'src/agent_trait.rs', lines 26:16-26:25 -/
def agent_trait.Message.Insts.CoreCmpPartialEqMessage.eq
  (self : agent_trait.Message) (other : agent_trait.Message) :
  Result Bool
  := do
  if self.content_id = other.content_id
  then
    agent_trait.MessageKind.Insts.CoreCmpPartialEqMessageKind.eq self.kind
      other.kind
  else ok false

/-- Trait implementation: [multi_agent::agent_trait::{core::cmp::PartialEq<multi_agent::agent_trait::Message> for multi_agent::agent_trait::Message}]
    Source: 'src/agent_trait.rs', lines 26:16-26:25 -/
@[reducible]
def agent_trait.Message.Insts.CoreCmpPartialEqMessage : core.cmp.PartialEq
  agent_trait.Message agent_trait.Message := {
  eq := agent_trait.Message.Insts.CoreCmpPartialEqMessage.eq
  ne := agent_trait.Message.Insts.CoreCmpPartialEqMessage.ne
}

/-- [multi_agent::agent_trait::{core::cmp::Eq for multi_agent::agent_trait::Message}::assert_receiver_is_total_eq]:
    Source: 'src/agent_trait.rs', lines 26:27-26:29 -/
def agent_trait.Message.Insts.CoreCmpEq.assert_receiver_is_total_eq
  (self : agent_trait.Message) : Result Unit := do
  ok ()

/-- Trait implementation: [multi_agent::agent_trait::{core::cmp::Eq for multi_agent::agent_trait::Message}]
    Source: 'src/agent_trait.rs', lines 26:27-26:29 -/
@[reducible]
def agent_trait.Message.Insts.CoreCmpEq : core.cmp.Eq agent_trait.Message := {
  partialEqInst := agent_trait.Message.Insts.CoreCmpPartialEqMessage
  assert_receiver_is_total_eq :=
    agent_trait.Message.Insts.CoreCmpEq.assert_receiver_is_total_eq
}

/-- [multi_agent::agent_trait::{core::fmt::Debug for multi_agent::agent_trait::Message}::fmt]:
    Source: 'src/agent_trait.rs', lines 26:31-26:36 -/
def agent_trait.Message.Insts.CoreFmtDebug.fmt
  (self : agent_trait.Message) (f : core.fmt.Formatter) :
  Result ((core.result.Result Unit core.fmt.Error) × core.fmt.Formatter)
  := do
  let dyn := Dyn.mk _ agent_trait.MessageKind.Insts.CoreFmtDebug self.kind
  let dyn1 := Dyn.mk _ (core.fmt.DebugShared core.fmt.DebugU32) self.content_id
  core.fmt.Formatter.debug_struct_field2_finish f (toStr "Message") (toStr
    "kind") dyn (toStr "content_id") dyn1

/-- Trait implementation: [multi_agent::agent_trait::{core::fmt::Debug for multi_agent::agent_trait::Message}]
    Source: 'src/agent_trait.rs', lines 26:31-26:36 -/
@[reducible]
def agent_trait.Message.Insts.CoreFmtDebug : core.fmt.Debug agent_trait.Message
  := {
  fmt := agent_trait.Message.Insts.CoreFmtDebug.fmt
}

/-- [multi_agent::agent_trait::Recipient]
    Source: 'src/agent_trait.rs', lines 34:0-38:1 -/
@[discriminant isize]
inductive agent_trait.Recipient where
| Direct : Std.U32 → agent_trait.Recipient
| Broadcast : agent_trait.Recipient
| Topic : Std.U32 → agent_trait.Recipient

/-- [multi_agent::agent_trait::{core::clone::Clone for multi_agent::agent_trait::Recipient}::clone]:
    Source: 'src/agent_trait.rs', lines 33:9-33:14 -/
def agent_trait.Recipient.Insts.CoreCloneClone.clone
  (self : agent_trait.Recipient) : Result agent_trait.Recipient := do
  match self with
  | agent_trait.Recipient.Direct __self_0 =>
    let i ← lift (core.clone.impls.CloneU32.clone __self_0)
    ok (agent_trait.Recipient.Direct i)
  | agent_trait.Recipient.Broadcast => ok agent_trait.Recipient.Broadcast
  | agent_trait.Recipient.Topic __self_0 =>
    let i ← lift (core.clone.impls.CloneU32.clone __self_0)
    ok (agent_trait.Recipient.Topic i)

/-- Trait implementation: [multi_agent::agent_trait::{core::clone::Clone for multi_agent::agent_trait::Recipient}]
    Source: 'src/agent_trait.rs', lines 33:9-33:14 -/
@[reducible]
def agent_trait.Recipient.Insts.CoreCloneClone : core.clone.Clone
  agent_trait.Recipient := {
  clone := agent_trait.Recipient.Insts.CoreCloneClone.clone
}

/-- Trait implementation: [multi_agent::agent_trait::{core::marker::StructuralPartialEq for multi_agent::agent_trait::Recipient}]
    Source: 'src/agent_trait.rs', lines 33:16-33:25 -/
@[reducible]
def agent_trait.Recipient.Insts.CoreMarkerStructuralPartialEq :
  core.marker.StructuralPartialEq agent_trait.Recipient := {
}

/-- [multi_agent::agent_trait::{core::cmp::PartialEq<multi_agent::agent_trait::Recipient> for multi_agent::agent_trait::Recipient}::ne]:
    Source: 'src/agent_trait.rs', lines 33:16-33:25 -/
axiom agent_trait.Recipient.Insts.CoreCmpPartialEqRecipient.ne
  : agent_trait.Recipient → agent_trait.Recipient → Result Bool

/-- [multi_agent::agent_trait::{core::cmp::PartialEq<multi_agent::agent_trait::Recipient> for multi_agent::agent_trait::Recipient}::eq]:
    Source: 'src/agent_trait.rs', lines 33:16-33:25 -/
def agent_trait.Recipient.Insts.CoreCmpPartialEqRecipient.eq
  (self : agent_trait.Recipient) (other : agent_trait.Recipient) :
  Result Bool
  := do
  let self1 := read_discriminant self
  let other1 := read_discriminant other
  if self1 = other1
  then
    match self with
    | agent_trait.Recipient.Direct __self_0 =>
      match other with
      | agent_trait.Recipient.Direct __arg1_0 =>
        lift (core.cmp.impls.PartialEqU32.eq __self_0 __arg1_0)
      | agent_trait.Recipient.Broadcast => ok true
      | agent_trait.Recipient.Topic _ => ok true
    | agent_trait.Recipient.Broadcast => ok true
    | agent_trait.Recipient.Topic __self_0 =>
      match other with
      | agent_trait.Recipient.Direct _ => ok true
      | agent_trait.Recipient.Broadcast => ok true
      | agent_trait.Recipient.Topic __arg1_0 =>
        lift (core.cmp.impls.PartialEqU32.eq __self_0 __arg1_0)
  else ok false

/-- Trait implementation: [multi_agent::agent_trait::{core::cmp::PartialEq<multi_agent::agent_trait::Recipient> for multi_agent::agent_trait::Recipient}]
    Source: 'src/agent_trait.rs', lines 33:16-33:25 -/
@[reducible]
def agent_trait.Recipient.Insts.CoreCmpPartialEqRecipient : core.cmp.PartialEq
  agent_trait.Recipient agent_trait.Recipient := {
  eq := agent_trait.Recipient.Insts.CoreCmpPartialEqRecipient.eq
  ne := agent_trait.Recipient.Insts.CoreCmpPartialEqRecipient.ne
}

/-- [multi_agent::agent_trait::{core::cmp::Eq for multi_agent::agent_trait::Recipient}::assert_receiver_is_total_eq]:
    Source: 'src/agent_trait.rs', lines 33:27-33:29 -/
def agent_trait.Recipient.Insts.CoreCmpEq.assert_receiver_is_total_eq
  (self : agent_trait.Recipient) : Result Unit := do
  ok ()

/-- Trait implementation: [multi_agent::agent_trait::{core::cmp::Eq for multi_agent::agent_trait::Recipient}]
    Source: 'src/agent_trait.rs', lines 33:27-33:29 -/
@[reducible]
def agent_trait.Recipient.Insts.CoreCmpEq : core.cmp.Eq agent_trait.Recipient
  := {
  partialEqInst := agent_trait.Recipient.Insts.CoreCmpPartialEqRecipient
  assert_receiver_is_total_eq :=
    agent_trait.Recipient.Insts.CoreCmpEq.assert_receiver_is_total_eq
}

/-- [multi_agent::agent_trait::{core::fmt::Debug for multi_agent::agent_trait::Recipient}::fmt]:
    Source: 'src/agent_trait.rs', lines 33:31-33:36 -/
def agent_trait.Recipient.Insts.CoreFmtDebug.fmt
  (self : agent_trait.Recipient) (f : core.fmt.Formatter) :
  Result ((core.result.Result Unit core.fmt.Error) × core.fmt.Formatter)
  := do
  match self with
  | agent_trait.Recipient.Direct __self_0 =>
    let __self_01 := Dyn.mk _ (core.fmt.DebugShared core.fmt.DebugU32) __self_0
    core.fmt.Formatter.debug_tuple_field1_finish f (toStr "Direct") __self_01
  | agent_trait.Recipient.Broadcast =>
    core.fmt.Formatter.write_str f (toStr "Broadcast")
  | agent_trait.Recipient.Topic __self_0 =>
    let __self_01 := Dyn.mk _ (core.fmt.DebugShared core.fmt.DebugU32) __self_0
    core.fmt.Formatter.debug_tuple_field1_finish f (toStr "Topic") __self_01

/-- Trait implementation: [multi_agent::agent_trait::{core::fmt::Debug for multi_agent::agent_trait::Recipient}]
    Source: 'src/agent_trait.rs', lines 33:31-33:36 -/
@[reducible]
def agent_trait.Recipient.Insts.CoreFmtDebug : core.fmt.Debug
  agent_trait.Recipient := {
  fmt := agent_trait.Recipient.Insts.CoreFmtDebug.fmt
}

/-- [multi_agent::agent_trait::Envelope]
    Source: 'src/agent_trait.rs', lines 42:0-47:1 -/
structure agent_trait.Envelope where
  sender : Std.U32
  recipient : agent_trait.Recipient
  message : agent_trait.Message
  sequence_num : Std.U32

/-- [multi_agent::agent_trait::{core::clone::Clone for multi_agent::agent_trait::Envelope}::clone]:
    Source: 'src/agent_trait.rs', lines 41:9-41:14 -/
def agent_trait.Envelope.Insts.CoreCloneClone.clone
  (self : agent_trait.Envelope) : Result agent_trait.Envelope := do
  let i ← lift (core.clone.impls.CloneU32.clone self.sender)
  let r ← agent_trait.Recipient.Insts.CoreCloneClone.clone self.recipient
  let m ← agent_trait.Message.Insts.CoreCloneClone.clone self.message
  let i1 ← lift (core.clone.impls.CloneU32.clone self.sequence_num)
  ok { sender := i, recipient := r, message := m, sequence_num := i1 }

/-- Trait implementation: [multi_agent::agent_trait::{core::clone::Clone for multi_agent::agent_trait::Envelope}]
    Source: 'src/agent_trait.rs', lines 41:9-41:14 -/
@[reducible]
def agent_trait.Envelope.Insts.CoreCloneClone : core.clone.Clone
  agent_trait.Envelope := {
  clone := agent_trait.Envelope.Insts.CoreCloneClone.clone
}

/-- Trait implementation: [multi_agent::agent_trait::{core::marker::StructuralPartialEq for multi_agent::agent_trait::Envelope}]
    Source: 'src/agent_trait.rs', lines 41:16-41:25 -/
@[reducible]
def agent_trait.Envelope.Insts.CoreMarkerStructuralPartialEq :
  core.marker.StructuralPartialEq agent_trait.Envelope := {
}

/-- [multi_agent::agent_trait::{core::cmp::PartialEq<multi_agent::agent_trait::Envelope> for multi_agent::agent_trait::Envelope}::ne]:
    Source: 'src/agent_trait.rs', lines 41:16-41:25 -/
axiom agent_trait.Envelope.Insts.CoreCmpPartialEqEnvelope.ne
  : agent_trait.Envelope → agent_trait.Envelope → Result Bool

/-- [multi_agent::agent_trait::{core::cmp::PartialEq<multi_agent::agent_trait::Envelope> for multi_agent::agent_trait::Envelope}::eq]:
    Source: 'src/agent_trait.rs', lines 41:16-41:25 -/
def agent_trait.Envelope.Insts.CoreCmpPartialEqEnvelope.eq
  (self : agent_trait.Envelope) (other : agent_trait.Envelope) :
  Result Bool
  := do
  if self.sequence_num = other.sequence_num
  then
    if self.sender = other.sender
    then
      let b ←
        agent_trait.Recipient.Insts.CoreCmpPartialEqRecipient.eq self.recipient
          other.recipient
      if b
      then
        agent_trait.Message.Insts.CoreCmpPartialEqMessage.eq self.message
          other.message
      else ok false
    else ok false
  else ok false

/-- Trait implementation: [multi_agent::agent_trait::{core::cmp::PartialEq<multi_agent::agent_trait::Envelope> for multi_agent::agent_trait::Envelope}]
    Source: 'src/agent_trait.rs', lines 41:16-41:25 -/
@[reducible]
def agent_trait.Envelope.Insts.CoreCmpPartialEqEnvelope : core.cmp.PartialEq
  agent_trait.Envelope agent_trait.Envelope := {
  eq := agent_trait.Envelope.Insts.CoreCmpPartialEqEnvelope.eq
  ne := agent_trait.Envelope.Insts.CoreCmpPartialEqEnvelope.ne
}

/-- [multi_agent::agent_trait::{core::cmp::Eq for multi_agent::agent_trait::Envelope}::assert_receiver_is_total_eq]:
    Source: 'src/agent_trait.rs', lines 41:27-41:29 -/
def agent_trait.Envelope.Insts.CoreCmpEq.assert_receiver_is_total_eq
  (self : agent_trait.Envelope) : Result Unit := do
  ok ()

/-- Trait implementation: [multi_agent::agent_trait::{core::cmp::Eq for multi_agent::agent_trait::Envelope}]
    Source: 'src/agent_trait.rs', lines 41:27-41:29 -/
@[reducible]
def agent_trait.Envelope.Insts.CoreCmpEq : core.cmp.Eq agent_trait.Envelope
  := {
  partialEqInst := agent_trait.Envelope.Insts.CoreCmpPartialEqEnvelope
  assert_receiver_is_total_eq :=
    agent_trait.Envelope.Insts.CoreCmpEq.assert_receiver_is_total_eq
}

/-- [multi_agent::agent_trait::{core::fmt::Debug for multi_agent::agent_trait::Envelope}::fmt]:
    Source: 'src/agent_trait.rs', lines 41:31-41:36 -/
def agent_trait.Envelope.Insts.CoreFmtDebug.fmt
  (self : agent_trait.Envelope) (f : core.fmt.Formatter) :
  Result ((core.result.Result Unit core.fmt.Error) × core.fmt.Formatter)
  := do
  let dyn := Dyn.mk _ core.fmt.DebugU32 self.sender
  let dyn1 := Dyn.mk _ agent_trait.Recipient.Insts.CoreFmtDebug self.recipient
  let dyn2 := Dyn.mk _ agent_trait.Message.Insts.CoreFmtDebug self.message
  let dyn3 :=
    Dyn.mk _ (core.fmt.DebugShared core.fmt.DebugU32) self.sequence_num
  core.fmt.Formatter.debug_struct_field4_finish f (toStr "Envelope") (toStr
    "sender") dyn (toStr "recipient") dyn1 (toStr "message") dyn2 (toStr
    "sequence_num") dyn3

/-- Trait implementation: [multi_agent::agent_trait::{core::fmt::Debug for multi_agent::agent_trait::Envelope}]
    Source: 'src/agent_trait.rs', lines 41:31-41:36 -/
@[reducible]
def agent_trait.Envelope.Insts.CoreFmtDebug : core.fmt.Debug
  agent_trait.Envelope := {
  fmt := agent_trait.Envelope.Insts.CoreFmtDebug.fmt
}

/-- [multi_agent::agent_trait::ProtocolKind]
    Source: 'src/agent_trait.rs', lines 51:0-56:1 -/
@[discriminant isize]
inductive agent_trait.ProtocolKind where
| RequestResponse : agent_trait.ProtocolKind
| Pipeline : agent_trait.ProtocolKind
| Debate : agent_trait.ProtocolKind
| Voting : agent_trait.ProtocolKind

/-- [multi_agent::agent_trait::{core::clone::Clone for multi_agent::agent_trait::ProtocolKind}::clone]:
    Source: 'src/agent_trait.rs', lines 50:9-50:14 -/
def agent_trait.ProtocolKind.Insts.CoreCloneClone.clone
  (self : agent_trait.ProtocolKind) : Result agent_trait.ProtocolKind := do
  ok self

/-- Trait implementation: [multi_agent::agent_trait::{core::clone::Clone for multi_agent::agent_trait::ProtocolKind}]
    Source: 'src/agent_trait.rs', lines 50:9-50:14 -/
@[reducible]
def agent_trait.ProtocolKind.Insts.CoreCloneClone : core.clone.Clone
  agent_trait.ProtocolKind := {
  clone := agent_trait.ProtocolKind.Insts.CoreCloneClone.clone
}

/-- Trait implementation: [multi_agent::agent_trait::{core::marker::Copy for multi_agent::agent_trait::ProtocolKind}]
    Source: 'src/agent_trait.rs', lines 50:16-50:20 -/
@[reducible]
def agent_trait.ProtocolKind.Insts.CoreMarkerCopy : core.marker.Copy
  agent_trait.ProtocolKind := {
  cloneInst := agent_trait.ProtocolKind.Insts.CoreCloneClone
}

/-- Trait implementation: [multi_agent::agent_trait::{core::marker::StructuralPartialEq for multi_agent::agent_trait::ProtocolKind}]
    Source: 'src/agent_trait.rs', lines 50:22-50:31 -/
@[reducible]
def agent_trait.ProtocolKind.Insts.CoreMarkerStructuralPartialEq :
  core.marker.StructuralPartialEq agent_trait.ProtocolKind := {
}

/-- [multi_agent::agent_trait::{core::cmp::PartialEq<multi_agent::agent_trait::ProtocolKind> for multi_agent::agent_trait::ProtocolKind}::ne]:
    Source: 'src/agent_trait.rs', lines 50:22-50:31 -/
axiom agent_trait.ProtocolKind.Insts.CoreCmpPartialEqProtocolKind.ne
  : agent_trait.ProtocolKind → agent_trait.ProtocolKind → Result Bool

/-- [multi_agent::agent_trait::{core::cmp::PartialEq<multi_agent::agent_trait::ProtocolKind> for multi_agent::agent_trait::ProtocolKind}::eq]:
    Source: 'src/agent_trait.rs', lines 50:22-50:31 -/
def agent_trait.ProtocolKind.Insts.CoreCmpPartialEqProtocolKind.eq
  (self : agent_trait.ProtocolKind) (other : agent_trait.ProtocolKind) :
  Result Bool
  := do
  let self1 := read_discriminant self
  let other1 := read_discriminant other
  ok (self1 = other1)

/-- Trait implementation: [multi_agent::agent_trait::{core::cmp::PartialEq<multi_agent::agent_trait::ProtocolKind> for multi_agent::agent_trait::ProtocolKind}]
    Source: 'src/agent_trait.rs', lines 50:22-50:31 -/
@[reducible]
def agent_trait.ProtocolKind.Insts.CoreCmpPartialEqProtocolKind :
  core.cmp.PartialEq agent_trait.ProtocolKind agent_trait.ProtocolKind := {
  eq := agent_trait.ProtocolKind.Insts.CoreCmpPartialEqProtocolKind.eq
  ne := agent_trait.ProtocolKind.Insts.CoreCmpPartialEqProtocolKind.ne
}

/-- [multi_agent::agent_trait::{core::cmp::Eq for multi_agent::agent_trait::ProtocolKind}::assert_receiver_is_total_eq]:
    Source: 'src/agent_trait.rs', lines 50:33-50:35 -/
def agent_trait.ProtocolKind.Insts.CoreCmpEq.assert_receiver_is_total_eq
  (self : agent_trait.ProtocolKind) : Result Unit := do
  ok ()

/-- Trait implementation: [multi_agent::agent_trait::{core::cmp::Eq for multi_agent::agent_trait::ProtocolKind}]
    Source: 'src/agent_trait.rs', lines 50:33-50:35 -/
@[reducible]
def agent_trait.ProtocolKind.Insts.CoreCmpEq : core.cmp.Eq
  agent_trait.ProtocolKind := {
  partialEqInst := agent_trait.ProtocolKind.Insts.CoreCmpPartialEqProtocolKind
  assert_receiver_is_total_eq :=
    agent_trait.ProtocolKind.Insts.CoreCmpEq.assert_receiver_is_total_eq
}

/-- [multi_agent::agent_trait::{core::fmt::Debug for multi_agent::agent_trait::ProtocolKind}::fmt]:
    Source: 'src/agent_trait.rs', lines 50:37-50:42 -/
def agent_trait.ProtocolKind.Insts.CoreFmtDebug.fmt
  (self : agent_trait.ProtocolKind) (f : core.fmt.Formatter) :
  Result ((core.result.Result Unit core.fmt.Error) × core.fmt.Formatter)
  := do
  match self with
  | agent_trait.ProtocolKind.RequestResponse =>
    core.fmt.Formatter.write_str f (toStr "RequestResponse")
  | agent_trait.ProtocolKind.Pipeline =>
    core.fmt.Formatter.write_str f (toStr "Pipeline")
  | agent_trait.ProtocolKind.Debate =>
    core.fmt.Formatter.write_str f (toStr "Debate")
  | agent_trait.ProtocolKind.Voting =>
    core.fmt.Formatter.write_str f (toStr "Voting")

/-- Trait implementation: [multi_agent::agent_trait::{core::fmt::Debug for multi_agent::agent_trait::ProtocolKind}]
    Source: 'src/agent_trait.rs', lines 50:37-50:42 -/
@[reducible]
def agent_trait.ProtocolKind.Insts.CoreFmtDebug : core.fmt.Debug
  agent_trait.ProtocolKind := {
  fmt := agent_trait.ProtocolKind.Insts.CoreFmtDebug.fmt
}

/-- [multi_agent::agent_trait::CoordinatorState]
    Source: 'src/agent_trait.rs', lines 60:0-65:1 -/
structure agent_trait.CoordinatorState where
  protocol : agent_trait.ProtocolKind
  managed_agents : alloc.vec.Vec Std.U32
  pending_responses : Std.U32
  round : Std.U32

/-- [multi_agent::agent_trait::{core::clone::Clone for multi_agent::agent_trait::CoordinatorState}::clone]:
    Source: 'src/agent_trait.rs', lines 59:9-59:14 -/
def agent_trait.CoordinatorState.Insts.CoreCloneClone.clone
  (self : agent_trait.CoordinatorState) :
  Result agent_trait.CoordinatorState
  := do
  let pk ← agent_trait.ProtocolKind.Insts.CoreCloneClone.clone self.protocol
  let v ← alloc.vec.CloneVec.clone core.clone.CloneU32 self.managed_agents
  let i ← lift (core.clone.impls.CloneU32.clone self.pending_responses)
  let i1 ← lift (core.clone.impls.CloneU32.clone self.round)
  ok
    { protocol := pk, managed_agents := v, pending_responses := i, round := i1
    }

/-- Trait implementation: [multi_agent::agent_trait::{core::clone::Clone for multi_agent::agent_trait::CoordinatorState}]
    Source: 'src/agent_trait.rs', lines 59:9-59:14 -/
@[reducible]
def agent_trait.CoordinatorState.Insts.CoreCloneClone : core.clone.Clone
  agent_trait.CoordinatorState := {
  clone := agent_trait.CoordinatorState.Insts.CoreCloneClone.clone
}

/-- Trait implementation: [multi_agent::agent_trait::{core::marker::StructuralPartialEq for multi_agent::agent_trait::CoordinatorState}]
    Source: 'src/agent_trait.rs', lines 59:16-59:25 -/
@[reducible]
def agent_trait.CoordinatorState.Insts.CoreMarkerStructuralPartialEq :
  core.marker.StructuralPartialEq agent_trait.CoordinatorState := {
}

/-- [multi_agent::agent_trait::{core::cmp::PartialEq<multi_agent::agent_trait::CoordinatorState> for multi_agent::agent_trait::CoordinatorState}::ne]:
    Source: 'src/agent_trait.rs', lines 59:16-59:25 -/
axiom agent_trait.CoordinatorState.Insts.CoreCmpPartialEqCoordinatorState.ne
  :
  agent_trait.CoordinatorState → agent_trait.CoordinatorState → Result Bool

/-- [multi_agent::agent_trait::{core::cmp::PartialEq<multi_agent::agent_trait::CoordinatorState> for multi_agent::agent_trait::CoordinatorState}::eq]:
    Source: 'src/agent_trait.rs', lines 59:16-59:25 -/
def agent_trait.CoordinatorState.Insts.CoreCmpPartialEqCoordinatorState.eq
  (self : agent_trait.CoordinatorState) (other : agent_trait.CoordinatorState)
  :
  Result Bool
  := do
  if self.pending_responses = other.pending_responses
  then
    if self.round = other.round
    then
      let b ←
        agent_trait.ProtocolKind.Insts.CoreCmpPartialEqProtocolKind.eq
          self.protocol other.protocol
      if b
      then
        alloc.vec.partial_eq.PartialEqVec.eq core.cmp.PartialEqU32
          self.managed_agents other.managed_agents
      else ok false
    else ok false
  else ok false

/-- Trait implementation: [multi_agent::agent_trait::{core::cmp::PartialEq<multi_agent::agent_trait::CoordinatorState> for multi_agent::agent_trait::CoordinatorState}]
    Source: 'src/agent_trait.rs', lines 59:16-59:25 -/
@[reducible]
def agent_trait.CoordinatorState.Insts.CoreCmpPartialEqCoordinatorState :
  core.cmp.PartialEq agent_trait.CoordinatorState agent_trait.CoordinatorState
  := {
  eq := agent_trait.CoordinatorState.Insts.CoreCmpPartialEqCoordinatorState.eq
  ne := agent_trait.CoordinatorState.Insts.CoreCmpPartialEqCoordinatorState.ne
}

/-- [multi_agent::agent_trait::{core::cmp::Eq for multi_agent::agent_trait::CoordinatorState}::assert_receiver_is_total_eq]:
    Source: 'src/agent_trait.rs', lines 59:27-59:29 -/
def agent_trait.CoordinatorState.Insts.CoreCmpEq.assert_receiver_is_total_eq
  (self : agent_trait.CoordinatorState) : Result Unit := do
  ok ()

/-- Trait implementation: [multi_agent::agent_trait::{core::cmp::Eq for multi_agent::agent_trait::CoordinatorState}]
    Source: 'src/agent_trait.rs', lines 59:27-59:29 -/
@[reducible]
def agent_trait.CoordinatorState.Insts.CoreCmpEq : core.cmp.Eq
  agent_trait.CoordinatorState := {
  partialEqInst :=
    agent_trait.CoordinatorState.Insts.CoreCmpPartialEqCoordinatorState
  assert_receiver_is_total_eq :=
    agent_trait.CoordinatorState.Insts.CoreCmpEq.assert_receiver_is_total_eq
}

/-- [multi_agent::agent_trait::{core::fmt::Debug for multi_agent::agent_trait::CoordinatorState}::fmt]:
    Source: 'src/agent_trait.rs', lines 59:31-59:36 -/
def agent_trait.CoordinatorState.Insts.CoreFmtDebug.fmt
  (self : agent_trait.CoordinatorState) (f : core.fmt.Formatter) :
  Result ((core.result.Result Unit core.fmt.Error) × core.fmt.Formatter)
  := do
  let dyn := Dyn.mk _ agent_trait.ProtocolKind.Insts.CoreFmtDebug self.protocol
  let dyn1 :=
    Dyn.mk _ (core.fmt.DebugVec core.fmt.DebugU32) self.managed_agents
  let dyn2 := Dyn.mk _ core.fmt.DebugU32 self.pending_responses
  let dyn3 := Dyn.mk _ (core.fmt.DebugShared core.fmt.DebugU32) self.round
  core.fmt.Formatter.debug_struct_field4_finish f (toStr "CoordinatorState")
    (toStr "protocol") dyn (toStr "managed_agents") dyn1 (toStr
    "pending_responses") dyn2 (toStr "round") dyn3

/-- Trait implementation: [multi_agent::agent_trait::{core::fmt::Debug for multi_agent::agent_trait::CoordinatorState}]
    Source: 'src/agent_trait.rs', lines 59:31-59:36 -/
@[reducible]
def agent_trait.CoordinatorState.Insts.CoreFmtDebug : core.fmt.Debug
  agent_trait.CoordinatorState := {
  fmt := agent_trait.CoordinatorState.Insts.CoreFmtDebug.fmt
}

/-- [multi_agent::agent_trait::SpecialistState]
    Source: 'src/agent_trait.rs', lines 69:0-73:1 -/
structure agent_trait.SpecialistState where
  specialty : Std.U32
  context : alloc.vec.Vec Std.U32
  steps_taken : Std.U32

/-- [multi_agent::agent_trait::{core::clone::Clone for multi_agent::agent_trait::SpecialistState}::clone]:
    Source: 'src/agent_trait.rs', lines 68:9-68:14 -/
def agent_trait.SpecialistState.Insts.CoreCloneClone.clone
  (self : agent_trait.SpecialistState) :
  Result agent_trait.SpecialistState
  := do
  let i ← lift (core.clone.impls.CloneU32.clone self.specialty)
  let v ← alloc.vec.CloneVec.clone core.clone.CloneU32 self.context
  let i1 ← lift (core.clone.impls.CloneU32.clone self.steps_taken)
  ok { specialty := i, context := v, steps_taken := i1 }

/-- Trait implementation: [multi_agent::agent_trait::{core::clone::Clone for multi_agent::agent_trait::SpecialistState}]
    Source: 'src/agent_trait.rs', lines 68:9-68:14 -/
@[reducible]
def agent_trait.SpecialistState.Insts.CoreCloneClone : core.clone.Clone
  agent_trait.SpecialistState := {
  clone := agent_trait.SpecialistState.Insts.CoreCloneClone.clone
}

/-- Trait implementation: [multi_agent::agent_trait::{core::marker::StructuralPartialEq for multi_agent::agent_trait::SpecialistState}]
    Source: 'src/agent_trait.rs', lines 68:16-68:25 -/
@[reducible]
def agent_trait.SpecialistState.Insts.CoreMarkerStructuralPartialEq :
  core.marker.StructuralPartialEq agent_trait.SpecialistState := {
}

/-- [multi_agent::agent_trait::{core::cmp::PartialEq<multi_agent::agent_trait::SpecialistState> for multi_agent::agent_trait::SpecialistState}::ne]:
    Source: 'src/agent_trait.rs', lines 68:16-68:25 -/
axiom agent_trait.SpecialistState.Insts.CoreCmpPartialEqSpecialistState.ne
  : agent_trait.SpecialistState → agent_trait.SpecialistState → Result Bool

/-- [multi_agent::agent_trait::{core::cmp::PartialEq<multi_agent::agent_trait::SpecialistState> for multi_agent::agent_trait::SpecialistState}::eq]:
    Source: 'src/agent_trait.rs', lines 68:16-68:25 -/
def agent_trait.SpecialistState.Insts.CoreCmpPartialEqSpecialistState.eq
  (self : agent_trait.SpecialistState) (other : agent_trait.SpecialistState) :
  Result Bool
  := do
  if self.specialty = other.specialty
  then
    if self.steps_taken = other.steps_taken
    then
      alloc.vec.partial_eq.PartialEqVec.eq core.cmp.PartialEqU32 self.context
        other.context
    else ok false
  else ok false

/-- Trait implementation: [multi_agent::agent_trait::{core::cmp::PartialEq<multi_agent::agent_trait::SpecialistState> for multi_agent::agent_trait::SpecialistState}]
    Source: 'src/agent_trait.rs', lines 68:16-68:25 -/
@[reducible]
def agent_trait.SpecialistState.Insts.CoreCmpPartialEqSpecialistState :
  core.cmp.PartialEq agent_trait.SpecialistState agent_trait.SpecialistState
  := {
  eq := agent_trait.SpecialistState.Insts.CoreCmpPartialEqSpecialistState.eq
  ne := agent_trait.SpecialistState.Insts.CoreCmpPartialEqSpecialistState.ne
}

/-- [multi_agent::agent_trait::{core::cmp::Eq for multi_agent::agent_trait::SpecialistState}::assert_receiver_is_total_eq]:
    Source: 'src/agent_trait.rs', lines 68:27-68:29 -/
def agent_trait.SpecialistState.Insts.CoreCmpEq.assert_receiver_is_total_eq
  (self : agent_trait.SpecialistState) : Result Unit := do
  ok ()

/-- Trait implementation: [multi_agent::agent_trait::{core::cmp::Eq for multi_agent::agent_trait::SpecialistState}]
    Source: 'src/agent_trait.rs', lines 68:27-68:29 -/
@[reducible]
def agent_trait.SpecialistState.Insts.CoreCmpEq : core.cmp.Eq
  agent_trait.SpecialistState := {
  partialEqInst :=
    agent_trait.SpecialistState.Insts.CoreCmpPartialEqSpecialistState
  assert_receiver_is_total_eq :=
    agent_trait.SpecialistState.Insts.CoreCmpEq.assert_receiver_is_total_eq
}

/-- [multi_agent::agent_trait::{core::fmt::Debug for multi_agent::agent_trait::SpecialistState}::fmt]:
    Source: 'src/agent_trait.rs', lines 68:31-68:36 -/
def agent_trait.SpecialistState.Insts.CoreFmtDebug.fmt
  (self : agent_trait.SpecialistState) (f : core.fmt.Formatter) :
  Result ((core.result.Result Unit core.fmt.Error) × core.fmt.Formatter)
  := do
  let dyn := Dyn.mk _ core.fmt.DebugU32 self.specialty
  let dyn1 := Dyn.mk _ (core.fmt.DebugVec core.fmt.DebugU32) self.context
  let dyn2 :=
    Dyn.mk _ (core.fmt.DebugShared core.fmt.DebugU32) self.steps_taken
  core.fmt.Formatter.debug_struct_field3_finish f (toStr "SpecialistState")
    (toStr "specialty") dyn (toStr "context") dyn1 (toStr "steps_taken") dyn2

/-- Trait implementation: [multi_agent::agent_trait::{core::fmt::Debug for multi_agent::agent_trait::SpecialistState}]
    Source: 'src/agent_trait.rs', lines 68:31-68:36 -/
@[reducible]
def agent_trait.SpecialistState.Insts.CoreFmtDebug : core.fmt.Debug
  agent_trait.SpecialistState := {
  fmt := agent_trait.SpecialistState.Insts.CoreFmtDebug.fmt
}

/-- [multi_agent::agent_trait::CriticState]
    Source: 'src/agent_trait.rs', lines 77:0-80:1 -/
structure agent_trait.CriticState where
  criteria : alloc.vec.Vec Std.U32
  reviews_given : Std.U32

/-- [multi_agent::agent_trait::{core::clone::Clone for multi_agent::agent_trait::CriticState}::clone]:
    Source: 'src/agent_trait.rs', lines 76:9-76:14 -/
def agent_trait.CriticState.Insts.CoreCloneClone.clone
  (self : agent_trait.CriticState) : Result agent_trait.CriticState := do
  let v ← alloc.vec.CloneVec.clone core.clone.CloneU32 self.criteria
  let i ← lift (core.clone.impls.CloneU32.clone self.reviews_given)
  ok { criteria := v, reviews_given := i }

/-- Trait implementation: [multi_agent::agent_trait::{core::clone::Clone for multi_agent::agent_trait::CriticState}]
    Source: 'src/agent_trait.rs', lines 76:9-76:14 -/
@[reducible]
def agent_trait.CriticState.Insts.CoreCloneClone : core.clone.Clone
  agent_trait.CriticState := {
  clone := agent_trait.CriticState.Insts.CoreCloneClone.clone
}

/-- Trait implementation: [multi_agent::agent_trait::{core::marker::StructuralPartialEq for multi_agent::agent_trait::CriticState}]
    Source: 'src/agent_trait.rs', lines 76:16-76:25 -/
@[reducible]
def agent_trait.CriticState.Insts.CoreMarkerStructuralPartialEq :
  core.marker.StructuralPartialEq agent_trait.CriticState := {
}

/-- [multi_agent::agent_trait::{core::cmp::PartialEq<multi_agent::agent_trait::CriticState> for multi_agent::agent_trait::CriticState}::ne]:
    Source: 'src/agent_trait.rs', lines 76:16-76:25 -/
axiom agent_trait.CriticState.Insts.CoreCmpPartialEqCriticState.ne
  : agent_trait.CriticState → agent_trait.CriticState → Result Bool

/-- [multi_agent::agent_trait::{core::cmp::PartialEq<multi_agent::agent_trait::CriticState> for multi_agent::agent_trait::CriticState}::eq]:
    Source: 'src/agent_trait.rs', lines 76:16-76:25 -/
def agent_trait.CriticState.Insts.CoreCmpPartialEqCriticState.eq
  (self : agent_trait.CriticState) (other : agent_trait.CriticState) :
  Result Bool
  := do
  if self.reviews_given = other.reviews_given
  then
    alloc.vec.partial_eq.PartialEqVec.eq core.cmp.PartialEqU32 self.criteria
      other.criteria
  else ok false

/-- Trait implementation: [multi_agent::agent_trait::{core::cmp::PartialEq<multi_agent::agent_trait::CriticState> for multi_agent::agent_trait::CriticState}]
    Source: 'src/agent_trait.rs', lines 76:16-76:25 -/
@[reducible]
def agent_trait.CriticState.Insts.CoreCmpPartialEqCriticState :
  core.cmp.PartialEq agent_trait.CriticState agent_trait.CriticState := {
  eq := agent_trait.CriticState.Insts.CoreCmpPartialEqCriticState.eq
  ne := agent_trait.CriticState.Insts.CoreCmpPartialEqCriticState.ne
}

/-- [multi_agent::agent_trait::{core::cmp::Eq for multi_agent::agent_trait::CriticState}::assert_receiver_is_total_eq]:
    Source: 'src/agent_trait.rs', lines 76:27-76:29 -/
def agent_trait.CriticState.Insts.CoreCmpEq.assert_receiver_is_total_eq
  (self : agent_trait.CriticState) : Result Unit := do
  ok ()

/-- Trait implementation: [multi_agent::agent_trait::{core::cmp::Eq for multi_agent::agent_trait::CriticState}]
    Source: 'src/agent_trait.rs', lines 76:27-76:29 -/
@[reducible]
def agent_trait.CriticState.Insts.CoreCmpEq : core.cmp.Eq
  agent_trait.CriticState := {
  partialEqInst := agent_trait.CriticState.Insts.CoreCmpPartialEqCriticState
  assert_receiver_is_total_eq :=
    agent_trait.CriticState.Insts.CoreCmpEq.assert_receiver_is_total_eq
}

/-- [multi_agent::agent_trait::{core::fmt::Debug for multi_agent::agent_trait::CriticState}::fmt]:
    Source: 'src/agent_trait.rs', lines 76:31-76:36 -/
def agent_trait.CriticState.Insts.CoreFmtDebug.fmt
  (self : agent_trait.CriticState) (f : core.fmt.Formatter) :
  Result ((core.result.Result Unit core.fmt.Error) × core.fmt.Formatter)
  := do
  let dyn := Dyn.mk _ (core.fmt.DebugVec core.fmt.DebugU32) self.criteria
  let dyn1 :=
    Dyn.mk _ (core.fmt.DebugShared core.fmt.DebugU32) self.reviews_given
  core.fmt.Formatter.debug_struct_field2_finish f (toStr "CriticState") (toStr
    "criteria") dyn (toStr "reviews_given") dyn1

/-- Trait implementation: [multi_agent::agent_trait::{core::fmt::Debug for multi_agent::agent_trait::CriticState}]
    Source: 'src/agent_trait.rs', lines 76:31-76:36 -/
@[reducible]
def agent_trait.CriticState.Insts.CoreFmtDebug : core.fmt.Debug
  agent_trait.CriticState := {
  fmt := agent_trait.CriticState.Insts.CoreFmtDebug.fmt
}

/-- [multi_agent::agent_trait::AgentKind]
    Source: 'src/agent_trait.rs', lines 84:0-88:1 -/
@[discriminant isize]
inductive agent_trait.AgentKind where
| Coordinator : agent_trait.CoordinatorState → agent_trait.AgentKind
| Specialist : agent_trait.SpecialistState → agent_trait.AgentKind
| Critic : agent_trait.CriticState → agent_trait.AgentKind

/-- [multi_agent::agent_trait::{core::clone::Clone for multi_agent::agent_trait::AgentKind}::clone]:
    Source: 'src/agent_trait.rs', lines 83:9-83:14 -/
def agent_trait.AgentKind.Insts.CoreCloneClone.clone
  (self : agent_trait.AgentKind) : Result agent_trait.AgentKind := do
  match self with
  | agent_trait.AgentKind.Coordinator __self_0 =>
    let cs ← agent_trait.CoordinatorState.Insts.CoreCloneClone.clone __self_0
    ok (agent_trait.AgentKind.Coordinator cs)
  | agent_trait.AgentKind.Specialist __self_0 =>
    let ss ← agent_trait.SpecialistState.Insts.CoreCloneClone.clone __self_0
    ok (agent_trait.AgentKind.Specialist ss)
  | agent_trait.AgentKind.Critic __self_0 =>
    let cs ← agent_trait.CriticState.Insts.CoreCloneClone.clone __self_0
    ok (agent_trait.AgentKind.Critic cs)

/-- Trait implementation: [multi_agent::agent_trait::{core::clone::Clone for multi_agent::agent_trait::AgentKind}]
    Source: 'src/agent_trait.rs', lines 83:9-83:14 -/
@[reducible]
def agent_trait.AgentKind.Insts.CoreCloneClone : core.clone.Clone
  agent_trait.AgentKind := {
  clone := agent_trait.AgentKind.Insts.CoreCloneClone.clone
}

/-- Trait implementation: [multi_agent::agent_trait::{core::marker::StructuralPartialEq for multi_agent::agent_trait::AgentKind}]
    Source: 'src/agent_trait.rs', lines 83:16-83:25 -/
@[reducible]
def agent_trait.AgentKind.Insts.CoreMarkerStructuralPartialEq :
  core.marker.StructuralPartialEq agent_trait.AgentKind := {
}

/-- [multi_agent::agent_trait::{core::cmp::PartialEq<multi_agent::agent_trait::AgentKind> for multi_agent::agent_trait::AgentKind}::ne]:
    Source: 'src/agent_trait.rs', lines 83:16-83:25 -/
axiom agent_trait.AgentKind.Insts.CoreCmpPartialEqAgentKind.ne
  : agent_trait.AgentKind → agent_trait.AgentKind → Result Bool

/-- [multi_agent::agent_trait::{core::cmp::PartialEq<multi_agent::agent_trait::AgentKind> for multi_agent::agent_trait::AgentKind}::eq]:
    Source: 'src/agent_trait.rs', lines 83:16-83:25 -/
def agent_trait.AgentKind.Insts.CoreCmpPartialEqAgentKind.eq
  (self : agent_trait.AgentKind) (other : agent_trait.AgentKind) :
  Result Bool
  := do
  let self1 := read_discriminant self
  let other1 := read_discriminant other
  if self1 = other1
  then
    match self with
    | agent_trait.AgentKind.Coordinator __self_0 =>
      match other with
      | agent_trait.AgentKind.Coordinator __arg1_0 =>
        agent_trait.CoordinatorState.Insts.CoreCmpPartialEqCoordinatorState.eq
          __self_0 __arg1_0
      | agent_trait.AgentKind.Specialist _ => fail panic
      | agent_trait.AgentKind.Critic _ => fail panic
    | agent_trait.AgentKind.Specialist __self_0 =>
      match other with
      | agent_trait.AgentKind.Coordinator _ => fail panic
      | agent_trait.AgentKind.Specialist __arg1_0 =>
        agent_trait.SpecialistState.Insts.CoreCmpPartialEqSpecialistState.eq
          __self_0 __arg1_0
      | agent_trait.AgentKind.Critic _ => fail panic
    | agent_trait.AgentKind.Critic __self_0 =>
      match other with
      | agent_trait.AgentKind.Coordinator _ => fail panic
      | agent_trait.AgentKind.Specialist _ => fail panic
      | agent_trait.AgentKind.Critic __arg1_0 =>
        agent_trait.CriticState.Insts.CoreCmpPartialEqCriticState.eq __self_0
          __arg1_0
  else ok false

/-- Trait implementation: [multi_agent::agent_trait::{core::cmp::PartialEq<multi_agent::agent_trait::AgentKind> for multi_agent::agent_trait::AgentKind}]
    Source: 'src/agent_trait.rs', lines 83:16-83:25 -/
@[reducible]
def agent_trait.AgentKind.Insts.CoreCmpPartialEqAgentKind : core.cmp.PartialEq
  agent_trait.AgentKind agent_trait.AgentKind := {
  eq := agent_trait.AgentKind.Insts.CoreCmpPartialEqAgentKind.eq
  ne := agent_trait.AgentKind.Insts.CoreCmpPartialEqAgentKind.ne
}

/-- [multi_agent::agent_trait::{core::cmp::Eq for multi_agent::agent_trait::AgentKind}::assert_receiver_is_total_eq]:
    Source: 'src/agent_trait.rs', lines 83:27-83:29 -/
def agent_trait.AgentKind.Insts.CoreCmpEq.assert_receiver_is_total_eq
  (self : agent_trait.AgentKind) : Result Unit := do
  ok ()

/-- Trait implementation: [multi_agent::agent_trait::{core::cmp::Eq for multi_agent::agent_trait::AgentKind}]
    Source: 'src/agent_trait.rs', lines 83:27-83:29 -/
@[reducible]
def agent_trait.AgentKind.Insts.CoreCmpEq : core.cmp.Eq agent_trait.AgentKind
  := {
  partialEqInst := agent_trait.AgentKind.Insts.CoreCmpPartialEqAgentKind
  assert_receiver_is_total_eq :=
    agent_trait.AgentKind.Insts.CoreCmpEq.assert_receiver_is_total_eq
}

/-- [multi_agent::agent_trait::{core::fmt::Debug for multi_agent::agent_trait::AgentKind}::fmt]:
    Source: 'src/agent_trait.rs', lines 83:31-83:36 -/
def agent_trait.AgentKind.Insts.CoreFmtDebug.fmt
  (self : agent_trait.AgentKind) (f : core.fmt.Formatter) :
  Result ((core.result.Result Unit core.fmt.Error) × core.fmt.Formatter)
  := do
  match self with
  | agent_trait.AgentKind.Coordinator __self_0 =>
    let __self_01 :=
      Dyn.mk _ (core.fmt.DebugShared
        agent_trait.CoordinatorState.Insts.CoreFmtDebug) __self_0
    core.fmt.Formatter.debug_tuple_field1_finish f (toStr "Coordinator")
      __self_01
  | agent_trait.AgentKind.Specialist __self_0 =>
    let __self_01 :=
      Dyn.mk _ (core.fmt.DebugShared
        agent_trait.SpecialistState.Insts.CoreFmtDebug) __self_0
    core.fmt.Formatter.debug_tuple_field1_finish f (toStr "Specialist")
      __self_01
  | agent_trait.AgentKind.Critic __self_0 =>
    let __self_01 :=
      Dyn.mk _ (core.fmt.DebugShared
        agent_trait.CriticState.Insts.CoreFmtDebug) __self_0
    core.fmt.Formatter.debug_tuple_field1_finish f (toStr "Critic") __self_01

/-- Trait implementation: [multi_agent::agent_trait::{core::fmt::Debug for multi_agent::agent_trait::AgentKind}]
    Source: 'src/agent_trait.rs', lines 83:31-83:36 -/
@[reducible]
def agent_trait.AgentKind.Insts.CoreFmtDebug : core.fmt.Debug
  agent_trait.AgentKind := {
  fmt := agent_trait.AgentKind.Insts.CoreFmtDebug.fmt
}

/-- [multi_agent::agent_trait::AgentInstance]
    Source: 'src/agent_trait.rs', lines 92:0-98:1 -/
structure agent_trait.AgentInstance where
  id : Std.U32
  kind : agent_trait.AgentKind
  state : agent_trait.AgentState
  inbox : alloc.vec.Vec agent_trait.Envelope
  outbox : alloc.vec.Vec agent_trait.Envelope

/-- [multi_agent::agent_trait::{core::clone::Clone for multi_agent::agent_trait::AgentInstance}::clone]:
    Source: 'src/agent_trait.rs', lines 91:9-91:14 -/
def agent_trait.AgentInstance.Insts.CoreCloneClone.clone
  (self : agent_trait.AgentInstance) : Result agent_trait.AgentInstance := do
  let i ← lift (core.clone.impls.CloneU32.clone self.id)
  let ak ← agent_trait.AgentKind.Insts.CoreCloneClone.clone self.kind
  let as1 ← agent_trait.AgentState.Insts.CoreCloneClone.clone self.state
  let v ←
    alloc.vec.CloneVec.clone agent_trait.Envelope.Insts.CoreCloneClone
      self.inbox
  let v1 ←
    alloc.vec.CloneVec.clone agent_trait.Envelope.Insts.CoreCloneClone
      self.outbox
  ok { id := i, kind := ak, state := as1, inbox := v, outbox := v1 }

/-- Trait implementation: [multi_agent::agent_trait::{core::clone::Clone for multi_agent::agent_trait::AgentInstance}]
    Source: 'src/agent_trait.rs', lines 91:9-91:14 -/
@[reducible]
def agent_trait.AgentInstance.Insts.CoreCloneClone : core.clone.Clone
  agent_trait.AgentInstance := {
  clone := agent_trait.AgentInstance.Insts.CoreCloneClone.clone
}

/-- Trait implementation: [multi_agent::agent_trait::{core::marker::StructuralPartialEq for multi_agent::agent_trait::AgentInstance}]
    Source: 'src/agent_trait.rs', lines 91:16-91:25 -/
@[reducible]
def agent_trait.AgentInstance.Insts.CoreMarkerStructuralPartialEq :
  core.marker.StructuralPartialEq agent_trait.AgentInstance := {
}

/-- [multi_agent::agent_trait::{core::cmp::PartialEq<multi_agent::agent_trait::AgentInstance> for multi_agent::agent_trait::AgentInstance}::ne]:
    Source: 'src/agent_trait.rs', lines 91:16-91:25 -/
axiom agent_trait.AgentInstance.Insts.CoreCmpPartialEqAgentInstance.ne
  : agent_trait.AgentInstance → agent_trait.AgentInstance → Result Bool

/-- [multi_agent::agent_trait::{core::cmp::PartialEq<multi_agent::agent_trait::AgentInstance> for multi_agent::agent_trait::AgentInstance}::eq]:
    Source: 'src/agent_trait.rs', lines 91:16-91:25 -/
def agent_trait.AgentInstance.Insts.CoreCmpPartialEqAgentInstance.eq
  (self : agent_trait.AgentInstance) (other : agent_trait.AgentInstance) :
  Result Bool
  := do
  if self.id = other.id
  then
    let b ←
      agent_trait.AgentKind.Insts.CoreCmpPartialEqAgentKind.eq self.kind
        other.kind
    if b
    then
      let b1 ←
        agent_trait.AgentState.Insts.CoreCmpPartialEqAgentState.eq self.state
          other.state
      if b1
      then
        let b2 ←
          alloc.vec.partial_eq.PartialEqVec.eq
            agent_trait.Envelope.Insts.CoreCmpPartialEqEnvelope self.inbox
            other.inbox
        if b2
        then
          alloc.vec.partial_eq.PartialEqVec.eq
            agent_trait.Envelope.Insts.CoreCmpPartialEqEnvelope self.outbox
            other.outbox
        else ok false
      else ok false
    else ok false
  else ok false

/-- Trait implementation: [multi_agent::agent_trait::{core::cmp::PartialEq<multi_agent::agent_trait::AgentInstance> for multi_agent::agent_trait::AgentInstance}]
    Source: 'src/agent_trait.rs', lines 91:16-91:25 -/
@[reducible]
def agent_trait.AgentInstance.Insts.CoreCmpPartialEqAgentInstance :
  core.cmp.PartialEq agent_trait.AgentInstance agent_trait.AgentInstance := {
  eq := agent_trait.AgentInstance.Insts.CoreCmpPartialEqAgentInstance.eq
  ne := agent_trait.AgentInstance.Insts.CoreCmpPartialEqAgentInstance.ne
}

/-- [multi_agent::agent_trait::{core::cmp::Eq for multi_agent::agent_trait::AgentInstance}::assert_receiver_is_total_eq]:
    Source: 'src/agent_trait.rs', lines 91:27-91:29 -/
def agent_trait.AgentInstance.Insts.CoreCmpEq.assert_receiver_is_total_eq
  (self : agent_trait.AgentInstance) : Result Unit := do
  ok ()

/-- Trait implementation: [multi_agent::agent_trait::{core::cmp::Eq for multi_agent::agent_trait::AgentInstance}]
    Source: 'src/agent_trait.rs', lines 91:27-91:29 -/
@[reducible]
def agent_trait.AgentInstance.Insts.CoreCmpEq : core.cmp.Eq
  agent_trait.AgentInstance := {
  partialEqInst :=
    agent_trait.AgentInstance.Insts.CoreCmpPartialEqAgentInstance
  assert_receiver_is_total_eq :=
    agent_trait.AgentInstance.Insts.CoreCmpEq.assert_receiver_is_total_eq
}

/-- [multi_agent::agent_trait::{core::fmt::Debug for multi_agent::agent_trait::AgentInstance}::fmt]:
    Source: 'src/agent_trait.rs', lines 91:31-91:36 -/
def agent_trait.AgentInstance.Insts.CoreFmtDebug.fmt
  (self : agent_trait.AgentInstance) (f : core.fmt.Formatter) :
  Result ((core.result.Result Unit core.fmt.Error) × core.fmt.Formatter)
  := do
  let dyn := Dyn.mk _ core.fmt.DebugU32 self.id
  let dyn1 := Dyn.mk _ agent_trait.AgentKind.Insts.CoreFmtDebug self.kind
  let dyn2 := Dyn.mk _ agent_trait.AgentState.Insts.CoreFmtDebug self.state
  let dyn3 :=
    Dyn.mk _ (core.fmt.DebugVec agent_trait.Envelope.Insts.CoreFmtDebug)
      self.inbox
  let dyn4 :=
    Dyn.mk _ (core.fmt.DebugShared (core.fmt.DebugVec
      agent_trait.Envelope.Insts.CoreFmtDebug)) self.outbox
  core.fmt.Formatter.debug_struct_field5_finish f (toStr "AgentInstance")
    (toStr "id") dyn (toStr "kind") dyn1 (toStr "state") dyn2 (toStr "inbox")
    dyn3 (toStr "outbox") dyn4

/-- Trait implementation: [multi_agent::agent_trait::{core::fmt::Debug for multi_agent::agent_trait::AgentInstance}]
    Source: 'src/agent_trait.rs', lines 91:31-91:36 -/
@[reducible]
def agent_trait.AgentInstance.Insts.CoreFmtDebug : core.fmt.Debug
  agent_trait.AgentInstance := {
  fmt := agent_trait.AgentInstance.Insts.CoreFmtDebug.fmt
}

/-- [multi_agent::agents::coordinator::coordinator_process]: loop body 0:
    Source: 'src/agents/coordinator.rs', lines 20:12-30:13 -/
@[rust_loop_body]
def agents.coordinator.coordinator_process_loop0.body
  (i : Std.U32) (self_id : Std.U32) (iter : core.slice.iter.Iter Std.U32)
  (outbox : alloc.vec.Vec agent_trait.Envelope) :
  Result (ControlFlow ((core.slice.iter.Iter Std.U32) × (alloc.vec.Vec
    agent_trait.Envelope)) (alloc.vec.Vec agent_trait.Envelope))
  := do
  let (o, iter1) ← core.slice.iter.IteratorSliceIter.next iter
  match o with
  | none => ok (done outbox)
  | some agent_id =>
    let outbox1 ←
      alloc.vec.Vec.push outbox
        ({
           sender := self_id,
           recipient := (agent_trait.Recipient.Direct agent_id),
           message :=
             { kind := agent_trait.MessageKind.Delegation, content_id := i },
           sequence_num := 0#u32
         } : agent_trait.Envelope)
    ok (cont (iter1, outbox1))

/-- [multi_agent::agents::coordinator::coordinator_process]: loop 0:
    Source: 'src/agents/coordinator.rs', lines 20:12-30:13 -/
@[rust_loop]
def agents.coordinator.coordinator_process_loop0
  (iter : core.slice.iter.Iter Std.U32) (i : Std.U32) (self_id : Std.U32)
  (outbox : alloc.vec.Vec agent_trait.Envelope) :
  Result (alloc.vec.Vec agent_trait.Envelope)
  := do
  loop
    (fun (iter1, outbox1) => agents.coordinator.coordinator_process_loop0.body
      i self_id iter1 outbox1)
    (iter, outbox)

/-- [multi_agent::agents::coordinator::coordinator_process]: loop body 1:
    Source: 'src/agents/coordinator.rs', lines 67:20-77:21 -/
@[rust_loop_body]
def agents.coordinator.coordinator_process_loop1.body
  (i : Std.U32) (self_id : Std.U32) (iter : core.slice.iter.Iter Std.U32)
  (outbox : alloc.vec.Vec agent_trait.Envelope) :
  Result (ControlFlow ((core.slice.iter.Iter Std.U32) × (alloc.vec.Vec
    agent_trait.Envelope)) (alloc.vec.Vec agent_trait.Envelope))
  := do
  let (o, iter1) ← core.slice.iter.IteratorSliceIter.next iter
  match o with
  | none => ok (done outbox)
  | some agent_id =>
    let outbox1 ←
      alloc.vec.Vec.push outbox
        ({
           sender := self_id,
           recipient := (agent_trait.Recipient.Direct agent_id),
           message := { kind := agent_trait.MessageKind.Vote, content_id := i },
           sequence_num := 0#u32
         } : agent_trait.Envelope)
    ok (cont (iter1, outbox1))

/-- [multi_agent::agents::coordinator::coordinator_process]: loop 1:
    Source: 'src/agents/coordinator.rs', lines 67:20-77:21 -/
@[rust_loop]
def agents.coordinator.coordinator_process_loop1
  (iter : core.slice.iter.Iter Std.U32) (i : Std.U32) (self_id : Std.U32)
  (outbox : alloc.vec.Vec agent_trait.Envelope) :
  Result (alloc.vec.Vec agent_trait.Envelope)
  := do
  loop
    (fun (iter1, outbox1) => agents.coordinator.coordinator_process_loop1.body
      i self_id iter1 outbox1)
    (iter, outbox)

/-- [multi_agent::agents::coordinator::coordinator_process]:
    Source: 'src/agents/coordinator.rs', lines 9:0-86:1 -/
def agents.coordinator.coordinator_process
  (state : agent_trait.CoordinatorState) (envelope : agent_trait.Envelope)
  (self_id : Std.U32) :
  Result ((alloc.vec.Vec agent_trait.Envelope) × agent_trait.CoordinatorState)
  := do
  match envelope.message.kind with
  | agent_trait.MessageKind.Task =>
    let i := alloc.vec.Vec.len state.managed_agents
    let i1 ← lift (UScalar.cast .U32 i)
    let i2 ← state.round + 1#u32
    let iter ←
      SharedAVec.Insts.CoreIterTraitsCollectIntoIteratorSharedATIter.into_iter
        Global state.managed_agents
    let outbox ←
      agents.coordinator.coordinator_process_loop0 iter
        envelope.message.content_id self_id (alloc.vec.Vec.new
        agent_trait.Envelope)
    ok (outbox, { state with pending_responses := i1, round := i2 })
  | agent_trait.MessageKind.Response =>
    let i ←
      if state.pending_responses > 0#u32
      then state.pending_responses - 1#u32
      else ok state.pending_responses
    if i = 0#u32
    then
      let outbox ←
        alloc.vec.Vec.push (alloc.vec.Vec.new agent_trait.Envelope)
          ({
             sender := self_id,
             recipient := (agent_trait.Recipient.Direct envelope.sender),
             message :=
               {
                 envelope.message
                   with
                   kind := agent_trait.MessageKind.Completion
               },
             sequence_num := 0#u32
           } : agent_trait.Envelope)
      ok (outbox, { state with pending_responses := i })
    else
      ok (alloc.vec.Vec.new agent_trait.Envelope,
        { state with pending_responses := i })
  | agent_trait.MessageKind.Review =>
    match state.protocol with
    | agent_trait.ProtocolKind.RequestResponse =>
      ok (alloc.vec.Vec.new agent_trait.Envelope, state)
    | agent_trait.ProtocolKind.Pipeline =>
      ok (alloc.vec.Vec.new agent_trait.Envelope, state)
    | agent_trait.ProtocolKind.Debate =>
      let outbox ←
        alloc.vec.Vec.push (alloc.vec.Vec.new agent_trait.Envelope)
          ({
             sender := self_id,
             recipient := agent_trait.Recipient.Broadcast,
             message := envelope.message,
             sequence_num := 0#u32
           } : agent_trait.Envelope)
      ok (outbox, state)
    | agent_trait.ProtocolKind.Voting =>
      let iter ←
        SharedAVec.Insts.CoreIterTraitsCollectIntoIteratorSharedATIter.into_iter
          Global state.managed_agents
      let outbox ←
        agents.coordinator.coordinator_process_loop1 iter
          envelope.message.content_id self_id (alloc.vec.Vec.new
          agent_trait.Envelope)
      let i := alloc.vec.Vec.len state.managed_agents
      let i1 ← lift (UScalar.cast .U32 i)
      ok (outbox, { state with pending_responses := i1 })
  | agent_trait.MessageKind.Vote =>
    ok (alloc.vec.Vec.new agent_trait.Envelope, state)
  | agent_trait.MessageKind.Delegation =>
    ok (alloc.vec.Vec.new agent_trait.Envelope, state)
  | agent_trait.MessageKind.Completion =>
    ok (alloc.vec.Vec.new agent_trait.Envelope, state)
  | agent_trait.MessageKind.Error =>
    ok (alloc.vec.Vec.new agent_trait.Envelope, state)

/-- [multi_agent::agents::critic::critic_process]:
    Source: 'src/agents/critic.rs', lines 6:0-44:1 -/
def agents.critic.critic_process
  (state : agent_trait.CriticState) (envelope : agent_trait.Envelope)
  (self_id : Std.U32) :
  Result ((alloc.vec.Vec agent_trait.Envelope) × agent_trait.CriticState)
  := do
  match envelope.message.kind with
  | agent_trait.MessageKind.Task =>
    let i ← state.reviews_given + 1#u32
    let outbox ←
      alloc.vec.Vec.push (alloc.vec.Vec.new agent_trait.Envelope)
        ({
           sender := self_id,
           recipient := (agent_trait.Recipient.Direct envelope.sender),
           message :=
             { envelope.message with kind := agent_trait.MessageKind.Review },
           sequence_num := 0#u32
         } : agent_trait.Envelope)
    ok (outbox, { state with reviews_given := i })
  | agent_trait.MessageKind.Response =>
    let i ← state.reviews_given + 1#u32
    let outbox ←
      alloc.vec.Vec.push (alloc.vec.Vec.new agent_trait.Envelope)
        ({
           sender := self_id,
           recipient := (agent_trait.Recipient.Direct envelope.sender),
           message :=
             { envelope.message with kind := agent_trait.MessageKind.Review },
           sequence_num := 0#u32
         } : agent_trait.Envelope)
    ok (outbox, { state with reviews_given := i })
  | agent_trait.MessageKind.Review =>
    ok (alloc.vec.Vec.new agent_trait.Envelope, state)
  | agent_trait.MessageKind.Vote =>
    let b ← alloc.vec.Vec.is_empty Global state.criteria
    let vote_value ← if b
                       then ok 0#i32
                       else ok 1#i32
    let i ← lift (IScalar.hcast .U32 vote_value)
    let outbox ←
      alloc.vec.Vec.push (alloc.vec.Vec.new agent_trait.Envelope)
        ({
           sender := self_id,
           recipient := (agent_trait.Recipient.Direct envelope.sender),
           message := { kind := agent_trait.MessageKind.Vote, content_id := i },
           sequence_num := 0#u32
         } : agent_trait.Envelope)
    ok (outbox, state)
  | agent_trait.MessageKind.Delegation =>
    let i ← state.reviews_given + 1#u32
    let outbox ←
      alloc.vec.Vec.push (alloc.vec.Vec.new agent_trait.Envelope)
        ({
           sender := self_id,
           recipient := (agent_trait.Recipient.Direct envelope.sender),
           message :=
             { envelope.message with kind := agent_trait.MessageKind.Review },
           sequence_num := 0#u32
         } : agent_trait.Envelope)
    ok (outbox, { state with reviews_given := i })
  | agent_trait.MessageKind.Completion =>
    ok (alloc.vec.Vec.new agent_trait.Envelope, state)
  | agent_trait.MessageKind.Error =>
    ok (alloc.vec.Vec.new agent_trait.Envelope, state)

/-- [multi_agent::agents::specialist::specialist_process]:
    Source: 'src/agents/specialist.rs', lines 7:0-49:1 -/
def agents.specialist.specialist_process
  (state : agent_trait.SpecialistState) (envelope : agent_trait.Envelope)
  (self_id : Std.U32) :
  Result ((alloc.vec.Vec agent_trait.Envelope) × agent_trait.SpecialistState)
  := do
  sorry

/-- [multi_agent::agents::agent_process]: loop body 0:
    Source: 'src/agents/mod.rs', lines 18:4-33:5 -/
@[rust_loop_body]
def agents.agent_process_loop.body
  (i : Std.U32) (iter : core.slice.iter.Iter agent_trait.Envelope)
  (ak : agent_trait.AgentKind) (v : alloc.vec.Vec agent_trait.Envelope) :
  Result (ControlFlow ((core.slice.iter.Iter agent_trait.Envelope) ×
    agent_trait.AgentKind × (alloc.vec.Vec agent_trait.Envelope))
    (agent_trait.AgentKind × (alloc.vec.Vec agent_trait.Envelope)))
  := do
  let (o, iter1) ← core.slice.iter.IteratorSliceIter.next iter
  match o with
  | none => ok (done (ak, v))
  | some env =>
    match ak with
    | agent_trait.AgentKind.Coordinator cs =>
      let (out, cs1) ← agents.coordinator.coordinator_process cs env i
      let v1 ←
        alloc.vec.Vec.Insts.CoreIterTraitsCollectExtend.extend Global
          (core.iter.traits.collect.IntoIteratorVec agent_trait.Envelope) v out
      ok (cont (iter1, agent_trait.AgentKind.Coordinator cs1, v1))
    | agent_trait.AgentKind.Specialist ss =>
      let (out, ss1) ← agents.specialist.specialist_process ss env i
      let v1 ←
        alloc.vec.Vec.Insts.CoreIterTraitsCollectExtend.extend Global
          (core.iter.traits.collect.IntoIteratorVec agent_trait.Envelope) v out
      ok (cont (iter1, agent_trait.AgentKind.Specialist ss1, v1))
    | agent_trait.AgentKind.Critic cs =>
      let (out, cs1) ← agents.critic.critic_process cs env i
      let v1 ←
        alloc.vec.Vec.Insts.CoreIterTraitsCollectExtend.extend Global
          (core.iter.traits.collect.IntoIteratorVec agent_trait.Envelope) v out
      ok (cont (iter1, agent_trait.AgentKind.Critic cs1, v1))

/-- [multi_agent::agents::agent_process]: loop 0:
    Source: 'src/agents/mod.rs', lines 18:4-33:5 -/
@[rust_loop]
def agents.agent_process_loop
  (iter : core.slice.iter.Iter agent_trait.Envelope) (i : Std.U32)
  (ak : agent_trait.AgentKind) (v : alloc.vec.Vec agent_trait.Envelope) :
  Result (agent_trait.AgentKind × (alloc.vec.Vec agent_trait.Envelope))
  := do
  loop
    (fun (iter1, ak1, v1) => agents.agent_process_loop.body i iter1 ak1 v1)
    (iter, ak, v)

/-- [multi_agent::agents::agent_process]:
    Source: 'src/agents/mod.rs', lines 9:0-35:1 -/
def agents.agent_process
  (agent : agent_trait.AgentInstance) : Result agent_trait.AgentInstance := do
  let b ←
    agent_trait.AgentState.Insts.CoreCmpPartialEqAgentState.eq agent.state
      agent_trait.AgentState.Finished
  if b
  then ok agent
  else
    let b1 ←
      agent_trait.AgentState.Insts.CoreCmpPartialEqAgentState.eq agent.state
        agent_trait.AgentState.Failed
    if b1
    then ok agent
    else
      let b2 ← alloc.vec.Vec.is_empty Global agent.inbox
      if b2
      then ok agent
      else
        let (envelopes, v) ←
          core.mem.take (alloc.vec.Vec.Insts.CoreDefaultDefault
            agent_trait.Envelope) agent.inbox
        let iter ←
          SharedAVec.Insts.CoreIterTraitsCollectIntoIteratorSharedATIter.into_iter
            Global envelopes
        let (ak, v1) ←
          agents.agent_process_loop iter agent.id agent.kind agent.outbox
        ok
          {
            agent
              with
              kind := ak,
              state := agent_trait.AgentState.Ready,
              inbox := v,
              outbox := v1
          }

/-- [multi_agent::agents::is_agent_active]:
    Source: 'src/agents/mod.rs', lines 38:0-40:1 -/
def agents.is_agent_active
  (agent : agent_trait.AgentInstance) : Result Bool := do
  let b ←
    agent_trait.AgentState.Insts.CoreCmpPartialEqAgentState.ne agent.state
      agent_trait.AgentState.Finished
  if b
  then
    agent_trait.AgentState.Insts.CoreCmpPartialEqAgentState.ne agent.state
      agent_trait.AgentState.Failed
  else ok false

/-- [multi_agent::message_bus::MessageBus]
    Source: 'src/message_bus.rs', lines 5:0-9:1 -/
structure message_bus.MessageBus where
  queue : alloc.vec.Vec agent_trait.Envelope
  delivered : alloc.vec.Vec agent_trait.Envelope
  next_seq : Std.U32

/-- [multi_agent::message_bus::{core::clone::Clone for multi_agent::message_bus::MessageBus}::clone]:
    Source: 'src/message_bus.rs', lines 4:9-4:14 -/
def message_bus.MessageBus.Insts.CoreCloneClone.clone
  (self : message_bus.MessageBus) : Result message_bus.MessageBus := do
  let v ←
    alloc.vec.CloneVec.clone agent_trait.Envelope.Insts.CoreCloneClone
      self.queue
  let v1 ←
    alloc.vec.CloneVec.clone agent_trait.Envelope.Insts.CoreCloneClone
      self.delivered
  let i ← lift (core.clone.impls.CloneU32.clone self.next_seq)
  ok { queue := v, delivered := v1, next_seq := i }

/-- Trait implementation: [multi_agent::message_bus::{core::clone::Clone for multi_agent::message_bus::MessageBus}]
    Source: 'src/message_bus.rs', lines 4:9-4:14 -/
@[reducible]
def message_bus.MessageBus.Insts.CoreCloneClone : core.clone.Clone
  message_bus.MessageBus := {
  clone := message_bus.MessageBus.Insts.CoreCloneClone.clone
}

/-- Trait implementation: [multi_agent::message_bus::{core::marker::StructuralPartialEq for multi_agent::message_bus::MessageBus}]
    Source: 'src/message_bus.rs', lines 4:16-4:25 -/
@[reducible]
def message_bus.MessageBus.Insts.CoreMarkerStructuralPartialEq :
  core.marker.StructuralPartialEq message_bus.MessageBus := {
}

/-- [multi_agent::message_bus::{core::cmp::PartialEq<multi_agent::message_bus::MessageBus> for multi_agent::message_bus::MessageBus}::ne]:
    Source: 'src/message_bus.rs', lines 4:16-4:25 -/
axiom message_bus.MessageBus.Insts.CoreCmpPartialEqMessageBus.ne
  : message_bus.MessageBus → message_bus.MessageBus → Result Bool

/-- [multi_agent::message_bus::{core::cmp::PartialEq<multi_agent::message_bus::MessageBus> for multi_agent::message_bus::MessageBus}::eq]:
    Source: 'src/message_bus.rs', lines 4:16-4:25 -/
def message_bus.MessageBus.Insts.CoreCmpPartialEqMessageBus.eq
  (self : message_bus.MessageBus) (other : message_bus.MessageBus) :
  Result Bool
  := do
  if self.next_seq = other.next_seq
  then
    let b ←
      alloc.vec.partial_eq.PartialEqVec.eq
        agent_trait.Envelope.Insts.CoreCmpPartialEqEnvelope self.queue
        other.queue
    if b
    then
      alloc.vec.partial_eq.PartialEqVec.eq
        agent_trait.Envelope.Insts.CoreCmpPartialEqEnvelope self.delivered
        other.delivered
    else ok false
  else ok false

/-- Trait implementation: [multi_agent::message_bus::{core::cmp::PartialEq<multi_agent::message_bus::MessageBus> for multi_agent::message_bus::MessageBus}]
    Source: 'src/message_bus.rs', lines 4:16-4:25 -/
@[reducible]
def message_bus.MessageBus.Insts.CoreCmpPartialEqMessageBus :
  core.cmp.PartialEq message_bus.MessageBus message_bus.MessageBus := {
  eq := message_bus.MessageBus.Insts.CoreCmpPartialEqMessageBus.eq
  ne := message_bus.MessageBus.Insts.CoreCmpPartialEqMessageBus.ne
}

/-- [multi_agent::message_bus::{core::cmp::Eq for multi_agent::message_bus::MessageBus}::assert_receiver_is_total_eq]:
    Source: 'src/message_bus.rs', lines 4:27-4:29 -/
def message_bus.MessageBus.Insts.CoreCmpEq.assert_receiver_is_total_eq
  (self : message_bus.MessageBus) : Result Unit := do
  ok ()

/-- Trait implementation: [multi_agent::message_bus::{core::cmp::Eq for multi_agent::message_bus::MessageBus}]
    Source: 'src/message_bus.rs', lines 4:27-4:29 -/
@[reducible]
def message_bus.MessageBus.Insts.CoreCmpEq : core.cmp.Eq message_bus.MessageBus
  := {
  partialEqInst := message_bus.MessageBus.Insts.CoreCmpPartialEqMessageBus
  assert_receiver_is_total_eq :=
    message_bus.MessageBus.Insts.CoreCmpEq.assert_receiver_is_total_eq
}

/-- [multi_agent::message_bus::{core::fmt::Debug for multi_agent::message_bus::MessageBus}::fmt]:
    Source: 'src/message_bus.rs', lines 4:31-4:36 -/
def message_bus.MessageBus.Insts.CoreFmtDebug.fmt
  (self : message_bus.MessageBus) (f : core.fmt.Formatter) :
  Result ((core.result.Result Unit core.fmt.Error) × core.fmt.Formatter)
  := do
  let dyn :=
    Dyn.mk _ (core.fmt.DebugVec agent_trait.Envelope.Insts.CoreFmtDebug)
      self.queue
  let dyn1 :=
    Dyn.mk _ (core.fmt.DebugVec agent_trait.Envelope.Insts.CoreFmtDebug)
      self.delivered
  let dyn2 := Dyn.mk _ (core.fmt.DebugShared core.fmt.DebugU32) self.next_seq
  core.fmt.Formatter.debug_struct_field3_finish f (toStr "MessageBus") (toStr
    "queue") dyn (toStr "delivered") dyn1 (toStr "next_seq") dyn2

/-- Trait implementation: [multi_agent::message_bus::{core::fmt::Debug for multi_agent::message_bus::MessageBus}]
    Source: 'src/message_bus.rs', lines 4:31-4:36 -/
@[reducible]
def message_bus.MessageBus.Insts.CoreFmtDebug : core.fmt.Debug
  message_bus.MessageBus := {
  fmt := message_bus.MessageBus.Insts.CoreFmtDebug.fmt
}

/-- [multi_agent::message_bus::bus_new]:
    Source: 'src/message_bus.rs', lines 12:0-18:1 -/
def message_bus.bus_new : Result message_bus.MessageBus := do
  ok
    {
      queue := (alloc.vec.Vec.new agent_trait.Envelope),
      delivered := (alloc.vec.Vec.new agent_trait.Envelope),
      next_seq := 0#u32
    }

/-- [multi_agent::message_bus::bus_send]:
    Source: 'src/message_bus.rs', lines 21:0-25:1 -/
def message_bus.bus_send
  (bus : message_bus.MessageBus) (env : agent_trait.Envelope) :
  Result message_bus.MessageBus
  := do
  let i ← bus.next_seq + 1#u32
  let v ←
    alloc.vec.Vec.push bus.queue { env with sequence_num := bus.next_seq }
  ok { bus with queue := v, next_seq := i }

/-- [multi_agent::message_bus::envelope_targets]:
    Source: 'src/message_bus.rs', lines 28:0-39:1 -/
def message_bus.envelope_targets
  (env : agent_trait.Envelope) (agent_id : Std.U32) : Result Bool := do
  match env.recipient with
  | agent_trait.Recipient.Direct id => ok (id = agent_id)
  | agent_trait.Recipient.Broadcast => ok true
  | agent_trait.Recipient.Topic _ => ok false

/-- [multi_agent::message_bus::bus_deliver]: loop body 0:
    Source: 'src/message_bus.rs', lines 48:4-55:5 -/
@[rust_loop_body]
def message_bus.bus_deliver_loop.body
  (agent_id : Std.U32)
  (iter : alloc.vec.into_iter.IntoIter agent_trait.Envelope)
  (v : alloc.vec.Vec agent_trait.Envelope)
  (result : alloc.vec.Vec agent_trait.Envelope)
  (remaining : alloc.vec.Vec agent_trait.Envelope) :
  Result (ControlFlow ((alloc.vec.into_iter.IntoIter agent_trait.Envelope) ×
    (alloc.vec.Vec agent_trait.Envelope) × (alloc.vec.Vec
    agent_trait.Envelope) × (alloc.vec.Vec agent_trait.Envelope))
    ((alloc.vec.Vec agent_trait.Envelope) × (alloc.vec.Vec
    agent_trait.Envelope) × (alloc.vec.Vec agent_trait.Envelope)))
  := do
  let (o, iter1) ← alloc.vec.into_iter.IteratorIntoIter.next iter
  match o with
  | none => ok (done (v, result, remaining))
  | some env =>
    let b ← message_bus.envelope_targets env agent_id
    if b
    then
      let e ← agent_trait.Envelope.Insts.CoreCloneClone.clone env
      let result1 ← alloc.vec.Vec.push result e
      let v1 ← alloc.vec.Vec.push v env
      ok (cont (iter1, v1, result1, remaining))
    else
      let remaining1 ← alloc.vec.Vec.push remaining env
      ok (cont (iter1, v, result, remaining1))

/-- [multi_agent::message_bus::bus_deliver]: loop 0:
    Source: 'src/message_bus.rs', lines 48:4-55:5 -/
@[rust_loop]
def message_bus.bus_deliver_loop
  (iter : alloc.vec.into_iter.IntoIter agent_trait.Envelope)
  (v : alloc.vec.Vec agent_trait.Envelope) (agent_id : Std.U32)
  (result : alloc.vec.Vec agent_trait.Envelope)
  (remaining : alloc.vec.Vec agent_trait.Envelope) :
  Result ((alloc.vec.Vec agent_trait.Envelope) × (alloc.vec.Vec
    agent_trait.Envelope) × (alloc.vec.Vec agent_trait.Envelope))
  := do
  loop
    (fun (iter1, v1, result1, remaining1) => message_bus.bus_deliver_loop.body
      agent_id iter1 v1 result1 remaining1)
    (iter, v, result, remaining)

/-- [multi_agent::message_bus::bus_deliver]:
    Source: 'src/message_bus.rs', lines 43:0-58:1 -/
def message_bus.bus_deliver
  (bus : message_bus.MessageBus) (agent_id : Std.U32) :
  Result ((alloc.vec.Vec agent_trait.Envelope) × message_bus.MessageBus)
  := do
  let (queue, _) ←
    core.mem.take (alloc.vec.Vec.Insts.CoreDefaultDefault agent_trait.Envelope)
      bus.queue
  let iter ← alloc.vec.IntoIteratorVec.into_iter queue
  let (v, result, remaining) ←
    message_bus.bus_deliver_loop iter bus.delivered agent_id (alloc.vec.Vec.new
      agent_trait.Envelope) (alloc.vec.Vec.new agent_trait.Envelope)
  ok (result, { bus with queue := remaining, delivered := v })

/-- [multi_agent::message_bus::bus_deliver_next]:
    Source: 'src/message_bus.rs', lines 62:0-69:1 -/
def message_bus.bus_deliver_next
  (bus : message_bus.MessageBus) :
  Result ((Option agent_trait.Envelope) × message_bus.MessageBus)
  := do
  let b ← alloc.vec.Vec.is_empty Global bus.queue
  if b
  then ok (none, bus)
  else
    let (env, v) ← alloc.vec.Vec.remove Global bus.queue 0#usize
    let e ← agent_trait.Envelope.Insts.CoreCloneClone.clone env
    let v1 ← alloc.vec.Vec.push bus.delivered e
    ok (some env, { bus with queue := v, delivered := v1 })

/-- [multi_agent::message_bus::bus_is_empty]:
    Source: 'src/message_bus.rs', lines 72:0-74:1 -/
def message_bus.bus_is_empty (bus : message_bus.MessageBus) : Result Bool := do
  alloc.vec.Vec.is_empty Global bus.queue

/-- [multi_agent::message_bus::bus_delivered_count]:
    Source: 'src/message_bus.rs', lines 77:0-79:1 -/
def message_bus.bus_delivered_count
  (bus : message_bus.MessageBus) : Result Std.U32 := do
  let i := alloc.vec.Vec.len bus.delivered
  ok (UScalar.cast .U32 i)

/-- [multi_agent::message_bus::bus_pending_count]:
    Source: 'src/message_bus.rs', lines 82:0-84:1 -/
def message_bus.bus_pending_count
  (bus : message_bus.MessageBus) : Result Std.U32 := do
  let i := alloc.vec.Vec.len bus.queue
  ok (UScalar.cast .U32 i)

/-- [multi_agent::scheduler::SchedulerKind]
    Source: 'src/scheduler.rs', lines 5:0-8:1 -/
@[discriminant isize]
inductive scheduler.SchedulerKind where
| RoundRobin : scheduler.SchedulerKind
| Priority : scheduler.SchedulerKind

/-- [multi_agent::orchestrator::OrchestratorConfig]
    Source: 'src/orchestrator.rs', lines 9:0-12:1 -/
structure orchestrator.OrchestratorConfig where
  turn_budget : Std.U32
  scheduler_kind : scheduler.SchedulerKind

/-- [multi_agent::scheduler::{core::clone::Clone for multi_agent::scheduler::SchedulerKind}::clone]:
    Source: 'src/scheduler.rs', lines 4:9-4:14 -/
def scheduler.SchedulerKind.Insts.CoreCloneClone.clone
  (self : scheduler.SchedulerKind) : Result scheduler.SchedulerKind := do
  match self with
  | scheduler.SchedulerKind.RoundRobin => ok scheduler.SchedulerKind.RoundRobin
  | scheduler.SchedulerKind.Priority => ok scheduler.SchedulerKind.Priority

/-- [multi_agent::orchestrator::{core::clone::Clone for multi_agent::orchestrator::OrchestratorConfig}::clone]:
    Source: 'src/orchestrator.rs', lines 8:9-8:14 -/
def orchestrator.OrchestratorConfig.Insts.CoreCloneClone.clone
  (self : orchestrator.OrchestratorConfig) :
  Result orchestrator.OrchestratorConfig
  := do
  let i ← lift (core.clone.impls.CloneU32.clone self.turn_budget)
  let sk ←
    scheduler.SchedulerKind.Insts.CoreCloneClone.clone self.scheduler_kind
  ok { turn_budget := i, scheduler_kind := sk }

/-- Trait implementation: [multi_agent::orchestrator::{core::clone::Clone for multi_agent::orchestrator::OrchestratorConfig}]
    Source: 'src/orchestrator.rs', lines 8:9-8:14 -/
@[reducible]
def orchestrator.OrchestratorConfig.Insts.CoreCloneClone : core.clone.Clone
  orchestrator.OrchestratorConfig := {
  clone := orchestrator.OrchestratorConfig.Insts.CoreCloneClone.clone
}

/-- Trait implementation: [multi_agent::orchestrator::{core::marker::StructuralPartialEq for multi_agent::orchestrator::OrchestratorConfig}]
    Source: 'src/orchestrator.rs', lines 8:16-8:25 -/
@[reducible]
def orchestrator.OrchestratorConfig.Insts.CoreMarkerStructuralPartialEq :
  core.marker.StructuralPartialEq orchestrator.OrchestratorConfig := {
}

/-- [multi_agent::orchestrator::{core::cmp::PartialEq<multi_agent::orchestrator::OrchestratorConfig> for multi_agent::orchestrator::OrchestratorConfig}::ne]:
    Source: 'src/orchestrator.rs', lines 8:16-8:25 -/
axiom
  orchestrator.OrchestratorConfig.Insts.CoreCmpPartialEqOrchestratorConfig.ne
  :
  orchestrator.OrchestratorConfig → orchestrator.OrchestratorConfig →
    Result Bool

/-- [multi_agent::scheduler::{core::cmp::PartialEq<multi_agent::scheduler::SchedulerKind> for multi_agent::scheduler::SchedulerKind}::eq]:
    Source: 'src/scheduler.rs', lines 4:16-4:25 -/
def scheduler.SchedulerKind.Insts.CoreCmpPartialEqSchedulerKind.eq
  (self : scheduler.SchedulerKind) (other : scheduler.SchedulerKind) :
  Result Bool
  := do
  let self1 := read_discriminant self
  let other1 := read_discriminant other
  ok (self1 = other1)

/-- [multi_agent::orchestrator::{core::cmp::PartialEq<multi_agent::orchestrator::OrchestratorConfig> for multi_agent::orchestrator::OrchestratorConfig}::eq]:
    Source: 'src/orchestrator.rs', lines 8:16-8:25 -/
def orchestrator.OrchestratorConfig.Insts.CoreCmpPartialEqOrchestratorConfig.eq
  (self : orchestrator.OrchestratorConfig)
  (other : orchestrator.OrchestratorConfig) :
  Result Bool
  := do
  if self.turn_budget = other.turn_budget
  then
    scheduler.SchedulerKind.Insts.CoreCmpPartialEqSchedulerKind.eq
      self.scheduler_kind other.scheduler_kind
  else ok false

/-- Trait implementation: [multi_agent::orchestrator::{core::cmp::PartialEq<multi_agent::orchestrator::OrchestratorConfig> for multi_agent::orchestrator::OrchestratorConfig}]
    Source: 'src/orchestrator.rs', lines 8:16-8:25 -/
@[reducible]
def orchestrator.OrchestratorConfig.Insts.CoreCmpPartialEqOrchestratorConfig :
  core.cmp.PartialEq orchestrator.OrchestratorConfig
  orchestrator.OrchestratorConfig := {
  eq :=
    orchestrator.OrchestratorConfig.Insts.CoreCmpPartialEqOrchestratorConfig.eq
  ne :=
    orchestrator.OrchestratorConfig.Insts.CoreCmpPartialEqOrchestratorConfig.ne
}

/-- [multi_agent::orchestrator::{core::cmp::Eq for multi_agent::orchestrator::OrchestratorConfig}::assert_receiver_is_total_eq]:
    Source: 'src/orchestrator.rs', lines 8:27-8:29 -/
def orchestrator.OrchestratorConfig.Insts.CoreCmpEq.assert_receiver_is_total_eq
  (self : orchestrator.OrchestratorConfig) : Result Unit := do
  ok ()

/-- Trait implementation: [multi_agent::orchestrator::{core::cmp::Eq for multi_agent::orchestrator::OrchestratorConfig}]
    Source: 'src/orchestrator.rs', lines 8:27-8:29 -/
@[reducible]
def orchestrator.OrchestratorConfig.Insts.CoreCmpEq : core.cmp.Eq
  orchestrator.OrchestratorConfig := {
  partialEqInst :=
    orchestrator.OrchestratorConfig.Insts.CoreCmpPartialEqOrchestratorConfig
  assert_receiver_is_total_eq :=
    orchestrator.OrchestratorConfig.Insts.CoreCmpEq.assert_receiver_is_total_eq
}

/-- [multi_agent::scheduler::{core::fmt::Debug for multi_agent::scheduler::SchedulerKind}::fmt]:
    Source: 'src/scheduler.rs', lines 4:31-4:36 -/
def scheduler.SchedulerKind.Insts.CoreFmtDebug.fmt
  (self : scheduler.SchedulerKind) (f : core.fmt.Formatter) :
  Result ((core.result.Result Unit core.fmt.Error) × core.fmt.Formatter)
  := do
  match self with
  | scheduler.SchedulerKind.RoundRobin =>
    core.fmt.Formatter.write_str f (toStr "RoundRobin")
  | scheduler.SchedulerKind.Priority =>
    core.fmt.Formatter.write_str f (toStr "Priority")

/-- Trait implementation: [multi_agent::scheduler::{core::fmt::Debug for multi_agent::scheduler::SchedulerKind}]
    Source: 'src/scheduler.rs', lines 4:31-4:36 -/
@[reducible]
def scheduler.SchedulerKind.Insts.CoreFmtDebug : core.fmt.Debug
  scheduler.SchedulerKind := {
  fmt := scheduler.SchedulerKind.Insts.CoreFmtDebug.fmt
}

/-- [multi_agent::orchestrator::{core::fmt::Debug for multi_agent::orchestrator::OrchestratorConfig}::fmt]:
    Source: 'src/orchestrator.rs', lines 8:31-8:36 -/
def orchestrator.OrchestratorConfig.Insts.CoreFmtDebug.fmt
  (self : orchestrator.OrchestratorConfig) (f : core.fmt.Formatter) :
  Result ((core.result.Result Unit core.fmt.Error) × core.fmt.Formatter)
  := do
  let dyn := Dyn.mk _ core.fmt.DebugU32 self.turn_budget
  let dyn1 :=
    Dyn.mk _ (core.fmt.DebugShared scheduler.SchedulerKind.Insts.CoreFmtDebug)
      self.scheduler_kind
  core.fmt.Formatter.debug_struct_field2_finish f (toStr "OrchestratorConfig")
    (toStr "turn_budget") dyn (toStr "scheduler_kind") dyn1

/-- Trait implementation: [multi_agent::orchestrator::{core::fmt::Debug for multi_agent::orchestrator::OrchestratorConfig}]
    Source: 'src/orchestrator.rs', lines 8:31-8:36 -/
@[reducible]
def orchestrator.OrchestratorConfig.Insts.CoreFmtDebug : core.fmt.Debug
  orchestrator.OrchestratorConfig := {
  fmt := orchestrator.OrchestratorConfig.Insts.CoreFmtDebug.fmt
}

/-- [multi_agent::scheduler::Scheduler]
    Source: 'src/scheduler.rs', lines 12:0-17:1 -/
structure scheduler.Scheduler where
  kind : scheduler.SchedulerKind
  agent_ids : alloc.vec.Vec Std.U32
  current_index : Std.U32
  turns_given : alloc.vec.Vec Std.U32

/-- [multi_agent::router::TopicSubscription]
    Source: 'src/router.rs', lines 5:0-8:1 -/
structure router.TopicSubscription where
  topic_id : Std.U32
  agent_id : Std.U32

/-- [multi_agent::router::Router]
    Source: 'src/router.rs', lines 12:0-14:1 -/
structure router.Router where
  subscriptions : alloc.vec.Vec router.TopicSubscription

/-- [multi_agent::orchestrator::OrchestratorState]
    Source: 'src/orchestrator.rs', lines 16:0-23:1 -/
structure orchestrator.OrchestratorState where
  agents : alloc.vec.Vec agent_trait.AgentInstance
  bus : message_bus.MessageBus
  router : router.Router
  scheduler : scheduler.Scheduler
  turn_count : Std.U32
  config : orchestrator.OrchestratorConfig

/-- [multi_agent::scheduler::{core::clone::Clone for multi_agent::scheduler::Scheduler}::clone]:
    Source: 'src/scheduler.rs', lines 11:9-11:14 -/
def scheduler.Scheduler.Insts.CoreCloneClone.clone
  (self : scheduler.Scheduler) : Result scheduler.Scheduler := do
  let sk ← scheduler.SchedulerKind.Insts.CoreCloneClone.clone self.kind
  let v ← alloc.vec.CloneVec.clone core.clone.CloneU32 self.agent_ids
  let i ← lift (core.clone.impls.CloneU32.clone self.current_index)
  let v1 ← alloc.vec.CloneVec.clone core.clone.CloneU32 self.turns_given
  ok { kind := sk, agent_ids := v, current_index := i, turns_given := v1 }

/-- [multi_agent::router::{core::clone::Clone for multi_agent::router::TopicSubscription}::clone]:
    Source: 'src/router.rs', lines 4:9-4:14 -/
def router.TopicSubscription.Insts.CoreCloneClone.clone
  (self : router.TopicSubscription) : Result router.TopicSubscription := do
  let i ← lift (core.clone.impls.CloneU32.clone self.topic_id)
  let i1 ← lift (core.clone.impls.CloneU32.clone self.agent_id)
  ok { topic_id := i, agent_id := i1 }

/-- Trait implementation: [multi_agent::router::{core::clone::Clone for multi_agent::router::TopicSubscription}]
    Source: 'src/router.rs', lines 4:9-4:14 -/
@[reducible]
def router.TopicSubscription.Insts.CoreCloneClone : core.clone.Clone
  router.TopicSubscription := {
  clone := router.TopicSubscription.Insts.CoreCloneClone.clone
}

/-- [multi_agent::router::{core::clone::Clone for multi_agent::router::Router}::clone]:
    Source: 'src/router.rs', lines 11:9-11:14 -/
def router.Router.Insts.CoreCloneClone.clone
  (self : router.Router) : Result router.Router := do
  let v ←
    alloc.vec.CloneVec.clone router.TopicSubscription.Insts.CoreCloneClone
      self.subscriptions
  ok { subscriptions := v }

/-- [multi_agent::orchestrator::{core::clone::Clone for multi_agent::orchestrator::OrchestratorState}::clone]:
    Source: 'src/orchestrator.rs', lines 15:9-15:14 -/
def orchestrator.OrchestratorState.Insts.CoreCloneClone.clone
  (self : orchestrator.OrchestratorState) :
  Result orchestrator.OrchestratorState
  := do
  let v ←
    alloc.vec.CloneVec.clone agent_trait.AgentInstance.Insts.CoreCloneClone
      self.agents
  let mb ← message_bus.MessageBus.Insts.CoreCloneClone.clone self.bus
  let r ← router.Router.Insts.CoreCloneClone.clone self.router
  let s ← scheduler.Scheduler.Insts.CoreCloneClone.clone self.scheduler
  let i ← lift (core.clone.impls.CloneU32.clone self.turn_count)
  let oc ←
    orchestrator.OrchestratorConfig.Insts.CoreCloneClone.clone self.config
  ok
    {
      agents := v,
      bus := mb,
      router := r,
      scheduler := s,
      turn_count := i,
      config := oc
    }

/-- Trait implementation: [multi_agent::orchestrator::{core::clone::Clone for multi_agent::orchestrator::OrchestratorState}]
    Source: 'src/orchestrator.rs', lines 15:9-15:14 -/
@[reducible]
def orchestrator.OrchestratorState.Insts.CoreCloneClone : core.clone.Clone
  orchestrator.OrchestratorState := {
  clone := orchestrator.OrchestratorState.Insts.CoreCloneClone.clone
}

/-- Trait implementation: [multi_agent::orchestrator::{core::marker::StructuralPartialEq for multi_agent::orchestrator::OrchestratorState}]
    Source: 'src/orchestrator.rs', lines 15:16-15:25 -/
@[reducible]
def orchestrator.OrchestratorState.Insts.CoreMarkerStructuralPartialEq :
  core.marker.StructuralPartialEq orchestrator.OrchestratorState := {
}

/-- [multi_agent::orchestrator::{core::cmp::PartialEq<multi_agent::orchestrator::OrchestratorState> for multi_agent::orchestrator::OrchestratorState}::ne]:
    Source: 'src/orchestrator.rs', lines 15:16-15:25 -/
axiom orchestrator.OrchestratorState.Insts.CoreCmpPartialEqOrchestratorState.ne
  :
  orchestrator.OrchestratorState → orchestrator.OrchestratorState → Result
    Bool

/-- [multi_agent::scheduler::{core::cmp::PartialEq<multi_agent::scheduler::Scheduler> for multi_agent::scheduler::Scheduler}::eq]:
    Source: 'src/scheduler.rs', lines 11:16-11:25 -/
def scheduler.Scheduler.Insts.CoreCmpPartialEqScheduler.eq
  (self : scheduler.Scheduler) (other : scheduler.Scheduler) :
  Result Bool
  := do
  if self.current_index = other.current_index
  then
    let b ←
      scheduler.SchedulerKind.Insts.CoreCmpPartialEqSchedulerKind.eq 
        self.kind other.kind
    if b
    then
      let b1 ←
        alloc.vec.partial_eq.PartialEqVec.eq core.cmp.PartialEqU32
          self.agent_ids other.agent_ids
      if b1
      then
        alloc.vec.partial_eq.PartialEqVec.eq core.cmp.PartialEqU32
          self.turns_given other.turns_given
      else ok false
    else ok false
  else ok false

/-- [multi_agent::router::{core::cmp::PartialEq<multi_agent::router::TopicSubscription> for multi_agent::router::TopicSubscription}::ne]:
    Source: 'src/router.rs', lines 4:16-4:25 -/
axiom router.TopicSubscription.Insts.CoreCmpPartialEqTopicSubscription.ne
  : router.TopicSubscription → router.TopicSubscription → Result Bool

/-- [multi_agent::router::{core::cmp::PartialEq<multi_agent::router::TopicSubscription> for multi_agent::router::TopicSubscription}::eq]:
    Source: 'src/router.rs', lines 4:16-4:25 -/
def router.TopicSubscription.Insts.CoreCmpPartialEqTopicSubscription.eq
  (self : router.TopicSubscription) (other : router.TopicSubscription) :
  Result Bool
  := do
  if self.topic_id = other.topic_id
  then ok (self.agent_id = other.agent_id)
  else ok false

/-- Trait implementation: [multi_agent::router::{core::cmp::PartialEq<multi_agent::router::TopicSubscription> for multi_agent::router::TopicSubscription}]
    Source: 'src/router.rs', lines 4:16-4:25 -/
@[reducible]
def router.TopicSubscription.Insts.CoreCmpPartialEqTopicSubscription :
  core.cmp.PartialEq router.TopicSubscription router.TopicSubscription := {
  eq := router.TopicSubscription.Insts.CoreCmpPartialEqTopicSubscription.eq
  ne := router.TopicSubscription.Insts.CoreCmpPartialEqTopicSubscription.ne
}

/-- [multi_agent::router::{core::cmp::PartialEq<multi_agent::router::Router> for multi_agent::router::Router}::eq]:
    Source: 'src/router.rs', lines 11:16-11:25 -/
def router.Router.Insts.CoreCmpPartialEqRouter.eq
  (self : router.Router) (other : router.Router) : Result Bool := do
  alloc.vec.partial_eq.PartialEqVec.eq
    router.TopicSubscription.Insts.CoreCmpPartialEqTopicSubscription
    self.subscriptions other.subscriptions

/-- [multi_agent::orchestrator::{core::cmp::PartialEq<multi_agent::orchestrator::OrchestratorState> for multi_agent::orchestrator::OrchestratorState}::eq]:
    Source: 'src/orchestrator.rs', lines 15:16-15:25 -/
def orchestrator.OrchestratorState.Insts.CoreCmpPartialEqOrchestratorState.eq
  (self : orchestrator.OrchestratorState)
  (other : orchestrator.OrchestratorState) :
  Result Bool
  := do
  if self.turn_count = other.turn_count
  then
    let b ←
      alloc.vec.partial_eq.PartialEqVec.eq
        agent_trait.AgentInstance.Insts.CoreCmpPartialEqAgentInstance
        self.agents other.agents
    if b
    then
      let b1 ←
        message_bus.MessageBus.Insts.CoreCmpPartialEqMessageBus.eq self.bus
          other.bus
      if b1
      then
        let b2 ←
          router.Router.Insts.CoreCmpPartialEqRouter.eq self.router
            other.router
        if b2
        then
          let b3 ←
            scheduler.Scheduler.Insts.CoreCmpPartialEqScheduler.eq
              self.scheduler other.scheduler
          if b3
          then
            orchestrator.OrchestratorConfig.Insts.CoreCmpPartialEqOrchestratorConfig.eq
              self.config other.config
          else ok false
        else ok false
      else ok false
    else ok false
  else ok false

/-- Trait implementation: [multi_agent::orchestrator::{core::cmp::PartialEq<multi_agent::orchestrator::OrchestratorState> for multi_agent::orchestrator::OrchestratorState}]
    Source: 'src/orchestrator.rs', lines 15:16-15:25 -/
@[reducible]
def orchestrator.OrchestratorState.Insts.CoreCmpPartialEqOrchestratorState :
  core.cmp.PartialEq orchestrator.OrchestratorState
  orchestrator.OrchestratorState := {
  eq :=
    orchestrator.OrchestratorState.Insts.CoreCmpPartialEqOrchestratorState.eq
  ne :=
    orchestrator.OrchestratorState.Insts.CoreCmpPartialEqOrchestratorState.ne
}

/-- [multi_agent::orchestrator::{core::cmp::Eq for multi_agent::orchestrator::OrchestratorState}::assert_receiver_is_total_eq]:
    Source: 'src/orchestrator.rs', lines 15:27-15:29 -/
def orchestrator.OrchestratorState.Insts.CoreCmpEq.assert_receiver_is_total_eq
  (self : orchestrator.OrchestratorState) : Result Unit := do
  ok ()

/-- Trait implementation: [multi_agent::orchestrator::{core::cmp::Eq for multi_agent::orchestrator::OrchestratorState}]
    Source: 'src/orchestrator.rs', lines 15:27-15:29 -/
@[reducible]
def orchestrator.OrchestratorState.Insts.CoreCmpEq : core.cmp.Eq
  orchestrator.OrchestratorState := {
  partialEqInst :=
    orchestrator.OrchestratorState.Insts.CoreCmpPartialEqOrchestratorState
  assert_receiver_is_total_eq :=
    orchestrator.OrchestratorState.Insts.CoreCmpEq.assert_receiver_is_total_eq
}

/-- [multi_agent::scheduler::{core::fmt::Debug for multi_agent::scheduler::Scheduler}::fmt]:
    Source: 'src/scheduler.rs', lines 11:31-11:36 -/
def scheduler.Scheduler.Insts.CoreFmtDebug.fmt
  (self : scheduler.Scheduler) (f : core.fmt.Formatter) :
  Result ((core.result.Result Unit core.fmt.Error) × core.fmt.Formatter)
  := do
  let dyn := Dyn.mk _ scheduler.SchedulerKind.Insts.CoreFmtDebug self.kind
  let dyn1 := Dyn.mk _ (core.fmt.DebugVec core.fmt.DebugU32) self.agent_ids
  let dyn2 := Dyn.mk _ core.fmt.DebugU32 self.current_index
  let dyn3 :=
    Dyn.mk _ (core.fmt.DebugShared (core.fmt.DebugVec core.fmt.DebugU32))
      self.turns_given
  core.fmt.Formatter.debug_struct_field4_finish f (toStr "Scheduler") (toStr
    "kind") dyn (toStr "agent_ids") dyn1 (toStr "current_index") dyn2 (toStr
    "turns_given") dyn3

/-- Trait implementation: [multi_agent::scheduler::{core::fmt::Debug for multi_agent::scheduler::Scheduler}]
    Source: 'src/scheduler.rs', lines 11:31-11:36 -/
@[reducible]
def scheduler.Scheduler.Insts.CoreFmtDebug : core.fmt.Debug scheduler.Scheduler
  := {
  fmt := scheduler.Scheduler.Insts.CoreFmtDebug.fmt
}

/-- [multi_agent::router::{core::fmt::Debug for multi_agent::router::TopicSubscription}::fmt]:
    Source: 'src/router.rs', lines 4:31-4:36 -/
def router.TopicSubscription.Insts.CoreFmtDebug.fmt
  (self : router.TopicSubscription) (f : core.fmt.Formatter) :
  Result ((core.result.Result Unit core.fmt.Error) × core.fmt.Formatter)
  := do
  let dyn := Dyn.mk _ core.fmt.DebugU32 self.topic_id
  let dyn1 := Dyn.mk _ (core.fmt.DebugShared core.fmt.DebugU32) self.agent_id
  core.fmt.Formatter.debug_struct_field2_finish f (toStr "TopicSubscription")
    (toStr "topic_id") dyn (toStr "agent_id") dyn1

/-- Trait implementation: [multi_agent::router::{core::fmt::Debug for multi_agent::router::TopicSubscription}]
    Source: 'src/router.rs', lines 4:31-4:36 -/
@[reducible]
def router.TopicSubscription.Insts.CoreFmtDebug : core.fmt.Debug
  router.TopicSubscription := {
  fmt := router.TopicSubscription.Insts.CoreFmtDebug.fmt
}

/-- [multi_agent::router::{core::fmt::Debug for multi_agent::router::Router}::fmt]:
    Source: 'src/router.rs', lines 11:31-11:36 -/
def router.Router.Insts.CoreFmtDebug.fmt
  (self : router.Router) (f : core.fmt.Formatter) :
  Result ((core.result.Result Unit core.fmt.Error) × core.fmt.Formatter)
  := do
  let dyn :=
    Dyn.mk _ (core.fmt.DebugShared (core.fmt.DebugVec
      router.TopicSubscription.Insts.CoreFmtDebug)) self.subscriptions
  core.fmt.Formatter.debug_struct_field1_finish f (toStr "Router") (toStr
    "subscriptions") dyn

/-- Trait implementation: [multi_agent::router::{core::fmt::Debug for multi_agent::router::Router}]
    Source: 'src/router.rs', lines 11:31-11:36 -/
@[reducible]
def router.Router.Insts.CoreFmtDebug : core.fmt.Debug router.Router := {
  fmt := router.Router.Insts.CoreFmtDebug.fmt
}

/-- [multi_agent::orchestrator::{core::fmt::Debug for multi_agent::orchestrator::OrchestratorState}::fmt]:
    Source: 'src/orchestrator.rs', lines 15:31-15:36 -/
def orchestrator.OrchestratorState.Insts.CoreFmtDebug.fmt
  (self : orchestrator.OrchestratorState) (f : core.fmt.Formatter) :
  Result ((core.result.Result Unit core.fmt.Error) × core.fmt.Formatter)
  := do
  let dyn :=
    Dyn.mk _ (core.fmt.DebugVec agent_trait.AgentInstance.Insts.CoreFmtDebug)
      self.agents
  let dyn1 := Dyn.mk _ message_bus.MessageBus.Insts.CoreFmtDebug self.bus
  let dyn2 := Dyn.mk _ router.Router.Insts.CoreFmtDebug self.router
  let dyn3 := Dyn.mk _ scheduler.Scheduler.Insts.CoreFmtDebug self.scheduler
  let dyn4 := Dyn.mk _ core.fmt.DebugU32 self.turn_count
  let dyn5 :=
    Dyn.mk _ (core.fmt.DebugShared
      orchestrator.OrchestratorConfig.Insts.CoreFmtDebug) self.config
  let values :=
    Array.to_slice (Array.make 6#usize [ dyn, dyn1, dyn2, dyn3, dyn4, dyn5 ])
  let s ←
    lift (Array.to_slice
      (Array.make 6#usize [
        toStr "agents", toStr "bus", toStr "router", toStr "scheduler", toStr
        "turn_count", toStr "config"
        ]))
  core.fmt.Formatter.debug_struct_fields_finish f (toStr "OrchestratorState") s
    values

/-- Trait implementation: [multi_agent::orchestrator::{core::fmt::Debug for multi_agent::orchestrator::OrchestratorState}]
    Source: 'src/orchestrator.rs', lines 15:31-15:36 -/
@[reducible]
def orchestrator.OrchestratorState.Insts.CoreFmtDebug : core.fmt.Debug
  orchestrator.OrchestratorState := {
  fmt := orchestrator.OrchestratorState.Insts.CoreFmtDebug.fmt
}

/-- [multi_agent::orchestrator::find_agent_index]: loop body 0:
    Source: 'src/orchestrator.rs', lines 28:4-34:1 -/
@[rust_loop_body]
def orchestrator.find_agent_index_loop.body
  (agents : Slice agent_trait.AgentInstance) (agent_id : Std.U32)
  (iter : core.ops.range.Range Std.Usize) :
  Result (ControlFlow (core.ops.range.Range Std.Usize) (Option Std.Usize))
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok (done none)
  | some i =>
    let ai ← Slice.index_usize agents i
    if ai.id = agent_id
    then ok (done o)
    else ok (cont iter1)

/-- [multi_agent::orchestrator::find_agent_index]: loop 0:
    Source: 'src/orchestrator.rs', lines 28:4-34:1 -/
@[rust_loop]
def orchestrator.find_agent_index_loop
  (iter : core.ops.range.Range Std.Usize)
  (agents : Slice agent_trait.AgentInstance) (agent_id : Std.U32) :
  Result (Option Std.Usize)
  := do
  loop
    (fun iter1 => orchestrator.find_agent_index_loop.body agents agent_id
      iter1)
    iter

/-- [multi_agent::orchestrator::find_agent_index]:
    Source: 'src/orchestrator.rs', lines 27:0-34:1 -/
def orchestrator.find_agent_index
  (agents : Slice agent_trait.AgentInstance) (agent_id : Std.U32) :
  Result (Option Std.Usize)
  := do
  let i := Slice.len agents
  orchestrator.find_agent_index_loop { start := 0#usize, «end» := i } agents
    agent_id

/-- [multi_agent::orchestrator::all_agent_ids::closure]
    Source: 'src/orchestrator.rs', lines 38:22-38:30 -/
@[reducible]
def orchestrator.all_agent_ids.closure := Unit

/-- [multi_agent::orchestrator::all_agent_ids::{core::ops::function::FnMut<(&'_ (multi_agent::agent_trait::AgentInstance)), u32> for multi_agent::orchestrator::all_agent_ids::closure}::call_mut]:
    Source: 'src/orchestrator.rs', lines 38:22-38:30 -/
def
  orchestrator.all_agent_ids.closure.Insts.CoreOpsFunctionFnMutTupleSharedAgentInstanceU32.call_mut
  (c : orchestrator.all_agent_ids.closure)
  (tupled_args : agent_trait.AgentInstance) :
  Result (Std.U32 × orchestrator.all_agent_ids.closure)
  := do
  ok (tupled_args.id, c)

/-- [multi_agent::orchestrator::all_agent_ids::{core::ops::function::FnOnce<(&'_ (multi_agent::agent_trait::AgentInstance)), u32> for multi_agent::orchestrator::all_agent_ids::closure}::call_once]:
    Source: 'src/orchestrator.rs', lines 38:22-38:30 -/
def
  orchestrator.all_agent_ids.closure.Insts.CoreOpsFunctionFnOnceTupleSharedAgentInstanceU32.call_once
  (c : orchestrator.all_agent_ids.closure) (ai : agent_trait.AgentInstance) :
  Result Std.U32
  := do
  let (i, _) ←
    orchestrator.all_agent_ids.closure.Insts.CoreOpsFunctionFnMutTupleSharedAgentInstanceU32.call_mut
      c ai
  ok i

/-- Trait implementation: [multi_agent::orchestrator::all_agent_ids::{core::ops::function::FnOnce<(&'_ (multi_agent::agent_trait::AgentInstance)), u32> for multi_agent::orchestrator::all_agent_ids::closure}]
    Source: 'src/orchestrator.rs', lines 38:22-38:30 -/
@[reducible]
def
  orchestrator.all_agent_ids.closure.Insts.CoreOpsFunctionFnOnceTupleSharedAgentInstanceU32
  : core.ops.function.FnOnce orchestrator.all_agent_ids.closure
  agent_trait.AgentInstance Std.U32 := {
  call_once :=
    orchestrator.all_agent_ids.closure.Insts.CoreOpsFunctionFnOnceTupleSharedAgentInstanceU32.call_once
}

/-- Trait implementation: [multi_agent::orchestrator::all_agent_ids::{core::ops::function::FnMut<(&'_ (multi_agent::agent_trait::AgentInstance)), u32> for multi_agent::orchestrator::all_agent_ids::closure}]
    Source: 'src/orchestrator.rs', lines 38:22-38:30 -/
@[reducible]
def
  orchestrator.all_agent_ids.closure.Insts.CoreOpsFunctionFnMutTupleSharedAgentInstanceU32
  : core.ops.function.FnMut orchestrator.all_agent_ids.closure
  agent_trait.AgentInstance Std.U32 := {
  FnOnceInst :=
    orchestrator.all_agent_ids.closure.Insts.CoreOpsFunctionFnOnceTupleSharedAgentInstanceU32
  call_mut :=
    orchestrator.all_agent_ids.closure.Insts.CoreOpsFunctionFnMutTupleSharedAgentInstanceU32.call_mut
}

/-- [multi_agent::orchestrator::all_agent_ids]:
    Source: 'src/orchestrator.rs', lines 37:0-39:1 -/
def orchestrator.all_agent_ids
  (agents : Slice agent_trait.AgentInstance) :
  Result (alloc.vec.Vec Std.U32)
  := do
  let i ← core.slice.Slice.iter agents
  let m ←
    core.slice.iter.Iter.Insts.CoreIterTraitsIteratorIteratorSharedAT.map
      orchestrator.all_agent_ids.closure.Insts.CoreOpsFunctionFnMutTupleSharedAgentInstanceU32
      i ()
  core.iter.adapters.map.Map.Insts.CoreIterTraitsIteratorIterator.collect
    (core.iter.traits.iterator.IteratorSliceIter agent_trait.AgentInstance)
    orchestrator.all_agent_ids.closure.Insts.CoreOpsFunctionFnMutTupleSharedAgentInstanceU32
    (core.iter.traits.collect.FromIteratorVec Std.U32) m

/-- [multi_agent::scheduler::next_agent]: loop body 0:
    Source: 'src/scheduler.rs', lines 63:12-68:13 -/
@[rust_loop_body]
def scheduler.next_agent_loop.body
  (v : alloc.vec.Vec Std.U32) (iter : core.ops.range.Range Std.Usize)
  (best_idx : Std.Usize) (best_turns : Std.U32) :
  Result (ControlFlow ((core.ops.range.Range Std.Usize) × Std.Usize ×
    Std.U32) Std.Usize)
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok (done best_idx)
  | some i =>
    let i1 ←
      alloc.vec.Vec.index (core.slice.index.SliceIndexUsizeSlice Std.U32) v i
    if i1 < best_turns
    then ok (cont (iter1, i, i1))
    else ok (cont (iter1, best_idx, best_turns))

/-- [multi_agent::scheduler::next_agent]: loop 0:
    Source: 'src/scheduler.rs', lines 63:12-68:13 -/
@[rust_loop]
def scheduler.next_agent_loop
  (iter : core.ops.range.Range Std.Usize) (v : alloc.vec.Vec Std.U32)
  (best_idx : Std.Usize) (best_turns : Std.U32) :
  Result Std.Usize
  := do
  loop
    (fun (iter1, best_idx1, best_turns1) => scheduler.next_agent_loop.body v
      iter1 best_idx1 best_turns1)
    (iter, best_idx, best_turns)

/-- [multi_agent::scheduler::next_agent]:
    Source: 'src/scheduler.rs', lines 43:0-74:1 -/
def scheduler.next_agent
  (sched : scheduler.Scheduler) :
  Result ((Option Std.U32) × scheduler.Scheduler)
  := do
  let b ← alloc.vec.Vec.is_empty Global sched.agent_ids
  if b
  then ok (none, sched)
  else
    match sched.kind with
    | scheduler.SchedulerKind.RoundRobin =>
      let idx ← lift (UScalar.cast .Usize sched.current_index)
      let agent_id ←
        alloc.vec.Vec.index (core.slice.index.SliceIndexUsizeSlice Std.U32)
          sched.agent_ids idx
      let (i, index_mut_back) ←
        alloc.vec.Vec.index_mut (core.slice.index.SliceIndexUsizeSlice Std.U32)
          sched.turns_given idx
      let i1 ← i + 1#u32
      let i2 ← sched.current_index + 1#u32
      let i3 ← lift (UScalar.cast .Usize i2)
      let i4 := alloc.vec.Vec.len sched.agent_ids
      let i5 ← i3 % i4
      let i6 ← lift (UScalar.cast .U32 i5)
      let v := index_mut_back i1
      ok (some agent_id, { sched with current_index := i6, turns_given := v })
    | scheduler.SchedulerKind.Priority =>
      let best_turns ←
        alloc.vec.Vec.index (core.slice.index.SliceIndexUsizeSlice Std.U32)
          sched.turns_given 0#usize
      let i := alloc.vec.Vec.len sched.agent_ids
      let best_idx ←
        scheduler.next_agent_loop { start := 1#usize, «end» := i }
          sched.turns_given 0#usize best_turns
      let agent_id ←
        alloc.vec.Vec.index (core.slice.index.SliceIndexUsizeSlice Std.U32)
          sched.agent_ids best_idx
      let (i1, index_mut_back) ←
        alloc.vec.Vec.index_mut (core.slice.index.SliceIndexUsizeSlice Std.U32)
          sched.turns_given best_idx
      let i2 ← i1 + 1#u32
      let v := index_mut_back i2
      ok (some agent_id, { sched with turns_given := v })

/-- [multi_agent::router::resolve_recipient]:
    Source: 'src/router.rs', lines 35:0-53:1 -/
def router.resolve_recipient
  (router : router.Router) (recipient : agent_trait.Recipient)
  (all_agent_ids : Slice Std.U32) :
  Result (alloc.vec.Vec Std.U32)
  := do
  sorry

/-- [multi_agent::orchestrator::orchestrator_step]: loop body 1:
    Source: 'src/orchestrator.rs', lines 78:8-86:9 -/
@[rust_loop_body]
def orchestrator.orchestrator_step_loop0_loop0.body
  (i : Std.U32) (m : agent_trait.Message)
  (iter : alloc.vec.into_iter.IntoIter Std.U32) (mb : message_bus.MessageBus) :
  Result (ControlFlow ((alloc.vec.into_iter.IntoIter Std.U32) ×
    message_bus.MessageBus) message_bus.MessageBus)
  := do
  let (o, iter1) ← alloc.vec.into_iter.IteratorIntoIter.next iter
  match o with
  | none => ok (done mb)
  | some target_id =>
    let m1 ← agent_trait.Message.Insts.CoreCloneClone.clone m
    let mb1 ←
      message_bus.bus_send mb
        {
          sender := i,
          recipient := (agent_trait.Recipient.Direct target_id),
          message := m1,
          sequence_num := 0#u32
        }
    ok (cont (iter1, mb1))

/-- [multi_agent::orchestrator::orchestrator_step]: loop 1:
    Source: 'src/orchestrator.rs', lines 78:8-86:9 -/
@[rust_loop]
def orchestrator.orchestrator_step_loop0_loop0
  (iter : alloc.vec.into_iter.IntoIter Std.U32) (mb : message_bus.MessageBus)
  (i : Std.U32) (m : agent_trait.Message) :
  Result message_bus.MessageBus
  := do
  loop
    (fun (iter1, mb1) => orchestrator.orchestrator_step_loop0_loop0.body i m
      iter1 mb1)
    (iter, mb)

/-- [multi_agent::orchestrator::orchestrator_step]: loop body 0:
    Source: 'src/orchestrator.rs', lines 75:4-87:5 -/
@[rust_loop_body]
def orchestrator.orchestrator_step_loop0.body
  (r : router.Router) (ids : alloc.vec.Vec Std.U32)
  (iter : alloc.vec.into_iter.IntoIter agent_trait.Envelope)
  (mb : message_bus.MessageBus) :
  Result (ControlFlow ((alloc.vec.into_iter.IntoIter agent_trait.Envelope) ×
    message_bus.MessageBus) message_bus.MessageBus)
  := do
  let (o, iter1) ← alloc.vec.into_iter.IteratorIntoIter.next iter
  match o with
  | none => ok (done mb)
  | some env =>
    let s := alloc.vec.Vec.deref ids
    let targets ← router.resolve_recipient r env.recipient s
    let iter2 ← alloc.vec.IntoIteratorVec.into_iter targets
    let mb1 ←
      orchestrator.orchestrator_step_loop0_loop0 iter2 mb env.sender
        env.message
    ok (cont (iter1, mb1))

/-- [multi_agent::orchestrator::orchestrator_step]: loop 0:
    Source: 'src/orchestrator.rs', lines 75:4-87:5 -/
@[rust_loop]
def orchestrator.orchestrator_step_loop0
  (iter : alloc.vec.into_iter.IntoIter agent_trait.Envelope)
  (mb : message_bus.MessageBus) (r : router.Router)
  (ids : alloc.vec.Vec Std.U32) :
  Result message_bus.MessageBus
  := do
  loop
    (fun (iter1, mb1) => orchestrator.orchestrator_step_loop0.body r ids iter1
      mb1)
    (iter, mb)

/-- [multi_agent::orchestrator::orchestrator_step]:
    Source: 'src/orchestrator.rs', lines 47:0-90:1 -/
def orchestrator.orchestrator_step
  (state : orchestrator.OrchestratorState) :
  Result orchestrator.OrchestratorState
  := do
  let (o, s) ← scheduler.next_agent state.scheduler
  match o with
  | none => ok { state with scheduler := s }
  | some id =>
    let s1 := alloc.vec.Vec.deref state.agents
    let o1 ← orchestrator.find_agent_index s1 id
    match o1 with
    | none => ok { state with scheduler := s }
    | some idx =>
      let ai ←
        alloc.vec.Vec.index (core.slice.index.SliceIndexUsizeSlice
          agent_trait.AgentInstance) state.agents idx
      let b ← agents.is_agent_active ai
      if b
      then
        let (delivered, mb) ← message_bus.bus_deliver state.bus id
        let (ai1, index_mut_back) ←
          alloc.vec.Vec.index_mut (core.slice.index.SliceIndexUsizeSlice
            agent_trait.AgentInstance) state.agents idx
        let v ←
          alloc.vec.Vec.Insts.CoreIterTraitsCollectExtend.extend Global
            (core.iter.traits.collect.IntoIteratorVec agent_trait.Envelope)
            ai1.inbox delivered
        let v1 := index_mut_back { ai1 with inbox := v }
        let (ai2, index_mut_back1) ←
          alloc.vec.Vec.index_mut (core.slice.index.SliceIndexUsizeSlice
            agent_trait.AgentInstance) v1 idx
        let ai3 ← agents.agent_process ai2
        let v2 := index_mut_back1 ai3
        let (ai4, index_mut_back2) ←
          alloc.vec.Vec.index_mut (core.slice.index.SliceIndexUsizeSlice
            agent_trait.AgentInstance) v2 idx
        let (outbox, v3) ←
          core.mem.take (alloc.vec.Vec.Insts.CoreDefaultDefault
            agent_trait.Envelope) ai4.outbox
        let v4 := index_mut_back2 { ai4 with outbox := v3 }
        let s2 := alloc.vec.Vec.deref v4
        let ids ← orchestrator.all_agent_ids s2
        let iter ← alloc.vec.IntoIteratorVec.into_iter outbox
        let mb1 ←
          orchestrator.orchestrator_step_loop0 iter mb state.router ids
        let i ← state.turn_count + 1#u32
        ok
          {
            state
              with
              agents := v4, bus := mb1, scheduler := s, turn_count := i
          }
      else
        let i ← state.turn_count + 1#u32
        ok { state with scheduler := s, turn_count := i }

/-- [multi_agent::orchestrator::has_active_agents]: loop body 0:
    Source: 'src/orchestrator.rs', lines 104:4-110:1 -/
@[rust_loop_body]
def orchestrator.has_active_agents_loop.body
  (iter : core.slice.iter.Iter agent_trait.AgentInstance) :
  Result (ControlFlow (core.slice.iter.Iter agent_trait.AgentInstance) Bool)
  := do
  let (o, iter1) ← core.slice.iter.IteratorSliceIter.next iter
  match o with
  | none => ok (done false)
  | some agent =>
    let b ← agents.is_agent_active agent
    if b
    then ok (done true)
    else ok (cont iter1)

/-- [multi_agent::orchestrator::has_active_agents]: loop 0:
    Source: 'src/orchestrator.rs', lines 104:4-110:1 -/
@[rust_loop]
def orchestrator.has_active_agents_loop
  (iter : core.slice.iter.Iter agent_trait.AgentInstance) : Result Bool := do
  loop
    (fun iter1 => orchestrator.has_active_agents_loop.body iter1)
    iter

/-- [multi_agent::orchestrator::has_active_agents]:
    Source: 'src/orchestrator.rs', lines 103:0-110:1 -/
def orchestrator.has_active_agents
  (state : orchestrator.OrchestratorState) : Result Bool := do
  let iter ←
    SharedAVec.Insts.CoreIterTraitsCollectIntoIteratorSharedATIter.into_iter
      Global state.agents
  orchestrator.has_active_agents_loop iter

/-- [multi_agent::orchestrator::orchestrator_run]: loop body 0:
    Source: 'src/orchestrator.rs', lines 94:4-99:5 -/
@[rust_loop_body]
def orchestrator.orchestrator_run_loop.body
  (state : orchestrator.OrchestratorState) :
  Result (ControlFlow orchestrator.OrchestratorState ((alloc.vec.Vec
    agent_trait.AgentInstance) × message_bus.MessageBus × router.Router ×
    scheduler.Scheduler × Std.U32 × Std.U32 × scheduler.SchedulerKind))
  := do
  if state.turn_count < state.config.turn_budget
  then
    let b ← orchestrator.has_active_agents state
    if b
    then let state1 ← orchestrator.orchestrator_step state
         ok (cont state1)
    else
      let b1 ← message_bus.bus_is_empty state.bus
      if b1
      then
        ok (done (state.agents, state.bus, state.router, state.scheduler,
          state.turn_count, state.config.turn_budget,
          state.config.scheduler_kind))
      else let state1 ← orchestrator.orchestrator_step state
           ok (cont state1)
  else
    ok (done (state.agents, state.bus, state.router, state.scheduler,
      state.turn_count, state.config.turn_budget, state.config.scheduler_kind))

/-- [multi_agent::orchestrator::orchestrator_run]: loop 0:
    Source: 'src/orchestrator.rs', lines 94:4-99:5 -/
@[rust_loop]
def orchestrator.orchestrator_run_loop
  (state : orchestrator.OrchestratorState) :
  Result ((alloc.vec.Vec agent_trait.AgentInstance) × message_bus.MessageBus
    × router.Router × scheduler.Scheduler × Std.U32 × Std.U32 ×
    scheduler.SchedulerKind)
  := do
  loop
    (fun state1 => orchestrator.orchestrator_run_loop.body state1)
    state

/-- [multi_agent::orchestrator::orchestrator_run]:
    Source: 'src/orchestrator.rs', lines 93:0-100:1 -/
def orchestrator.orchestrator_run
  (state : orchestrator.OrchestratorState) :
  Result orchestrator.OrchestratorState
  := do
  let (v, mb, r, s, i, i1, sk) ← orchestrator.orchestrator_run_loop state
  ok
    {
      agents := v,
      bus := mb,
      router := r,
      scheduler := s,
      turn_count := i,
      config := { turn_budget := i1, scheduler_kind := sk }
    }

/-- [multi_agent::orchestrator::finished_agent_count::closure]
    Source: 'src/orchestrator.rs', lines 117:16-117:51 -/
@[reducible]
def orchestrator.finished_agent_count.closure := Unit

/-- [multi_agent::orchestrator::finished_agent_count::{core::ops::function::FnMut<(&'_ (&'_ (multi_agent::agent_trait::AgentInstance))), bool> for multi_agent::orchestrator::finished_agent_count::closure}::call_mut]:
    Source: 'src/orchestrator.rs', lines 117:16-117:51 -/
def
  orchestrator.finished_agent_count.closure.Insts.CoreOpsFunctionFnMutTupleSharedSharedAgentInstanceBool.call_mut
  (c : orchestrator.finished_agent_count.closure)
  (tupled_args : agent_trait.AgentInstance) :
  Result (Bool × orchestrator.finished_agent_count.closure)
  := do
  let b ←
    agent_trait.AgentState.Insts.CoreCmpPartialEqAgentState.eq
      tupled_args.state agent_trait.AgentState.Finished
  ok (b, c)

/-- [multi_agent::orchestrator::finished_agent_count::{core::ops::function::FnOnce<(&'_ (&'_ (multi_agent::agent_trait::AgentInstance))), bool> for multi_agent::orchestrator::finished_agent_count::closure}::call_once]:
    Source: 'src/orchestrator.rs', lines 117:16-117:51 -/
def
  orchestrator.finished_agent_count.closure.Insts.CoreOpsFunctionFnOnceTupleSharedSharedAgentInstanceBool.call_once
  (c : orchestrator.finished_agent_count.closure)
  (ai : agent_trait.AgentInstance) :
  Result Bool
  := do
  let (b, _) ←
    orchestrator.finished_agent_count.closure.Insts.CoreOpsFunctionFnMutTupleSharedSharedAgentInstanceBool.call_mut
      c ai
  ok b

/-- Trait implementation: [multi_agent::orchestrator::finished_agent_count::{core::ops::function::FnOnce<(&'_ (&'_ (multi_agent::agent_trait::AgentInstance))), bool> for multi_agent::orchestrator::finished_agent_count::closure}]
    Source: 'src/orchestrator.rs', lines 117:16-117:51 -/
@[reducible]
def
  orchestrator.finished_agent_count.closure.Insts.CoreOpsFunctionFnOnceTupleSharedSharedAgentInstanceBool
  : core.ops.function.FnOnce orchestrator.finished_agent_count.closure
  agent_trait.AgentInstance Bool := {
  call_once :=
    orchestrator.finished_agent_count.closure.Insts.CoreOpsFunctionFnOnceTupleSharedSharedAgentInstanceBool.call_once
}

/-- Trait implementation: [multi_agent::orchestrator::finished_agent_count::{core::ops::function::FnMut<(&'_ (&'_ (multi_agent::agent_trait::AgentInstance))), bool> for multi_agent::orchestrator::finished_agent_count::closure}]
    Source: 'src/orchestrator.rs', lines 117:16-117:51 -/
@[reducible]
def
  orchestrator.finished_agent_count.closure.Insts.CoreOpsFunctionFnMutTupleSharedSharedAgentInstanceBool
  : core.ops.function.FnMut orchestrator.finished_agent_count.closure
  agent_trait.AgentInstance Bool := {
  FnOnceInst :=
    orchestrator.finished_agent_count.closure.Insts.CoreOpsFunctionFnOnceTupleSharedSharedAgentInstanceBool
  call_mut :=
    orchestrator.finished_agent_count.closure.Insts.CoreOpsFunctionFnMutTupleSharedSharedAgentInstanceBool.call_mut
}

/-- [multi_agent::orchestrator::finished_agent_count]:
    Source: 'src/orchestrator.rs', lines 113:0-119:1 -/
def orchestrator.finished_agent_count
  (state : orchestrator.OrchestratorState) : Result Std.U32 := do
  sorry

/-- [multi_agent::protocols::debate::DebateState]
    Source: 'src/protocols/debate.rs', lines 5:0-10:1 -/
structure protocols.debate.DebateState where
  topic_id : Std.U32
  rounds_remaining : Std.U32
  max_rounds : Std.U32
  arguments : alloc.vec.Vec (Std.U32 × Std.U32)

/-- [multi_agent::protocols::debate::{core::clone::Clone for multi_agent::protocols::debate::DebateState}::clone]:
    Source: 'src/protocols/debate.rs', lines 4:9-4:14 -/
def protocols.debate.DebateState.Insts.CoreCloneClone.clone
  (self : protocols.debate.DebateState) :
  Result protocols.debate.DebateState
  := do
  let i ← lift (core.clone.impls.CloneU32.clone self.topic_id)
  let i1 ← lift (core.clone.impls.CloneU32.clone self.rounds_remaining)
  let i2 ← lift (core.clone.impls.CloneU32.clone self.max_rounds)
  let v ←
    alloc.vec.CloneVec.clone (BuiltinClone (Std.U32 × Std.U32)) self.arguments
  ok
    { topic_id := i, rounds_remaining := i1, max_rounds := i2, arguments := v }

/-- Trait implementation: [multi_agent::protocols::debate::{core::clone::Clone for multi_agent::protocols::debate::DebateState}]
    Source: 'src/protocols/debate.rs', lines 4:9-4:14 -/
@[reducible]
def protocols.debate.DebateState.Insts.CoreCloneClone : core.clone.Clone
  protocols.debate.DebateState := {
  clone := protocols.debate.DebateState.Insts.CoreCloneClone.clone
}

/-- Trait implementation: [multi_agent::protocols::debate::{core::marker::StructuralPartialEq for multi_agent::protocols::debate::DebateState}]
    Source: 'src/protocols/debate.rs', lines 4:16-4:25 -/
@[reducible]
def protocols.debate.DebateState.Insts.CoreMarkerStructuralPartialEq :
  core.marker.StructuralPartialEq protocols.debate.DebateState := {
}

/-- [multi_agent::protocols::debate::{core::cmp::PartialEq<multi_agent::protocols::debate::DebateState> for multi_agent::protocols::debate::DebateState}::ne]:
    Source: 'src/protocols/debate.rs', lines 4:16-4:25 -/
axiom protocols.debate.DebateState.Insts.CoreCmpPartialEqDebateState.ne
  :
  protocols.debate.DebateState → protocols.debate.DebateState → Result Bool

/-- [multi_agent::protocols::debate::{core::cmp::PartialEq<multi_agent::protocols::debate::DebateState> for multi_agent::protocols::debate::DebateState}::eq]:
    Source: 'src/protocols/debate.rs', lines 4:16-4:25 -/
def protocols.debate.DebateState.Insts.CoreCmpPartialEqDebateState.eq
  (self : protocols.debate.DebateState) (other : protocols.debate.DebateState)
  :
  Result Bool
  := do
  if self.topic_id = other.topic_id
  then
    if self.rounds_remaining = other.rounds_remaining
    then
      if self.max_rounds = other.max_rounds
      then
        alloc.vec.partial_eq.PartialEqVec.eq (Pair.Insts.CoreCmpPartialEqPair
          core.cmp.PartialEqU32 core.cmp.PartialEqU32) self.arguments
          other.arguments
      else ok false
    else ok false
  else ok false

/-- Trait implementation: [multi_agent::protocols::debate::{core::cmp::PartialEq<multi_agent::protocols::debate::DebateState> for multi_agent::protocols::debate::DebateState}]
    Source: 'src/protocols/debate.rs', lines 4:16-4:25 -/
@[reducible]
def protocols.debate.DebateState.Insts.CoreCmpPartialEqDebateState :
  core.cmp.PartialEq protocols.debate.DebateState protocols.debate.DebateState
  := {
  eq := protocols.debate.DebateState.Insts.CoreCmpPartialEqDebateState.eq
  ne := protocols.debate.DebateState.Insts.CoreCmpPartialEqDebateState.ne
}

/-- [multi_agent::protocols::debate::{core::cmp::Eq for multi_agent::protocols::debate::DebateState}::assert_receiver_is_total_eq]:
    Source: 'src/protocols/debate.rs', lines 4:27-4:29 -/
def protocols.debate.DebateState.Insts.CoreCmpEq.assert_receiver_is_total_eq
  (self : protocols.debate.DebateState) : Result Unit := do
  ok ()

/-- Trait implementation: [multi_agent::protocols::debate::{core::cmp::Eq for multi_agent::protocols::debate::DebateState}]
    Source: 'src/protocols/debate.rs', lines 4:27-4:29 -/
@[reducible]
def protocols.debate.DebateState.Insts.CoreCmpEq : core.cmp.Eq
  protocols.debate.DebateState := {
  partialEqInst :=
    protocols.debate.DebateState.Insts.CoreCmpPartialEqDebateState
  assert_receiver_is_total_eq :=
    protocols.debate.DebateState.Insts.CoreCmpEq.assert_receiver_is_total_eq
}

/-- [multi_agent::protocols::debate::{core::fmt::Debug for multi_agent::protocols::debate::DebateState}::fmt]:
    Source: 'src/protocols/debate.rs', lines 4:31-4:36 -/
def protocols.debate.DebateState.Insts.CoreFmtDebug.fmt
  (self : protocols.debate.DebateState) (f : core.fmt.Formatter) :
  Result ((core.result.Result Unit core.fmt.Error) × core.fmt.Formatter)
  := do
  let dyn := Dyn.mk _ core.fmt.DebugU32 self.topic_id
  let dyn1 := Dyn.mk _ core.fmt.DebugU32 self.rounds_remaining
  let dyn2 := Dyn.mk _ core.fmt.DebugU32 self.max_rounds
  let dyn3 :=
    Dyn.mk _ (core.fmt.DebugShared (core.fmt.DebugVec (Pair.Insts.CoreFmtDebug
      core.fmt.DebugU32 core.fmt.DebugU32))) self.arguments
  core.fmt.Formatter.debug_struct_field4_finish f (toStr "DebateState") (toStr
    "topic_id") dyn (toStr "rounds_remaining") dyn1 (toStr "max_rounds") dyn2
    (toStr "arguments") dyn3

/-- Trait implementation: [multi_agent::protocols::debate::{core::fmt::Debug for multi_agent::protocols::debate::DebateState}]
    Source: 'src/protocols/debate.rs', lines 4:31-4:36 -/
@[reducible]
def protocols.debate.DebateState.Insts.CoreFmtDebug : core.fmt.Debug
  protocols.debate.DebateState := {
  fmt := protocols.debate.DebateState.Insts.CoreFmtDebug.fmt
}

/-- [multi_agent::protocols::debate::debate_new]:
    Source: 'src/protocols/debate.rs', lines 13:0-20:1 -/
def protocols.debate.debate_new
  (topic_id : Std.U32) (max_rounds : Std.U32) :
  Result protocols.debate.DebateState
  := do
  ok
    {
      topic_id,
      rounds_remaining := max_rounds,
      max_rounds,
      arguments := (alloc.vec.Vec.new (Std.U32 × Std.U32))
    }

/-- [multi_agent::protocols::debate::debate_step]:
    Source: 'src/protocols/debate.rs', lines 24:0-30:1 -/
def protocols.debate.debate_step
  (state : protocols.debate.DebateState) (agent_id : Std.U32)
  (argument_id : Std.U32) :
  Result protocols.debate.DebateState
  := do
  if state.rounds_remaining = 0#u32
  then ok state
  else
    let v ← alloc.vec.Vec.push state.arguments (agent_id, argument_id)
    let i ← state.rounds_remaining - 1#u32
    ok { state with rounds_remaining := i, arguments := v }

/-- [multi_agent::protocols::debate::debate_is_finished]:
    Source: 'src/protocols/debate.rs', lines 33:0-35:1 -/
def protocols.debate.debate_is_finished
  (state : protocols.debate.DebateState) : Result Bool := do
  ok (state.rounds_remaining = 0#u32)

/-- [multi_agent::protocols::debate::debate_argument_count]:
    Source: 'src/protocols/debate.rs', lines 38:0-40:1 -/
def protocols.debate.debate_argument_count
  (state : protocols.debate.DebateState) : Result Std.U32 := do
  let i := alloc.vec.Vec.len state.arguments
  ok (UScalar.cast .U32 i)

/-- [multi_agent::protocols::pipeline::PipelineStage]
    Source: 'src/protocols/pipeline.rs', lines 5:0-9:1 -/
structure protocols.pipeline.PipelineStage where
  agent_id : Std.U32
  input_type : Std.U32
  output_type : Std.U32

/-- [multi_agent::protocols::pipeline::{core::clone::Clone for multi_agent::protocols::pipeline::PipelineStage}::clone]:
    Source: 'src/protocols/pipeline.rs', lines 4:9-4:14 -/
def protocols.pipeline.PipelineStage.Insts.CoreCloneClone.clone
  (self : protocols.pipeline.PipelineStage) :
  Result protocols.pipeline.PipelineStage
  := do
  let i ← lift (core.clone.impls.CloneU32.clone self.agent_id)
  let i1 ← lift (core.clone.impls.CloneU32.clone self.input_type)
  let i2 ← lift (core.clone.impls.CloneU32.clone self.output_type)
  ok { agent_id := i, input_type := i1, output_type := i2 }

/-- Trait implementation: [multi_agent::protocols::pipeline::{core::clone::Clone for multi_agent::protocols::pipeline::PipelineStage}]
    Source: 'src/protocols/pipeline.rs', lines 4:9-4:14 -/
@[reducible]
def protocols.pipeline.PipelineStage.Insts.CoreCloneClone : core.clone.Clone
  protocols.pipeline.PipelineStage := {
  clone := protocols.pipeline.PipelineStage.Insts.CoreCloneClone.clone
}

/-- Trait implementation: [multi_agent::protocols::pipeline::{core::marker::StructuralPartialEq for multi_agent::protocols::pipeline::PipelineStage}]
    Source: 'src/protocols/pipeline.rs', lines 4:16-4:25 -/
@[reducible]
def protocols.pipeline.PipelineStage.Insts.CoreMarkerStructuralPartialEq :
  core.marker.StructuralPartialEq protocols.pipeline.PipelineStage := {
}

/-- [multi_agent::protocols::pipeline::{core::cmp::PartialEq<multi_agent::protocols::pipeline::PipelineStage> for multi_agent::protocols::pipeline::PipelineStage}::ne]:
    Source: 'src/protocols/pipeline.rs', lines 4:16-4:25 -/
axiom protocols.pipeline.PipelineStage.Insts.CoreCmpPartialEqPipelineStage.ne
  :
  protocols.pipeline.PipelineStage → protocols.pipeline.PipelineStage →
    Result Bool

/-- [multi_agent::protocols::pipeline::{core::cmp::PartialEq<multi_agent::protocols::pipeline::PipelineStage> for multi_agent::protocols::pipeline::PipelineStage}::eq]:
    Source: 'src/protocols/pipeline.rs', lines 4:16-4:25 -/
def protocols.pipeline.PipelineStage.Insts.CoreCmpPartialEqPipelineStage.eq
  (self : protocols.pipeline.PipelineStage)
  (other : protocols.pipeline.PipelineStage) :
  Result Bool
  := do
  if self.input_type = other.input_type
  then
    if self.output_type = other.output_type
    then ok (self.agent_id = other.agent_id)
    else ok false
  else ok false

/-- Trait implementation: [multi_agent::protocols::pipeline::{core::cmp::PartialEq<multi_agent::protocols::pipeline::PipelineStage> for multi_agent::protocols::pipeline::PipelineStage}]
    Source: 'src/protocols/pipeline.rs', lines 4:16-4:25 -/
@[reducible]
def protocols.pipeline.PipelineStage.Insts.CoreCmpPartialEqPipelineStage :
  core.cmp.PartialEq protocols.pipeline.PipelineStage
  protocols.pipeline.PipelineStage := {
  eq := protocols.pipeline.PipelineStage.Insts.CoreCmpPartialEqPipelineStage.eq
  ne := protocols.pipeline.PipelineStage.Insts.CoreCmpPartialEqPipelineStage.ne
}

/-- [multi_agent::protocols::pipeline::{core::cmp::Eq for multi_agent::protocols::pipeline::PipelineStage}::assert_receiver_is_total_eq]:
    Source: 'src/protocols/pipeline.rs', lines 4:27-4:29 -/
def
  protocols.pipeline.PipelineStage.Insts.CoreCmpEq.assert_receiver_is_total_eq
  (self : protocols.pipeline.PipelineStage) : Result Unit := do
  ok ()

/-- Trait implementation: [multi_agent::protocols::pipeline::{core::cmp::Eq for multi_agent::protocols::pipeline::PipelineStage}]
    Source: 'src/protocols/pipeline.rs', lines 4:27-4:29 -/
@[reducible]
def protocols.pipeline.PipelineStage.Insts.CoreCmpEq : core.cmp.Eq
  protocols.pipeline.PipelineStage := {
  partialEqInst :=
    protocols.pipeline.PipelineStage.Insts.CoreCmpPartialEqPipelineStage
  assert_receiver_is_total_eq :=
    protocols.pipeline.PipelineStage.Insts.CoreCmpEq.assert_receiver_is_total_eq
}

/-- [multi_agent::protocols::pipeline::{core::fmt::Debug for multi_agent::protocols::pipeline::PipelineStage}::fmt]:
    Source: 'src/protocols/pipeline.rs', lines 4:31-4:36 -/
def protocols.pipeline.PipelineStage.Insts.CoreFmtDebug.fmt
  (self : protocols.pipeline.PipelineStage) (f : core.fmt.Formatter) :
  Result ((core.result.Result Unit core.fmt.Error) × core.fmt.Formatter)
  := do
  let dyn := Dyn.mk _ core.fmt.DebugU32 self.agent_id
  let dyn1 := Dyn.mk _ core.fmt.DebugU32 self.input_type
  let dyn2 :=
    Dyn.mk _ (core.fmt.DebugShared core.fmt.DebugU32) self.output_type
  core.fmt.Formatter.debug_struct_field3_finish f (toStr "PipelineStage")
    (toStr "agent_id") dyn (toStr "input_type") dyn1 (toStr "output_type") dyn2

/-- Trait implementation: [multi_agent::protocols::pipeline::{core::fmt::Debug for multi_agent::protocols::pipeline::PipelineStage}]
    Source: 'src/protocols/pipeline.rs', lines 4:31-4:36 -/
@[reducible]
def protocols.pipeline.PipelineStage.Insts.CoreFmtDebug : core.fmt.Debug
  protocols.pipeline.PipelineStage := {
  fmt := protocols.pipeline.PipelineStage.Insts.CoreFmtDebug.fmt
}

/-- [multi_agent::protocols::pipeline::Pipeline]
    Source: 'src/protocols/pipeline.rs', lines 13:0-15:1 -/
structure protocols.pipeline.Pipeline where
  stages : alloc.vec.Vec protocols.pipeline.PipelineStage

/-- [multi_agent::protocols::pipeline::{core::clone::Clone for multi_agent::protocols::pipeline::Pipeline}::clone]:
    Source: 'src/protocols/pipeline.rs', lines 12:9-12:14 -/
def protocols.pipeline.Pipeline.Insts.CoreCloneClone.clone
  (self : protocols.pipeline.Pipeline) :
  Result protocols.pipeline.Pipeline
  := do
  let v ←
    alloc.vec.CloneVec.clone
      protocols.pipeline.PipelineStage.Insts.CoreCloneClone self.stages
  ok { stages := v }

/-- Trait implementation: [multi_agent::protocols::pipeline::{core::clone::Clone for multi_agent::protocols::pipeline::Pipeline}]
    Source: 'src/protocols/pipeline.rs', lines 12:9-12:14 -/
@[reducible]
def protocols.pipeline.Pipeline.Insts.CoreCloneClone : core.clone.Clone
  protocols.pipeline.Pipeline := {
  clone := protocols.pipeline.Pipeline.Insts.CoreCloneClone.clone
}

/-- Trait implementation: [multi_agent::protocols::pipeline::{core::marker::StructuralPartialEq for multi_agent::protocols::pipeline::Pipeline}]
    Source: 'src/protocols/pipeline.rs', lines 12:16-12:25 -/
@[reducible]
def protocols.pipeline.Pipeline.Insts.CoreMarkerStructuralPartialEq :
  core.marker.StructuralPartialEq protocols.pipeline.Pipeline := {
}

/-- [multi_agent::protocols::pipeline::{core::cmp::PartialEq<multi_agent::protocols::pipeline::Pipeline> for multi_agent::protocols::pipeline::Pipeline}::ne]:
    Source: 'src/protocols/pipeline.rs', lines 12:16-12:25 -/
axiom protocols.pipeline.Pipeline.Insts.CoreCmpPartialEqPipeline.ne
  : protocols.pipeline.Pipeline → protocols.pipeline.Pipeline → Result Bool

/-- [multi_agent::protocols::pipeline::{core::cmp::PartialEq<multi_agent::protocols::pipeline::Pipeline> for multi_agent::protocols::pipeline::Pipeline}::eq]:
    Source: 'src/protocols/pipeline.rs', lines 12:16-12:25 -/
def protocols.pipeline.Pipeline.Insts.CoreCmpPartialEqPipeline.eq
  (self : protocols.pipeline.Pipeline) (other : protocols.pipeline.Pipeline) :
  Result Bool
  := do
  alloc.vec.partial_eq.PartialEqVec.eq
    protocols.pipeline.PipelineStage.Insts.CoreCmpPartialEqPipelineStage
    self.stages other.stages

/-- Trait implementation: [multi_agent::protocols::pipeline::{core::cmp::PartialEq<multi_agent::protocols::pipeline::Pipeline> for multi_agent::protocols::pipeline::Pipeline}]
    Source: 'src/protocols/pipeline.rs', lines 12:16-12:25 -/
@[reducible]
def protocols.pipeline.Pipeline.Insts.CoreCmpPartialEqPipeline :
  core.cmp.PartialEq protocols.pipeline.Pipeline protocols.pipeline.Pipeline
  := {
  eq := protocols.pipeline.Pipeline.Insts.CoreCmpPartialEqPipeline.eq
  ne := protocols.pipeline.Pipeline.Insts.CoreCmpPartialEqPipeline.ne
}

/-- [multi_agent::protocols::pipeline::{core::cmp::Eq for multi_agent::protocols::pipeline::Pipeline}::assert_receiver_is_total_eq]:
    Source: 'src/protocols/pipeline.rs', lines 12:27-12:29 -/
def protocols.pipeline.Pipeline.Insts.CoreCmpEq.assert_receiver_is_total_eq
  (self : protocols.pipeline.Pipeline) : Result Unit := do
  ok ()

/-- Trait implementation: [multi_agent::protocols::pipeline::{core::cmp::Eq for multi_agent::protocols::pipeline::Pipeline}]
    Source: 'src/protocols/pipeline.rs', lines 12:27-12:29 -/
@[reducible]
def protocols.pipeline.Pipeline.Insts.CoreCmpEq : core.cmp.Eq
  protocols.pipeline.Pipeline := {
  partialEqInst := protocols.pipeline.Pipeline.Insts.CoreCmpPartialEqPipeline
  assert_receiver_is_total_eq :=
    protocols.pipeline.Pipeline.Insts.CoreCmpEq.assert_receiver_is_total_eq
}

/-- [multi_agent::protocols::pipeline::{core::fmt::Debug for multi_agent::protocols::pipeline::Pipeline}::fmt]:
    Source: 'src/protocols/pipeline.rs', lines 12:31-12:36 -/
def protocols.pipeline.Pipeline.Insts.CoreFmtDebug.fmt
  (self : protocols.pipeline.Pipeline) (f : core.fmt.Formatter) :
  Result ((core.result.Result Unit core.fmt.Error) × core.fmt.Formatter)
  := do
  let dyn :=
    Dyn.mk _ (core.fmt.DebugShared (core.fmt.DebugVec
      protocols.pipeline.PipelineStage.Insts.CoreFmtDebug)) self.stages
  core.fmt.Formatter.debug_struct_field1_finish f (toStr "Pipeline") (toStr
    "stages") dyn

/-- Trait implementation: [multi_agent::protocols::pipeline::{core::fmt::Debug for multi_agent::protocols::pipeline::Pipeline}]
    Source: 'src/protocols/pipeline.rs', lines 12:31-12:36 -/
@[reducible]
def protocols.pipeline.Pipeline.Insts.CoreFmtDebug : core.fmt.Debug
  protocols.pipeline.Pipeline := {
  fmt := protocols.pipeline.Pipeline.Insts.CoreFmtDebug.fmt
}

/-- [multi_agent::protocols::pipeline::pipeline_new]:
    Source: 'src/protocols/pipeline.rs', lines 18:0-20:1 -/
def protocols.pipeline.pipeline_new : Result protocols.pipeline.Pipeline := do
  ok { stages := (alloc.vec.Vec.new protocols.pipeline.PipelineStage) }

/-- [multi_agent::protocols::pipeline::pipeline_add_stage]:
    Source: 'src/protocols/pipeline.rs', lines 23:0-34:1 -/
def protocols.pipeline.pipeline_add_stage
  (pipeline : protocols.pipeline.Pipeline) (agent_id : Std.U32)
  (input_type : Std.U32) (output_type : Std.U32) :
  Result protocols.pipeline.Pipeline
  := do
  let v ←
    alloc.vec.Vec.push pipeline.stages ({ agent_id, input_type, output_type } :
      protocols.pipeline.PipelineStage)
  ok { stages := v }

/-- [multi_agent::protocols::pipeline::is_pipeline_valid]: loop body 0:
    Source: 'src/protocols/pipeline.rs', lines 42:4-48:1 -/
@[rust_loop_body]
def protocols.pipeline.is_pipeline_valid_loop.body
  (v : alloc.vec.Vec protocols.pipeline.PipelineStage)
  (iter : core.ops.range.Range Std.Usize) :
  Result (ControlFlow (core.ops.range.Range Std.Usize) Bool)
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok (done true)
  | some i =>
    let ps ←
      alloc.vec.Vec.index (core.slice.index.SliceIndexUsizeSlice
        protocols.pipeline.PipelineStage) v i
    let i1 ← i + 1#usize
    let ps1 ←
      alloc.vec.Vec.index (core.slice.index.SliceIndexUsizeSlice
        protocols.pipeline.PipelineStage) v i1
    if ps.output_type != ps1.input_type
    then ok (done false)
    else ok (cont iter1)

/-- [multi_agent::protocols::pipeline::is_pipeline_valid]: loop 0:
    Source: 'src/protocols/pipeline.rs', lines 42:4-48:1 -/
@[rust_loop]
def protocols.pipeline.is_pipeline_valid_loop
  (iter : core.ops.range.Range Std.Usize)
  (v : alloc.vec.Vec protocols.pipeline.PipelineStage) :
  Result Bool
  := do
  loop
    (fun iter1 => protocols.pipeline.is_pipeline_valid_loop.body v iter1)
    iter

/-- [multi_agent::protocols::pipeline::is_pipeline_valid]:
    Source: 'src/protocols/pipeline.rs', lines 38:0-48:1 -/
def protocols.pipeline.is_pipeline_valid
  (pipeline : protocols.pipeline.Pipeline) : Result Bool := do
  let i := alloc.vec.Vec.len pipeline.stages
  if i <= 1#usize
  then ok true
  else
    let i1 := alloc.vec.Vec.len pipeline.stages
    let i2 ← i1 - 1#usize
    protocols.pipeline.is_pipeline_valid_loop
      { start := 0#usize, «end» := i2 } pipeline.stages

/-- [multi_agent::protocols::pipeline::pipeline_agent_at]:
    Source: 'src/protocols/pipeline.rs', lines 51:0-58:1 -/
def protocols.pipeline.pipeline_agent_at
  (pipeline : protocols.pipeline.Pipeline) (stage_index : Std.U32) :
  Result (Option Std.U32)
  := do
  let idx ← lift (UScalar.cast .Usize stage_index)
  let i := alloc.vec.Vec.len pipeline.stages
  if idx < i
  then
    let ps ←
      alloc.vec.Vec.index (core.slice.index.SliceIndexUsizeSlice
        protocols.pipeline.PipelineStage) pipeline.stages idx
    ok (some ps.agent_id)
  else ok none

/-- [multi_agent::protocols::pipeline::pipeline_len]:
    Source: 'src/protocols/pipeline.rs', lines 61:0-63:1 -/
def protocols.pipeline.pipeline_len
  (pipeline : protocols.pipeline.Pipeline) : Result Std.U32 := do
  let i := alloc.vec.Vec.len pipeline.stages
  ok (UScalar.cast .U32 i)

/-- [multi_agent::protocols::request_response::RRState]
    Source: 'src/protocols/request_response.rs', lines 5:0-10:1 -/
@[discriminant isize]
inductive protocols.request_response.RRState where
| Idle : protocols.request_response.RRState
| AwaitingResponse : protocols.request_response.RRState
| Completed : protocols.request_response.RRState
| TimedOut : protocols.request_response.RRState

/-- [multi_agent::protocols::request_response::{core::clone::Clone for multi_agent::protocols::request_response::RRState}::clone]:
    Source: 'src/protocols/request_response.rs', lines 4:9-4:14 -/
def protocols.request_response.RRState.Insts.CoreCloneClone.clone
  (self : protocols.request_response.RRState) :
  Result protocols.request_response.RRState
  := do
  match self with
  | protocols.request_response.RRState.Idle =>
    ok protocols.request_response.RRState.Idle
  | protocols.request_response.RRState.AwaitingResponse =>
    ok protocols.request_response.RRState.AwaitingResponse
  | protocols.request_response.RRState.Completed =>
    ok protocols.request_response.RRState.Completed
  | protocols.request_response.RRState.TimedOut =>
    ok protocols.request_response.RRState.TimedOut

/-- Trait implementation: [multi_agent::protocols::request_response::{core::clone::Clone for multi_agent::protocols::request_response::RRState}]
    Source: 'src/protocols/request_response.rs', lines 4:9-4:14 -/
@[reducible]
def protocols.request_response.RRState.Insts.CoreCloneClone : core.clone.Clone
  protocols.request_response.RRState := {
  clone := protocols.request_response.RRState.Insts.CoreCloneClone.clone
}

/-- Trait implementation: [multi_agent::protocols::request_response::{core::marker::StructuralPartialEq for multi_agent::protocols::request_response::RRState}]
    Source: 'src/protocols/request_response.rs', lines 4:16-4:25 -/
@[reducible]
def protocols.request_response.RRState.Insts.CoreMarkerStructuralPartialEq :
  core.marker.StructuralPartialEq protocols.request_response.RRState := {
}

/-- [multi_agent::protocols::request_response::{core::cmp::PartialEq<multi_agent::protocols::request_response::RRState> for multi_agent::protocols::request_response::RRState}::ne]:
    Source: 'src/protocols/request_response.rs', lines 4:16-4:25 -/
axiom protocols.request_response.RRState.Insts.CoreCmpPartialEqRRState.ne
  :
  protocols.request_response.RRState → protocols.request_response.RRState →
    Result Bool

/-- [multi_agent::protocols::request_response::{core::cmp::PartialEq<multi_agent::protocols::request_response::RRState> for multi_agent::protocols::request_response::RRState}::eq]:
    Source: 'src/protocols/request_response.rs', lines 4:16-4:25 -/
def protocols.request_response.RRState.Insts.CoreCmpPartialEqRRState.eq
  (self : protocols.request_response.RRState)
  (other : protocols.request_response.RRState) :
  Result Bool
  := do
  let self1 := read_discriminant self
  let other1 := read_discriminant other
  ok (self1 = other1)

/-- Trait implementation: [multi_agent::protocols::request_response::{core::cmp::PartialEq<multi_agent::protocols::request_response::RRState> for multi_agent::protocols::request_response::RRState}]
    Source: 'src/protocols/request_response.rs', lines 4:16-4:25 -/
@[reducible]
def protocols.request_response.RRState.Insts.CoreCmpPartialEqRRState :
  core.cmp.PartialEq protocols.request_response.RRState
  protocols.request_response.RRState := {
  eq := protocols.request_response.RRState.Insts.CoreCmpPartialEqRRState.eq
  ne := protocols.request_response.RRState.Insts.CoreCmpPartialEqRRState.ne
}

/-- [multi_agent::protocols::request_response::{core::cmp::Eq for multi_agent::protocols::request_response::RRState}::assert_receiver_is_total_eq]:
    Source: 'src/protocols/request_response.rs', lines 4:27-4:29 -/
def
  protocols.request_response.RRState.Insts.CoreCmpEq.assert_receiver_is_total_eq
  (self : protocols.request_response.RRState) : Result Unit := do
  ok ()

/-- Trait implementation: [multi_agent::protocols::request_response::{core::cmp::Eq for multi_agent::protocols::request_response::RRState}]
    Source: 'src/protocols/request_response.rs', lines 4:27-4:29 -/
@[reducible]
def protocols.request_response.RRState.Insts.CoreCmpEq : core.cmp.Eq
  protocols.request_response.RRState := {
  partialEqInst :=
    protocols.request_response.RRState.Insts.CoreCmpPartialEqRRState
  assert_receiver_is_total_eq :=
    protocols.request_response.RRState.Insts.CoreCmpEq.assert_receiver_is_total_eq
}

/-- [multi_agent::protocols::request_response::{core::fmt::Debug for multi_agent::protocols::request_response::RRState}::fmt]:
    Source: 'src/protocols/request_response.rs', lines 4:31-4:36 -/
def protocols.request_response.RRState.Insts.CoreFmtDebug.fmt
  (self : protocols.request_response.RRState) (f : core.fmt.Formatter) :
  Result ((core.result.Result Unit core.fmt.Error) × core.fmt.Formatter)
  := do
  match self with
  | protocols.request_response.RRState.Idle =>
    core.fmt.Formatter.write_str f (toStr "Idle")
  | protocols.request_response.RRState.AwaitingResponse =>
    core.fmt.Formatter.write_str f (toStr "AwaitingResponse")
  | protocols.request_response.RRState.Completed =>
    core.fmt.Formatter.write_str f (toStr "Completed")
  | protocols.request_response.RRState.TimedOut =>
    core.fmt.Formatter.write_str f (toStr "TimedOut")

/-- Trait implementation: [multi_agent::protocols::request_response::{core::fmt::Debug for multi_agent::protocols::request_response::RRState}]
    Source: 'src/protocols/request_response.rs', lines 4:31-4:36 -/
@[reducible]
def protocols.request_response.RRState.Insts.CoreFmtDebug : core.fmt.Debug
  protocols.request_response.RRState := {
  fmt := protocols.request_response.RRState.Insts.CoreFmtDebug.fmt
}

/-- [multi_agent::protocols::request_response::RequestResponse]
    Source: 'src/protocols/request_response.rs', lines 14:0-21:1 -/
structure protocols.request_response.RequestResponse where
  requester : Std.U32
  responder : Std.U32
  request_id : Std.U32
  state : protocols.request_response.RRState
  ticks_waiting : Std.U32
  timeout : Std.U32

/-- [multi_agent::protocols::request_response::{core::clone::Clone for multi_agent::protocols::request_response::RequestResponse}::clone]:
    Source: 'src/protocols/request_response.rs', lines 13:9-13:14 -/
def protocols.request_response.RequestResponse.Insts.CoreCloneClone.clone
  (self : protocols.request_response.RequestResponse) :
  Result protocols.request_response.RequestResponse
  := do
  let i ← lift (core.clone.impls.CloneU32.clone self.requester)
  let i1 ← lift (core.clone.impls.CloneU32.clone self.responder)
  let i2 ← lift (core.clone.impls.CloneU32.clone self.request_id)
  let r ←
    protocols.request_response.RRState.Insts.CoreCloneClone.clone self.state
  let i3 ← lift (core.clone.impls.CloneU32.clone self.ticks_waiting)
  let i4 ← lift (core.clone.impls.CloneU32.clone self.timeout)
  ok
    {
      requester := i,
      responder := i1,
      request_id := i2,
      state := r,
      ticks_waiting := i3,
      timeout := i4
    }

/-- Trait implementation: [multi_agent::protocols::request_response::{core::clone::Clone for multi_agent::protocols::request_response::RequestResponse}]
    Source: 'src/protocols/request_response.rs', lines 13:9-13:14 -/
@[reducible]
def protocols.request_response.RequestResponse.Insts.CoreCloneClone :
  core.clone.Clone protocols.request_response.RequestResponse := {
  clone :=
    protocols.request_response.RequestResponse.Insts.CoreCloneClone.clone
}

/-- Trait implementation: [multi_agent::protocols::request_response::{core::marker::StructuralPartialEq for multi_agent::protocols::request_response::RequestResponse}]
    Source: 'src/protocols/request_response.rs', lines 13:16-13:25 -/
@[reducible]
def
  protocols.request_response.RequestResponse.Insts.CoreMarkerStructuralPartialEq
  : core.marker.StructuralPartialEq protocols.request_response.RequestResponse
  := {
}

/-- [multi_agent::protocols::request_response::{core::cmp::PartialEq<multi_agent::protocols::request_response::RequestResponse> for multi_agent::protocols::request_response::RequestResponse}::ne]:
    Source: 'src/protocols/request_response.rs', lines 13:16-13:25 -/
axiom
  protocols.request_response.RequestResponse.Insts.CoreCmpPartialEqRequestResponse.ne
  :
  protocols.request_response.RequestResponse →
    protocols.request_response.RequestResponse → Result Bool

/-- [multi_agent::protocols::request_response::{core::cmp::PartialEq<multi_agent::protocols::request_response::RequestResponse> for multi_agent::protocols::request_response::RequestResponse}::eq]:
    Source: 'src/protocols/request_response.rs', lines 13:16-13:25 -/
def
  protocols.request_response.RequestResponse.Insts.CoreCmpPartialEqRequestResponse.eq
  (self : protocols.request_response.RequestResponse)
  (other : protocols.request_response.RequestResponse) :
  Result Bool
  := do
  if self.request_id = other.request_id
  then
    if self.ticks_waiting = other.ticks_waiting
    then
      if self.timeout = other.timeout
      then
        if self.requester = other.requester
        then
          if self.responder = other.responder
          then
            protocols.request_response.RRState.Insts.CoreCmpPartialEqRRState.eq
              self.state other.state
          else ok false
        else ok false
      else ok false
    else ok false
  else ok false

/-- Trait implementation: [multi_agent::protocols::request_response::{core::cmp::PartialEq<multi_agent::protocols::request_response::RequestResponse> for multi_agent::protocols::request_response::RequestResponse}]
    Source: 'src/protocols/request_response.rs', lines 13:16-13:25 -/
@[reducible]
def
  protocols.request_response.RequestResponse.Insts.CoreCmpPartialEqRequestResponse
  : core.cmp.PartialEq protocols.request_response.RequestResponse
  protocols.request_response.RequestResponse := {
  eq :=
    protocols.request_response.RequestResponse.Insts.CoreCmpPartialEqRequestResponse.eq
  ne :=
    protocols.request_response.RequestResponse.Insts.CoreCmpPartialEqRequestResponse.ne
}

/-- [multi_agent::protocols::request_response::{core::cmp::Eq for multi_agent::protocols::request_response::RequestResponse}::assert_receiver_is_total_eq]:
    Source: 'src/protocols/request_response.rs', lines 13:27-13:29 -/
def
  protocols.request_response.RequestResponse.Insts.CoreCmpEq.assert_receiver_is_total_eq
  (self : protocols.request_response.RequestResponse) : Result Unit := do
  ok ()

/-- Trait implementation: [multi_agent::protocols::request_response::{core::cmp::Eq for multi_agent::protocols::request_response::RequestResponse}]
    Source: 'src/protocols/request_response.rs', lines 13:27-13:29 -/
@[reducible]
def protocols.request_response.RequestResponse.Insts.CoreCmpEq : core.cmp.Eq
  protocols.request_response.RequestResponse := {
  partialEqInst :=
    protocols.request_response.RequestResponse.Insts.CoreCmpPartialEqRequestResponse
  assert_receiver_is_total_eq :=
    protocols.request_response.RequestResponse.Insts.CoreCmpEq.assert_receiver_is_total_eq
}

/-- [multi_agent::protocols::request_response::{core::fmt::Debug for multi_agent::protocols::request_response::RequestResponse}::fmt]:
    Source: 'src/protocols/request_response.rs', lines 13:31-13:36 -/
def protocols.request_response.RequestResponse.Insts.CoreFmtDebug.fmt
  (self : protocols.request_response.RequestResponse) (f : core.fmt.Formatter)
  :
  Result ((core.result.Result Unit core.fmt.Error) × core.fmt.Formatter)
  := do
  let dyn := Dyn.mk _ core.fmt.DebugU32 self.requester
  let dyn1 := Dyn.mk _ core.fmt.DebugU32 self.responder
  let dyn2 := Dyn.mk _ core.fmt.DebugU32 self.request_id
  let dyn3 :=
    Dyn.mk _ protocols.request_response.RRState.Insts.CoreFmtDebug self.state
  let dyn4 := Dyn.mk _ core.fmt.DebugU32 self.ticks_waiting
  let dyn5 := Dyn.mk _ (core.fmt.DebugShared core.fmt.DebugU32) self.timeout
  let values :=
    Array.to_slice (Array.make 6#usize [ dyn, dyn1, dyn2, dyn3, dyn4, dyn5 ])
  let s ←
    lift (Array.to_slice
      (Array.make 6#usize [
        toStr "requester", toStr "responder", toStr "request_id", toStr
        "state", toStr "ticks_waiting", toStr "timeout"
        ]))
  core.fmt.Formatter.debug_struct_fields_finish f (toStr "RequestResponse") s
    values

/-- Trait implementation: [multi_agent::protocols::request_response::{core::fmt::Debug for multi_agent::protocols::request_response::RequestResponse}]
    Source: 'src/protocols/request_response.rs', lines 13:31-13:36 -/
@[reducible]
def protocols.request_response.RequestResponse.Insts.CoreFmtDebug :
  core.fmt.Debug protocols.request_response.RequestResponse := {
  fmt := protocols.request_response.RequestResponse.Insts.CoreFmtDebug.fmt
}

/-- [multi_agent::protocols::request_response::rr_new]:
    Source: 'src/protocols/request_response.rs', lines 24:0-38:1 -/
def protocols.request_response.rr_new
  (requester : Std.U32) (responder : Std.U32) (request_id : Std.U32)
  (timeout : Std.U32) :
  Result protocols.request_response.RequestResponse
  := do
  ok
    {
      requester,
      responder,
      request_id,
      state := protocols.request_response.RRState.Idle,
      ticks_waiting := 0#u32,
      timeout
    }

/-- [multi_agent::protocols::request_response::rr_send]:
    Source: 'src/protocols/request_response.rs', lines 41:0-45:1 -/
def protocols.request_response.rr_send
  (rr : protocols.request_response.RequestResponse) :
  Result protocols.request_response.RequestResponse
  := do
  let b ←
    protocols.request_response.RRState.Insts.CoreCmpPartialEqRRState.eq
      rr.state protocols.request_response.RRState.Idle
  if b
  then
    ok { rr with state := protocols.request_response.RRState.AwaitingResponse }
  else ok rr

/-- [multi_agent::protocols::request_response::rr_receive]:
    Source: 'src/protocols/request_response.rs', lines 48:0-52:1 -/
def protocols.request_response.rr_receive
  (rr : protocols.request_response.RequestResponse) :
  Result protocols.request_response.RequestResponse
  := do
  let b ←
    protocols.request_response.RRState.Insts.CoreCmpPartialEqRRState.eq
      rr.state protocols.request_response.RRState.AwaitingResponse
  if b
  then ok { rr with state := protocols.request_response.RRState.Completed }
  else ok rr

/-- [multi_agent::protocols::request_response::rr_tick]:
    Source: 'src/protocols/request_response.rs', lines 55:0-62:1 -/
def protocols.request_response.rr_tick
  (rr : protocols.request_response.RequestResponse) :
  Result protocols.request_response.RequestResponse
  := do
  let b ←
    protocols.request_response.RRState.Insts.CoreCmpPartialEqRRState.eq
      rr.state protocols.request_response.RRState.AwaitingResponse
  if b
  then
    let i ← rr.ticks_waiting + 1#u32
    if i >= rr.timeout
    then
      ok
        {
          rr
            with
            state := protocols.request_response.RRState.TimedOut,
            ticks_waiting := i
        }
    else ok { rr with ticks_waiting := i }
  else ok rr

/-- [multi_agent::protocols::request_response::rr_is_done]:
    Source: 'src/protocols/request_response.rs', lines 65:0-67:1 -/
def protocols.request_response.rr_is_done
  (rr : protocols.request_response.RequestResponse) : Result Bool := do
  let b ←
    protocols.request_response.RRState.Insts.CoreCmpPartialEqRRState.eq
      rr.state protocols.request_response.RRState.Completed
  if b
  then ok true
  else
    protocols.request_response.RRState.Insts.CoreCmpPartialEqRRState.eq
      rr.state protocols.request_response.RRState.TimedOut

/-- [multi_agent::protocols::voting::VotingRound]
    Source: 'src/protocols/voting.rs', lines 5:0-10:1 -/
structure protocols.voting.VotingRound where
  proposal_id : Std.U32
  votes : alloc.vec.Vec (Std.U32 × Bool)
  required_majority : Std.U32
  expected_voters : Std.U32

/-- [multi_agent::protocols::voting::{core::clone::Clone for multi_agent::protocols::voting::VotingRound}::clone]:
    Source: 'src/protocols/voting.rs', lines 4:9-4:14 -/
def protocols.voting.VotingRound.Insts.CoreCloneClone.clone
  (self : protocols.voting.VotingRound) :
  Result protocols.voting.VotingRound
  := do
  let i ← lift (core.clone.impls.CloneU32.clone self.proposal_id)
  let v ←
    alloc.vec.CloneVec.clone (BuiltinClone (Std.U32 × Bool)) self.votes
  let i1 ← lift (core.clone.impls.CloneU32.clone self.required_majority)
  let i2 ← lift (core.clone.impls.CloneU32.clone self.expected_voters)
  ok
    {
      proposal_id := i,
      votes := v,
      required_majority := i1,
      expected_voters := i2
    }

/-- Trait implementation: [multi_agent::protocols::voting::{core::clone::Clone for multi_agent::protocols::voting::VotingRound}]
    Source: 'src/protocols/voting.rs', lines 4:9-4:14 -/
@[reducible]
def protocols.voting.VotingRound.Insts.CoreCloneClone : core.clone.Clone
  protocols.voting.VotingRound := {
  clone := protocols.voting.VotingRound.Insts.CoreCloneClone.clone
}

/-- Trait implementation: [multi_agent::protocols::voting::{core::marker::StructuralPartialEq for multi_agent::protocols::voting::VotingRound}]
    Source: 'src/protocols/voting.rs', lines 4:16-4:25 -/
@[reducible]
def protocols.voting.VotingRound.Insts.CoreMarkerStructuralPartialEq :
  core.marker.StructuralPartialEq protocols.voting.VotingRound := {
}

/-- [multi_agent::protocols::voting::{core::cmp::PartialEq<multi_agent::protocols::voting::VotingRound> for multi_agent::protocols::voting::VotingRound}::ne]:
    Source: 'src/protocols/voting.rs', lines 4:16-4:25 -/
axiom protocols.voting.VotingRound.Insts.CoreCmpPartialEqVotingRound.ne
  :
  protocols.voting.VotingRound → protocols.voting.VotingRound → Result Bool

/-- [multi_agent::protocols::voting::{core::cmp::PartialEq<multi_agent::protocols::voting::VotingRound> for multi_agent::protocols::voting::VotingRound}::eq]:
    Source: 'src/protocols/voting.rs', lines 4:16-4:25 -/
def protocols.voting.VotingRound.Insts.CoreCmpPartialEqVotingRound.eq
  (self : protocols.voting.VotingRound) (other : protocols.voting.VotingRound)
  :
  Result Bool
  := do
  if self.proposal_id = other.proposal_id
  then
    if self.required_majority = other.required_majority
    then
      if self.expected_voters = other.expected_voters
      then
        alloc.vec.partial_eq.PartialEqVec.eq (Pair.Insts.CoreCmpPartialEqPair
          core.cmp.PartialEqU32 core.cmp.PartialEqBool) self.votes other.votes
      else ok false
    else ok false
  else ok false

/-- Trait implementation: [multi_agent::protocols::voting::{core::cmp::PartialEq<multi_agent::protocols::voting::VotingRound> for multi_agent::protocols::voting::VotingRound}]
    Source: 'src/protocols/voting.rs', lines 4:16-4:25 -/
@[reducible]
def protocols.voting.VotingRound.Insts.CoreCmpPartialEqVotingRound :
  core.cmp.PartialEq protocols.voting.VotingRound protocols.voting.VotingRound
  := {
  eq := protocols.voting.VotingRound.Insts.CoreCmpPartialEqVotingRound.eq
  ne := protocols.voting.VotingRound.Insts.CoreCmpPartialEqVotingRound.ne
}

/-- [multi_agent::protocols::voting::{core::cmp::Eq for multi_agent::protocols::voting::VotingRound}::assert_receiver_is_total_eq]:
    Source: 'src/protocols/voting.rs', lines 4:27-4:29 -/
def protocols.voting.VotingRound.Insts.CoreCmpEq.assert_receiver_is_total_eq
  (self : protocols.voting.VotingRound) : Result Unit := do
  ok ()

/-- Trait implementation: [multi_agent::protocols::voting::{core::cmp::Eq for multi_agent::protocols::voting::VotingRound}]
    Source: 'src/protocols/voting.rs', lines 4:27-4:29 -/
@[reducible]
def protocols.voting.VotingRound.Insts.CoreCmpEq : core.cmp.Eq
  protocols.voting.VotingRound := {
  partialEqInst :=
    protocols.voting.VotingRound.Insts.CoreCmpPartialEqVotingRound
  assert_receiver_is_total_eq :=
    protocols.voting.VotingRound.Insts.CoreCmpEq.assert_receiver_is_total_eq
}

/-- [multi_agent::protocols::voting::{core::fmt::Debug for multi_agent::protocols::voting::VotingRound}::fmt]:
    Source: 'src/protocols/voting.rs', lines 4:31-4:36 -/
def protocols.voting.VotingRound.Insts.CoreFmtDebug.fmt
  (self : protocols.voting.VotingRound) (f : core.fmt.Formatter) :
  Result ((core.result.Result Unit core.fmt.Error) × core.fmt.Formatter)
  := do
  let dyn := Dyn.mk _ core.fmt.DebugU32 self.proposal_id
  let dyn1 :=
    Dyn.mk _ (core.fmt.DebugVec (Pair.Insts.CoreFmtDebug core.fmt.DebugU32
      core.fmt.DebugBool)) self.votes
  let dyn2 := Dyn.mk _ core.fmt.DebugU32 self.required_majority
  let dyn3 :=
    Dyn.mk _ (core.fmt.DebugShared core.fmt.DebugU32) self.expected_voters
  core.fmt.Formatter.debug_struct_field4_finish f (toStr "VotingRound") (toStr
    "proposal_id") dyn (toStr "votes") dyn1 (toStr "required_majority") dyn2
    (toStr "expected_voters") dyn3

/-- Trait implementation: [multi_agent::protocols::voting::{core::fmt::Debug for multi_agent::protocols::voting::VotingRound}]
    Source: 'src/protocols/voting.rs', lines 4:31-4:36 -/
@[reducible]
def protocols.voting.VotingRound.Insts.CoreFmtDebug : core.fmt.Debug
  protocols.voting.VotingRound := {
  fmt := protocols.voting.VotingRound.Insts.CoreFmtDebug.fmt
}

/-- [multi_agent::protocols::voting::voting_new]:
    Source: 'src/protocols/voting.rs', lines 13:0-20:1 -/
def protocols.voting.voting_new
  (proposal_id : Std.U32) (required_majority : Std.U32)
  (expected_voters : Std.U32) :
  Result protocols.voting.VotingRound
  := do
  ok
    {
      proposal_id,
      votes := (alloc.vec.Vec.new (Std.U32 × Bool)),
      required_majority,
      expected_voters
    }

/-- [multi_agent::protocols::voting::cast_vote]: loop body 0:
    Source: 'src/protocols/voting.rs', lines 24:4-30:1 -/
@[rust_loop_body]
def protocols.voting.cast_vote_loop.body
  (v : alloc.vec.Vec (Std.U32 × Bool)) (agent_id : Std.U32) (vote : Bool)
  (iter : core.slice.iter.Iter (Std.U32 × Bool)) :
  Result (ControlFlow (core.slice.iter.Iter (Std.U32 × Bool)) (alloc.vec.Vec
    (Std.U32 × Bool)))
  := do
  let (o, iter1) ← core.slice.iter.IteratorSliceIter.next iter
  match o with
  | none => let v1 ← alloc.vec.Vec.push v (agent_id, vote)
            ok (done v1)
  | some p =>
    let (id, _) := p
    if id = agent_id
    then ok (done sorry)
    else ok (cont iter1)

/-- [multi_agent::protocols::voting::cast_vote]: loop 0:
    Source: 'src/protocols/voting.rs', lines 24:4-30:1 -/
@[rust_loop]
def protocols.voting.cast_vote_loop
  (iter : core.slice.iter.Iter (Std.U32 × Bool))
  (v : alloc.vec.Vec (Std.U32 × Bool)) (agent_id : Std.U32) (vote : Bool) :
  Result (alloc.vec.Vec (Std.U32 × Bool))
  := do
  loop
    (fun iter1 => protocols.voting.cast_vote_loop.body v agent_id vote iter1)
    iter

/-- [multi_agent::protocols::voting::cast_vote]:
    Source: 'src/protocols/voting.rs', lines 23:0-30:1 -/
def protocols.voting.cast_vote
  (round : protocols.voting.VotingRound) (agent_id : Std.U32) (vote : Bool) :
  Result protocols.voting.VotingRound
  := do
  let iter ←
    SharedAVec.Insts.CoreIterTraitsCollectIntoIteratorSharedATIter.into_iter
      Global round.votes
  let v ← protocols.voting.cast_vote_loop iter round.votes agent_id vote
  ok { round with votes := v }

/-- [multi_agent::protocols::voting::tally::closure]
    Source: 'src/protocols/voting.rs', lines 42:46-42:57 -/
@[reducible]
def protocols.voting.tally.closure := Unit

/-- [multi_agent::protocols::voting::tally::{core::ops::function::FnMut<(&'_ (&'_ ((u32, bool)))), bool> for multi_agent::protocols::voting::tally::closure}::call_mut]:
    Source: 'src/protocols/voting.rs', lines 42:46-42:57 -/
def
  protocols.voting.tally.closure.Insts.CoreOpsFunctionFnMutTupleSharedSharedPairU32BoolBool.call_mut
  (c : protocols.voting.tally.closure) (tupled_args : (Std.U32 × Bool)) :
  Result (Bool × protocols.voting.tally.closure)
  := do
  let (_, v) := tupled_args
  ok (v, c)

/-- [multi_agent::protocols::voting::tally::{core::ops::function::FnOnce<(&'_ (&'_ ((u32, bool)))), bool> for multi_agent::protocols::voting::tally::closure}::call_once]:
    Source: 'src/protocols/voting.rs', lines 42:46-42:57 -/
def
  protocols.voting.tally.closure.Insts.CoreOpsFunctionFnOnceTupleSharedSharedPairU32BoolBool.call_once
  (c : protocols.voting.tally.closure) (p : (Std.U32 × Bool)) :
  Result Bool
  := do
  let (b, _) ←
    protocols.voting.tally.closure.Insts.CoreOpsFunctionFnMutTupleSharedSharedPairU32BoolBool.call_mut
      c p
  ok b

/-- Trait implementation: [multi_agent::protocols::voting::tally::{core::ops::function::FnOnce<(&'_ (&'_ ((u32, bool)))), bool> for multi_agent::protocols::voting::tally::closure}]
    Source: 'src/protocols/voting.rs', lines 42:46-42:57 -/
@[reducible]
def
  protocols.voting.tally.closure.Insts.CoreOpsFunctionFnOnceTupleSharedSharedPairU32BoolBool
  : core.ops.function.FnOnce protocols.voting.tally.closure (Std.U32 × Bool)
  Bool := {
  call_once :=
    protocols.voting.tally.closure.Insts.CoreOpsFunctionFnOnceTupleSharedSharedPairU32BoolBool.call_once
}

/-- Trait implementation: [multi_agent::protocols::voting::tally::{core::ops::function::FnMut<(&'_ (&'_ ((u32, bool)))), bool> for multi_agent::protocols::voting::tally::closure}]
    Source: 'src/protocols/voting.rs', lines 42:46-42:57 -/
@[reducible]
def
  protocols.voting.tally.closure.Insts.CoreOpsFunctionFnMutTupleSharedSharedPairU32BoolBool
  : core.ops.function.FnMut protocols.voting.tally.closure (Std.U32 × Bool)
  Bool := {
  FnOnceInst :=
    protocols.voting.tally.closure.Insts.CoreOpsFunctionFnOnceTupleSharedSharedPairU32BoolBool
  call_mut :=
    protocols.voting.tally.closure.Insts.CoreOpsFunctionFnMutTupleSharedSharedPairU32BoolBool.call_mut
}

/-- [multi_agent::protocols::voting::tally]:
    Source: 'src/protocols/voting.rs', lines 37:0-50:1 -/
def protocols.voting.tally
  (round : protocols.voting.VotingRound) : Result (Option Bool) := do
  sorry

/-- [multi_agent::protocols::voting::yes_count::closure]
    Source: 'src/protocols/voting.rs', lines 54:30-54:41 -/
@[reducible]
def protocols.voting.yes_count.closure := Unit

/-- [multi_agent::protocols::voting::yes_count::{core::ops::function::FnMut<(&'_ (&'_ ((u32, bool)))), bool> for multi_agent::protocols::voting::yes_count::closure}::call_mut]:
    Source: 'src/protocols/voting.rs', lines 54:30-54:41 -/
def
  protocols.voting.yes_count.closure.Insts.CoreOpsFunctionFnMutTupleSharedSharedPairU32BoolBool.call_mut
  (c : protocols.voting.yes_count.closure) (tupled_args : (Std.U32 × Bool)) :
  Result (Bool × protocols.voting.yes_count.closure)
  := do
  let (_, v) := tupled_args
  ok (v, c)

/-- [multi_agent::protocols::voting::yes_count::{core::ops::function::FnOnce<(&'_ (&'_ ((u32, bool)))), bool> for multi_agent::protocols::voting::yes_count::closure}::call_once]:
    Source: 'src/protocols/voting.rs', lines 54:30-54:41 -/
def
  protocols.voting.yes_count.closure.Insts.CoreOpsFunctionFnOnceTupleSharedSharedPairU32BoolBool.call_once
  (c : protocols.voting.yes_count.closure) (p : (Std.U32 × Bool)) :
  Result Bool
  := do
  let (b, _) ←
    protocols.voting.yes_count.closure.Insts.CoreOpsFunctionFnMutTupleSharedSharedPairU32BoolBool.call_mut
      c p
  ok b

/-- Trait implementation: [multi_agent::protocols::voting::yes_count::{core::ops::function::FnOnce<(&'_ (&'_ ((u32, bool)))), bool> for multi_agent::protocols::voting::yes_count::closure}]
    Source: 'src/protocols/voting.rs', lines 54:30-54:41 -/
@[reducible]
def
  protocols.voting.yes_count.closure.Insts.CoreOpsFunctionFnOnceTupleSharedSharedPairU32BoolBool
  : core.ops.function.FnOnce protocols.voting.yes_count.closure (Std.U32 ×
  Bool) Bool := {
  call_once :=
    protocols.voting.yes_count.closure.Insts.CoreOpsFunctionFnOnceTupleSharedSharedPairU32BoolBool.call_once
}

/-- Trait implementation: [multi_agent::protocols::voting::yes_count::{core::ops::function::FnMut<(&'_ (&'_ ((u32, bool)))), bool> for multi_agent::protocols::voting::yes_count::closure}]
    Source: 'src/protocols/voting.rs', lines 54:30-54:41 -/
@[reducible]
def
  protocols.voting.yes_count.closure.Insts.CoreOpsFunctionFnMutTupleSharedSharedPairU32BoolBool
  : core.ops.function.FnMut protocols.voting.yes_count.closure (Std.U32 ×
  Bool) Bool := {
  FnOnceInst :=
    protocols.voting.yes_count.closure.Insts.CoreOpsFunctionFnOnceTupleSharedSharedPairU32BoolBool
  call_mut :=
    protocols.voting.yes_count.closure.Insts.CoreOpsFunctionFnMutTupleSharedSharedPairU32BoolBool.call_mut
}

/-- [multi_agent::protocols::voting::yes_count]:
    Source: 'src/protocols/voting.rs', lines 53:0-55:1 -/
def protocols.voting.yes_count
  (round : protocols.voting.VotingRound) : Result Std.U32 := do
  sorry

/-- [multi_agent::protocols::voting::no_count::closure]
    Source: 'src/protocols/voting.rs', lines 59:30-59:42 -/
@[reducible]
def protocols.voting.no_count.closure := Unit

/-- [multi_agent::protocols::voting::no_count::{core::ops::function::FnMut<(&'_ (&'_ ((u32, bool)))), bool> for multi_agent::protocols::voting::no_count::closure}::call_mut]:
    Source: 'src/protocols/voting.rs', lines 59:30-59:42 -/
def
  protocols.voting.no_count.closure.Insts.CoreOpsFunctionFnMutTupleSharedSharedPairU32BoolBool.call_mut
  (c : protocols.voting.no_count.closure) (tupled_args : (Std.U32 × Bool)) :
  Result (Bool × protocols.voting.no_count.closure)
  := do
  let (_, v) := tupled_args
  ok (¬ v, c)

/-- [multi_agent::protocols::voting::no_count::{core::ops::function::FnOnce<(&'_ (&'_ ((u32, bool)))), bool> for multi_agent::protocols::voting::no_count::closure}::call_once]:
    Source: 'src/protocols/voting.rs', lines 59:30-59:42 -/
def
  protocols.voting.no_count.closure.Insts.CoreOpsFunctionFnOnceTupleSharedSharedPairU32BoolBool.call_once
  (c : protocols.voting.no_count.closure) (p : (Std.U32 × Bool)) :
  Result Bool
  := do
  let (b, _) ←
    protocols.voting.no_count.closure.Insts.CoreOpsFunctionFnMutTupleSharedSharedPairU32BoolBool.call_mut
      c p
  ok b

/-- Trait implementation: [multi_agent::protocols::voting::no_count::{core::ops::function::FnOnce<(&'_ (&'_ ((u32, bool)))), bool> for multi_agent::protocols::voting::no_count::closure}]
    Source: 'src/protocols/voting.rs', lines 59:30-59:42 -/
@[reducible]
def
  protocols.voting.no_count.closure.Insts.CoreOpsFunctionFnOnceTupleSharedSharedPairU32BoolBool
  : core.ops.function.FnOnce protocols.voting.no_count.closure (Std.U32 ×
  Bool) Bool := {
  call_once :=
    protocols.voting.no_count.closure.Insts.CoreOpsFunctionFnOnceTupleSharedSharedPairU32BoolBool.call_once
}

/-- Trait implementation: [multi_agent::protocols::voting::no_count::{core::ops::function::FnMut<(&'_ (&'_ ((u32, bool)))), bool> for multi_agent::protocols::voting::no_count::closure}]
    Source: 'src/protocols/voting.rs', lines 59:30-59:42 -/
@[reducible]
def
  protocols.voting.no_count.closure.Insts.CoreOpsFunctionFnMutTupleSharedSharedPairU32BoolBool
  : core.ops.function.FnMut protocols.voting.no_count.closure (Std.U32 × Bool)
  Bool := {
  FnOnceInst :=
    protocols.voting.no_count.closure.Insts.CoreOpsFunctionFnOnceTupleSharedSharedPairU32BoolBool
  call_mut :=
    protocols.voting.no_count.closure.Insts.CoreOpsFunctionFnMutTupleSharedSharedPairU32BoolBool.call_mut
}

/-- [multi_agent::protocols::voting::no_count]:
    Source: 'src/protocols/voting.rs', lines 58:0-60:1 -/
def protocols.voting.no_count
  (round : protocols.voting.VotingRound) : Result Std.U32 := do
  sorry

/-- [multi_agent::protocols::voting::voting_is_complete]:
    Source: 'src/protocols/voting.rs', lines 63:0-65:1 -/
def protocols.voting.voting_is_complete
  (round : protocols.voting.VotingRound) : Result Bool := do
  let i := alloc.vec.Vec.len round.votes
  let i1 ← lift (UScalar.cast .U32 i)
  ok (i1 >= round.expected_voters)

/-- Trait implementation: [multi_agent::router::{core::marker::StructuralPartialEq for multi_agent::router::TopicSubscription}]
    Source: 'src/router.rs', lines 4:16-4:25 -/
@[reducible]
def router.TopicSubscription.Insts.CoreMarkerStructuralPartialEq :
  core.marker.StructuralPartialEq router.TopicSubscription := {
}

/-- [multi_agent::router::{core::cmp::Eq for multi_agent::router::TopicSubscription}::assert_receiver_is_total_eq]:
    Source: 'src/router.rs', lines 4:27-4:29 -/
def router.TopicSubscription.Insts.CoreCmpEq.assert_receiver_is_total_eq
  (self : router.TopicSubscription) : Result Unit := do
  ok ()

/-- Trait implementation: [multi_agent::router::{core::cmp::Eq for multi_agent::router::TopicSubscription}]
    Source: 'src/router.rs', lines 4:27-4:29 -/
@[reducible]
def router.TopicSubscription.Insts.CoreCmpEq : core.cmp.Eq
  router.TopicSubscription := {
  partialEqInst :=
    router.TopicSubscription.Insts.CoreCmpPartialEqTopicSubscription
  assert_receiver_is_total_eq :=
    router.TopicSubscription.Insts.CoreCmpEq.assert_receiver_is_total_eq
}

/-- Trait implementation: [multi_agent::router::{core::clone::Clone for multi_agent::router::Router}]
    Source: 'src/router.rs', lines 11:9-11:14 -/
@[reducible]
def router.Router.Insts.CoreCloneClone : core.clone.Clone router.Router := {
  clone := router.Router.Insts.CoreCloneClone.clone
}

/-- Trait implementation: [multi_agent::router::{core::marker::StructuralPartialEq for multi_agent::router::Router}]
    Source: 'src/router.rs', lines 11:16-11:25 -/
@[reducible]
def router.Router.Insts.CoreMarkerStructuralPartialEq :
  core.marker.StructuralPartialEq router.Router := {
}

/-- [multi_agent::router::{core::cmp::PartialEq<multi_agent::router::Router> for multi_agent::router::Router}::ne]:
    Source: 'src/router.rs', lines 11:16-11:25 -/
axiom router.Router.Insts.CoreCmpPartialEqRouter.ne
  : router.Router → router.Router → Result Bool

/-- Trait implementation: [multi_agent::router::{core::cmp::PartialEq<multi_agent::router::Router> for multi_agent::router::Router}]
    Source: 'src/router.rs', lines 11:16-11:25 -/
@[reducible]
def router.Router.Insts.CoreCmpPartialEqRouter : core.cmp.PartialEq
  router.Router router.Router := {
  eq := router.Router.Insts.CoreCmpPartialEqRouter.eq
  ne := router.Router.Insts.CoreCmpPartialEqRouter.ne
}

/-- [multi_agent::router::{core::cmp::Eq for multi_agent::router::Router}::assert_receiver_is_total_eq]:
    Source: 'src/router.rs', lines 11:27-11:29 -/
def router.Router.Insts.CoreCmpEq.assert_receiver_is_total_eq
  (self : router.Router) : Result Unit := do
  ok ()

/-- Trait implementation: [multi_agent::router::{core::cmp::Eq for multi_agent::router::Router}]
    Source: 'src/router.rs', lines 11:27-11:29 -/
@[reducible]
def router.Router.Insts.CoreCmpEq : core.cmp.Eq router.Router := {
  partialEqInst := router.Router.Insts.CoreCmpPartialEqRouter
  assert_receiver_is_total_eq :=
    router.Router.Insts.CoreCmpEq.assert_receiver_is_total_eq
}

/-- [multi_agent::router::router_new]:
    Source: 'src/router.rs', lines 17:0-21:1 -/
def router.router_new : Result router.Router := do
  ok { subscriptions := (alloc.vec.Vec.new router.TopicSubscription) }

/-- [multi_agent::router::router_subscribe]:
    Source: 'src/router.rs', lines 24:0-28:1 -/
def router.router_subscribe
  (router : router.Router) (topic_id : Std.U32) (agent_id : Std.U32) :
  Result router.Router
  := do
  let v ←
    alloc.vec.Vec.push router.subscriptions ({ topic_id, agent_id } :
      router.TopicSubscription)
  ok { subscriptions := v }

/-- Trait implementation: [multi_agent::scheduler::{core::clone::Clone for multi_agent::scheduler::SchedulerKind}]
    Source: 'src/scheduler.rs', lines 4:9-4:14 -/
@[reducible]
def scheduler.SchedulerKind.Insts.CoreCloneClone : core.clone.Clone
  scheduler.SchedulerKind := {
  clone := scheduler.SchedulerKind.Insts.CoreCloneClone.clone
}

/-- Trait implementation: [multi_agent::scheduler::{core::marker::StructuralPartialEq for multi_agent::scheduler::SchedulerKind}]
    Source: 'src/scheduler.rs', lines 4:16-4:25 -/
@[reducible]
def scheduler.SchedulerKind.Insts.CoreMarkerStructuralPartialEq :
  core.marker.StructuralPartialEq scheduler.SchedulerKind := {
}

/-- [multi_agent::scheduler::{core::cmp::PartialEq<multi_agent::scheduler::SchedulerKind> for multi_agent::scheduler::SchedulerKind}::ne]:
    Source: 'src/scheduler.rs', lines 4:16-4:25 -/
axiom scheduler.SchedulerKind.Insts.CoreCmpPartialEqSchedulerKind.ne
  : scheduler.SchedulerKind → scheduler.SchedulerKind → Result Bool

/-- Trait implementation: [multi_agent::scheduler::{core::cmp::PartialEq<multi_agent::scheduler::SchedulerKind> for multi_agent::scheduler::SchedulerKind}]
    Source: 'src/scheduler.rs', lines 4:16-4:25 -/
@[reducible]
def scheduler.SchedulerKind.Insts.CoreCmpPartialEqSchedulerKind :
  core.cmp.PartialEq scheduler.SchedulerKind scheduler.SchedulerKind := {
  eq := scheduler.SchedulerKind.Insts.CoreCmpPartialEqSchedulerKind.eq
  ne := scheduler.SchedulerKind.Insts.CoreCmpPartialEqSchedulerKind.ne
}

/-- [multi_agent::scheduler::{core::cmp::Eq for multi_agent::scheduler::SchedulerKind}::assert_receiver_is_total_eq]:
    Source: 'src/scheduler.rs', lines 4:27-4:29 -/
def scheduler.SchedulerKind.Insts.CoreCmpEq.assert_receiver_is_total_eq
  (self : scheduler.SchedulerKind) : Result Unit := do
  ok ()

/-- Trait implementation: [multi_agent::scheduler::{core::cmp::Eq for multi_agent::scheduler::SchedulerKind}]
    Source: 'src/scheduler.rs', lines 4:27-4:29 -/
@[reducible]
def scheduler.SchedulerKind.Insts.CoreCmpEq : core.cmp.Eq
  scheduler.SchedulerKind := {
  partialEqInst := scheduler.SchedulerKind.Insts.CoreCmpPartialEqSchedulerKind
  assert_receiver_is_total_eq :=
    scheduler.SchedulerKind.Insts.CoreCmpEq.assert_receiver_is_total_eq
}

/-- Trait implementation: [multi_agent::scheduler::{core::clone::Clone for multi_agent::scheduler::Scheduler}]
    Source: 'src/scheduler.rs', lines 11:9-11:14 -/
@[reducible]
def scheduler.Scheduler.Insts.CoreCloneClone : core.clone.Clone
  scheduler.Scheduler := {
  clone := scheduler.Scheduler.Insts.CoreCloneClone.clone
}

/-- Trait implementation: [multi_agent::scheduler::{core::marker::StructuralPartialEq for multi_agent::scheduler::Scheduler}]
    Source: 'src/scheduler.rs', lines 11:16-11:25 -/
@[reducible]
def scheduler.Scheduler.Insts.CoreMarkerStructuralPartialEq :
  core.marker.StructuralPartialEq scheduler.Scheduler := {
}

/-- [multi_agent::scheduler::{core::cmp::PartialEq<multi_agent::scheduler::Scheduler> for multi_agent::scheduler::Scheduler}::ne]:
    Source: 'src/scheduler.rs', lines 11:16-11:25 -/
axiom scheduler.Scheduler.Insts.CoreCmpPartialEqScheduler.ne
  : scheduler.Scheduler → scheduler.Scheduler → Result Bool

/-- Trait implementation: [multi_agent::scheduler::{core::cmp::PartialEq<multi_agent::scheduler::Scheduler> for multi_agent::scheduler::Scheduler}]
    Source: 'src/scheduler.rs', lines 11:16-11:25 -/
@[reducible]
def scheduler.Scheduler.Insts.CoreCmpPartialEqScheduler : core.cmp.PartialEq
  scheduler.Scheduler scheduler.Scheduler := {
  eq := scheduler.Scheduler.Insts.CoreCmpPartialEqScheduler.eq
  ne := scheduler.Scheduler.Insts.CoreCmpPartialEqScheduler.ne
}

/-- [multi_agent::scheduler::{core::cmp::Eq for multi_agent::scheduler::Scheduler}::assert_receiver_is_total_eq]:
    Source: 'src/scheduler.rs', lines 11:27-11:29 -/
def scheduler.Scheduler.Insts.CoreCmpEq.assert_receiver_is_total_eq
  (self : scheduler.Scheduler) : Result Unit := do
  ok ()

/-- Trait implementation: [multi_agent::scheduler::{core::cmp::Eq for multi_agent::scheduler::Scheduler}]
    Source: 'src/scheduler.rs', lines 11:27-11:29 -/
@[reducible]
def scheduler.Scheduler.Insts.CoreCmpEq : core.cmp.Eq scheduler.Scheduler := {
  partialEqInst := scheduler.Scheduler.Insts.CoreCmpPartialEqScheduler
  assert_receiver_is_total_eq :=
    scheduler.Scheduler.Insts.CoreCmpEq.assert_receiver_is_total_eq
}

/-- [multi_agent::scheduler::scheduler_round_robin]:
    Source: 'src/scheduler.rs', lines 20:0-28:1 -/
def scheduler.scheduler_round_robin
  (agent_ids : alloc.vec.Vec Std.U32) : Result scheduler.Scheduler := do
  let n := alloc.vec.Vec.len agent_ids
  let v ← alloc.vec.from_elem core.clone.CloneU32 0#u32 n
  ok
    {
      kind := scheduler.SchedulerKind.RoundRobin,
      agent_ids,
      current_index := 0#u32,
      turns_given := v
    }

/-- [multi_agent::scheduler::scheduler_priority]:
    Source: 'src/scheduler.rs', lines 31:0-39:1 -/
def scheduler.scheduler_priority
  (agent_ids : alloc.vec.Vec Std.U32) : Result scheduler.Scheduler := do
  let n := alloc.vec.Vec.len agent_ids
  let v ← alloc.vec.from_elem core.clone.CloneU32 0#u32 n
  ok
    {
      kind := scheduler.SchedulerKind.Priority,
      agent_ids,
      current_index := 0#u32,
      turns_given := v
    }

/-- [multi_agent::scheduler::turns_for_agent]: loop body 0:
    Source: 'src/scheduler.rs', lines 78:4-84:1 -/
@[rust_loop_body]
def scheduler.turns_for_agent_loop.body
  (v : alloc.vec.Vec Std.U32) (v1 : alloc.vec.Vec Std.U32) (agent_id : Std.U32)
  (iter : core.ops.range.Range Std.Usize) :
  Result (ControlFlow (core.ops.range.Range Std.Usize) Std.U32)
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok (done 0#u32)
  | some i =>
    let i1 ←
      alloc.vec.Vec.index (core.slice.index.SliceIndexUsizeSlice Std.U32) v i
    if i1 = agent_id
    then
      let i2 ←
        alloc.vec.Vec.index (core.slice.index.SliceIndexUsizeSlice Std.U32) v1
          i
      ok (done i2)
    else ok (cont iter1)

/-- [multi_agent::scheduler::turns_for_agent]: loop 0:
    Source: 'src/scheduler.rs', lines 78:4-84:1 -/
@[rust_loop]
def scheduler.turns_for_agent_loop
  (iter : core.ops.range.Range Std.Usize) (v : alloc.vec.Vec Std.U32)
  (v1 : alloc.vec.Vec Std.U32) (agent_id : Std.U32) :
  Result Std.U32
  := do
  loop
    (fun iter1 => scheduler.turns_for_agent_loop.body v v1 agent_id iter1)
    iter

/-- [multi_agent::scheduler::turns_for_agent]:
    Source: 'src/scheduler.rs', lines 77:0-84:1 -/
def scheduler.turns_for_agent
  (sched : scheduler.Scheduler) (agent_id : Std.U32) : Result Std.U32 := do
  let i := alloc.vec.Vec.len sched.agent_ids
  scheduler.turns_for_agent_loop { start := 0#usize, «end» := i }
    sched.agent_ids sched.turns_given agent_id

end multi_agent
